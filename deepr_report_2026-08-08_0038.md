## 🔴 En Kritik 3 Madde

- **[KRİTİK]** Uygulamada hiçbir kimlik doğrulama/yetkilendirme katmanı yok ve servis Cloudflare tüneliyle doğrudan internete açık (`server.py`, `serve.py`); chat mesajları HTML-escape edilmeden `|safe` ile basılıyor (XSS) — `web/templates/fragments/chat_thread.html:33` vb. Kimliksiz herhangi bir internet kullanıcısı strateji kodu üretme/exec etme, backtest tetikleme ve XSS payload'ı çalıştırma yeteneğine sahip. _(security)_
- **[KRİTİK]** İki bağımsız backtest/metrik motoru (`backtest.py` ve `strategy_studio/backtest.py`) max-drawdown'ı **ters işaretle** hesaplıyor (biri negatif fraksiyon, diğeri pozitif) ve farklı annualization varsayılanı (365 vs 252) kullanıyor — motorlar arası metrik karşılaştırması sessizce yanlış sonuç üretir. _(architecture: backtest.py:559, strategy_studio/backtest.py:656)_
- **[KRİTİK]** `calc_rsi`'a negatif `period` verildiğinde hata fırlamıyor, listenin sonundan sarmalanıp "geçerli görünen" ama anlamsız bir değer üretiyor; bu fonksiyon LLM üretimi custom block'lara açık ve genel `try/except` bunu yakalamıyor — sessizce yanlış RSI ile backtest kararı üretilebilir. _(edge case: indicators.py:29-51)_

---

## Performans Review
- **YÜKSEK** Token ledger özeti (`token_ledger.py:185`) her istekte dosyayı baştan okuyor, önbelleksiz; `web/routes/tokens.py:45-46` bunu istekte iki kez çağırıyor, `base.html:90-91` her sayfada 60 saniyede bir otomatik tetikliyor.
- **ORTA** Custom-block registry (`custom_block_store.py:162`) her `load_catalog()` çağrısında önbelleksiz okunuyor; `strategy.py:1114-1115`'te aynı istekte katalog+registry çifte okunuyor.
- **ORTA** `indicators.py:81-90` `sma()` O(n·period) — kayan toplama çevrilebilir.
- **DÜŞÜK** `backtest.py:1050` trade işaretçileri `iterrows()` ile satır satır çıkarılıyor (chart.py zaten column-based'e geçmişken).
- ✅ Genel olarak performans konusunda bilinçli (composer buffer'ları, ProcessPoolExecutor, column-based chart erişimi).

## Mimari Review
- **KRİTİK** İki bağımsız backtest/metrik motoru, ters işaretli max-DD + farklı annualization (yukarıda, en kritik 3'te).
- **YÜKSEK** `agent.py` 3957 satır god-module (LLM sağlayıcı + prompt + kod üretimi + test).
- **YÜKSEK** `web/routes/agent_backtest.py` 4760 satır — route katmanı iş mantığını da barındırıyor.
- **YÜKSEK** `composer.py` SRP ihlali — indikatör eval + dinamik modül yükleme + persistence + Nautilus Strategy tanımı aynı dosyada.
- **ORTA** Config 25+37 ayrı `os.environ.get` çağrısına dağılmış, merkezi settings yok.
- **ORTA** `web/mission.py`, `agent_backtest.py`'nin private state'ine isim-tabanlı gizli sözleşmeyle erişiyor.
- **ORTA** `server.py`'de process-global mutable state (`_context`), DI yok.
- **ORTA** `strategy_studio/store.py` (SQLite) vs `custom_block_store.py` (JSON) — aynı problem için iki ayrı persistence stratejisi.
- **DÜŞÜK** Dairesel bağımlılık fonksiyon-içi deferred import'larla maskeleniyor (agent_backtest.py'de 23 örnek).

## Code Review
- **YÜKSEK** God dosyalar: agent.py (3957), agent_backtest.py (4760), backtest.py route (2624), composer.py (2486), data.py (2267), backtest.py (2094).
- **ORTA** ~228 yerde sessizce yutulan `except Exception: pass/continue`.
- **ORTA** `raise ... from` zinciri kırık (~32 yer, ruff B904) — traceback'te kök neden kayboluyor.
- **DÜŞÜK** `zip()` `strict=` olmadan kullanılıyor (3 yer) — sessiz kesme riski.
- **DÜŞÜK** `_find_swing_highs/lows`, `calc_rsi/calc_rsi_series` kod tekrarı.
- **DÜŞÜK** Repo kökünde ~15+ atık debug/scratch dosyası (`_e2e*.py`, `ck.txt`, `_studio.html` ~1.7MB vb.).
- **BİLGİ** `unsloth_compiled_cache/` (LLM fine-tuning cache, 19 dosya) projeyle ilgisiz, kökte duruyor.
- **DÜŞÜK** `web/routes/backtest.py:1322` kullanılmayan döngü değişkeni.

## Güvenlik (Security) Review
- **KRİTİK** Auth/authz yok + internete açık (yukarıda, en kritik 3'te).
- **YÜKSEK** Chat mesajları unescaped `|safe` ile render — XSS (`chat_thread.html:33` ve 2 benzer template).
- **BİLGİ** Diğer alanlar (AST sandbox, subprocess izolasyonu, path-traversal koruması, parametrik SQL, env'den okunan API key'ler) zaten sıkı sertleştirilmiş.

## Brutal Review
- **KRİTİK** `_agent_worker` (`web/routes/agent_backtest.py:1888`) 1749 satır tek fonksiyon, 15+ iç içe closure — güvenle değiştirilemez.
- **YÜKSEK** agent.py + agent_backtest.py "dev-monolit" (98 ve 65 top-level fonksiyon).
- **YÜKSEK** Çalışma ağacında 946 satırlık commit edilmemiş diff + onlarca atık dosya proje köküne dökülmüş.
- **YÜKSEK** `unsloth_compiled_cache/` alakasız 21 trainer dosyasıyla kökte duruyor.
- **ORTA** Preview/smoke exec yolu (`strategy.py:240`, `composer.py:1136`) sandbox'ın process izolasyonuna hiç girmiyor — tek katman AST allowlist.
- **ORTA** `backtest.py` içinde 3 ayrı route neredeyse aynı `_worker` closure'ını kopyala-yapıştır tekrarlıyor.
- **ORTA** ruff'ta E501 kapalı, bugbear (B) hâlâ açılmamış — "future tightening" olarak itiraf edilmiş.
- **DÜŞÜK** ARCHITECTURE.md, motor geçişinin (BacktestEngine→BacktestNode) yarım kaldığını itiraf ediyor.
- **DÜŞÜK** Son 5 commit art arda aynı AUTO/agent alt sistemini "harden" ediyor — en kırılgan parça.

## End-User Negatif Test
- **YÜKSEK** CSV export'ta formül enjeksiyonu (Excel) — strateji adı sanitize edilmeden `csv.writer`'a yazılıyor (`reports.py:727`).
- **YÜKSEK** `/lab/run` hızlı-tekrar-tıklamaya karşı korumasız (diğer route'larda var, burada yok).
- **ORTA** `/robustness/run` yalnız client-side korumalı, server-side session guard yok.
- **ORTA** Boş/whitespace strateji adı kabul ediliyor.
- **ORTA** `/chart/data` sembol/kategori beyaz listeye karşı doğrulanmıyor, hata mesajı iç bilgi sızdırıyor.
- **ORTA** LLM'e giden metin alanlarında üst uzunluk sınırı yok — büyük girdi pahalı API çağrısına gidiyor.
- **DÜŞÜK** Custom block/strateji kataloğu process-global, oturum izolasyonu yok.

## End-User Pozitif Test
- **KRİTİK** "Strategy Builder" linki seed edilmemiş bir demo stratejisine sabit (`base.html:50`) — temiz kurulumda 404.
- **YÜKSEK** "Boş tarih = tüm önbellek" metni yanlış (gerçekte son 365 gün); `_DEFAULT_MAX_BARS` sabiti tanımlı ama hiç kullanılmıyor.
- **ORTA** AUTO brifing alanı boş değil, Türkçe test verisiyle dolu geliyor (`studio.html:97-111`) — dev verisi production'a sızmış.
- **ORTA** Backtest "verdict" rozeti aniden Türkçe, arayüzün geri kalanı İngilizce.
- **DÜŞÜK** Sidebar "ENGINE" kartı hep aynı sabit metni gösteriyor, gerçek model bilgisiyle beslenmiyor.

## Sınır Durumu (Edge Case) Testleri
- **KRİTİK** `calc_rsi` negatif period'da sessizce yanlış değer (yukarıda, en kritik 3'te).
- **KRİTİK** `calc_rsi(closes, 0)` → ZeroDivisionError, sandbox `except` ile yutuluyor, blok sessizce "sinyal yok" dönüyor.
- **YÜKSEK** Aynı sınıf hata `calc_atr`/`calc_adx`/`calc_volume_change`/`calc_nadaraya_watson`'da da var, hiçbiri period<=0 koruması içermiyor.
- **YÜKSEK** `chart_indicators.py::_rsi` period<=0 koruması yok, hata `chart.py`'nin geniş except'iyle yutuluyor.
- **ORTA** `price_breakout` overlay lookback=0'da `ValueError` (boş liste max/min).
- **ORTA** `_max_dd_from_series`/`run_monte_carlo` sıfır/negatif `peak`'e karşı korumasız (şu an dolaylı korunuyor ama fonksiyon kendisi savunmasız).
- **DÜŞÜK** `token_ledger.summary()` ts karşılaştırması ham string karşılaştırmasına dayanıyor.
- **BİLGİ** Kod tabanı birçok edge case'e karşı zaten bilinçli sertleştirilmiş (WFO window bounds, OOS split, Monte Carlo boş trade kontrolü).

## Entegrasyon Testleri
- **KRİTİK** `HttpAnthropicClient.complete()` (strategy_studio/ai.py:67-87) hiç retry/backoff yok — tek transient hata tüm AUTO döngüsünü kalıcı öldürüyor; agent.py'nin ana LLM yolu tam tersine son derece dayanıklı.
- **YÜKSEK** Production'da kullanılan `HttpAnthropicClient` hiçbir entegrasyon testinde gerçek ağ hatasına karşı test edilmiyor (testler mock kullanıyor).
- **ORTA** `HttpAnthropicClient`'ta model/max_tokens sabit kodlanmış, agent.py'nin öğrenilen-tavan mekanizmasından kopuk.
- **ORTA** `download_massive.py` bir döngü ortasında hata olursa önceki ticker'ların verisi manifest'e hiç girmeden kataloga yazılmış kalabiliyor (download_flatfiles.py'nin atomik deseninden yoksun).
- **DÜŞÜK** Canlı Bybit sandbox WebSocket entegrasyonu uçtan uca test edilmiyor (tasarım gereği düşük risk).

## Statik Analiz & Lint
- **BİLGİ** `unsloth_compiled_cache/` ruff'ın 2529 hatasının 2502'sini üretiyor, gerçek sinyali boğuyor; extend-exclude'a eklenmeli.
- **DÜŞÜK** Gerçek git-tracked kod (137 dosya) projenin kendi ruff kural setinde tamamen temiz; kalan 27 hata izlenmeyen atık scriptlerde.
- **ORTA** `raise ... from e` zinciri kurulmuyor (data.py'de 9+, mutations.py'de 6, vb.).
- **DÜŞÜK** Bugbear backlog'u ölçülebilir boyutta (32× B904, 61× broad except, 17× mutable default).
- **DÜŞÜK** `agent_backtest.py`'de closure-over-loop-variable (B023) deseni.
- **BİLGİ (pozitif)** `exec()`/`subprocess` kullanımlarının hepsi kasıtlı sandbox/whitelist korumalı.

## Sessiz Hata & Gözlemlenebilirlik
- **KRİTİK** `token_ledger.record()` — tüm yazma bloğu bare `except: pass`, dosyada `logging` hiç import edilmemiş; disk/izin hatası maliyet defterini sessizce durdurur.
- **KRİTİK** Robustness (WFO/MC/OOS) kayıt yazma tamamen `except Exception: pass` ile yutuluyor — arayüz "tamamlandı" gösterir, sonuç sütunları kalıcı boş kalır.
- **YÜKSEK** `Composer.on_bar` MTM equity snapshot hatası sessizce atlanıyor — seyreltilmiş/güvenilmez equity eğrisi.
- **YÜKSEK** `/sessions/{run_id}` `_read_events` kısmi okuma hatasında sessizce eksik ama tutarlı görünen özet basıyor.
- **YÜKSEK** AUTO custom block'un forensic `.py` kopyası yazılırken hata sessiz yutuluyor (aynı dosyada başka bir hata için var olan `audit_degraded` sinyali burada yok).
- **YÜKSEK** LLM istemcisi kurulamazsa (`_get_client()` başarısız) sessizce fallback'e düşülüyor, `degraded` işaretlenmiyor (birkaç satır altındaki asıl çağrı hatası doğru işaretleniyor).
- **YÜKSEK** `parallel_exec.py` worker'da stdout/stderr None senaryosu `sandbox.py`'deki gibi devnull'a yönlendirilmiyor, yalnız AttributeError yutuluyor.
- **ORTA** Deploy gate mesajı yanlış metrik adını gösterebiliyor (`objective` eşleşmezse sessizce `dsr`'a düşüyor, mesaj hâlâ eski adı yazıyor).
- **ORTA** "Kayıt yok" ile "log okunamadı" ayırt edilemiyor (backtest.py `_recent_runs`, reports.py `_parsed_log_records`).
- **ORTA** `data.py` catalog hataları `log.warning` yerine çıplak `print()` ile bildiriliyor — headless dağıtımda kayboluyor.

## Uçtan Uca (E2E) Testler
- **KRİTİK** `/backtest/run` → `/backtest/progress/{run_id}` tam döngüsü hiçbir testte gerçekten sürülmüyor.
- **YÜKSEK** "Strateji üret → backtest'e zincirle" akışı yalnız HTML attribute seviyesinde test ediliyor, fiili tetiklenme test edilmiyor.
- **YÜKSEK** Kök dizindeki manuel Playwright script'leri (`_e2e_auto.py` vb.) CI'a bağlı değil, versiyon kontrolünde değil.
- **ORTA** Soğuk-başlangıç (boş Bybit cache) → backtest-run kırılma zinciri testsiz.
- **ORTA** Gerçek Nautilus BacktestEngine + parquet cache üzerinden POST→sonuç uçtan uca entegrasyon testi yok.
- **ORTA** `_save_result_snapshot` best-effort sessiz hata — yazım başarısız olursa geçmiş sonuç tekrar açılamıyor, test edilmiyor.
- **DÜŞÜK** `GET /` (dashboard) hiçbir testte doğrulanmıyor.

## Birim (Unit) Testleri
- **KRİTİK** `agent.py` (3957 satır) neredeyse hiç unit-test edilmemiş — retry/backoff, rate-limit tespiti, JSON onarma, spec doğrulama gibi ~90 saf fonksiyon test edilmiyor.
- **YÜKSEK** `codegate.py`'deki `compile_with_loop_budget`/`_LoopBudgetInjector` (güvenlik-kritik loop-budget enjektörü) sıfır test kapsamı.
- **YÜKSEK** `IBFixedFeeModel.get_commission` hiçbir testte sayısal doğrulanmıyor — PnL sessizce bozulabilir.
- **ORTA** `run_backtest_node`/`run_composed_backtest_node` (BacktestNode yolu) sıfır test referansı.
- **ORTA** `custom_block_store.py`'deki isim/kimlik doğrulama fonksiyonları test edilmiyor.
- **DÜŞÜK** `parallel_exec.py` worker-sayısı otomatik-hesaplama mantığı test edilmiyor.
- **BİLGİ** `token_ledger.py` en iyi test edilmiş modül — örnek alınabilir kalite.
