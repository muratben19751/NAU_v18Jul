## Performans Review
- **[YÜKSEK]** calc_nadaraya_watson() O(n²) — her bar'da tam pencere üzerinde çift döngü — calc_nadaraya_watson, `for i in range(n): for j in range(n): ... math.exp(...)` şeklinde saf Python'da tam O(n²) çift döngü. Gauss çekirdeği bandwidth'in birkaç katından sonra pratik olarak sıfıra yaklaştığı halde, tüm n×n mesafe/ağırlık çifti hesaplanıyor — halbuki pencere ±k·bandwidth ile kırpılsa veya numpy broadcasting kullanılsa O(n) veya çok daha ucuz olurdu. composer.py:1177'de `closes_view = closes[-cap:] if cap else list(closes)` ile her bar'da (backtest'in HER mumu için) bu fonksiyon tekrar çağrılıyor; cap tipik olarak NAU_WINDOW=260 civarında sabitleniyor (composer.py:1734-1744), yani bar başına ~260²≈67.600 exp() çağrısı. 50.000 barlık (≈1 yıllık 1 dakikalık) bir backtest'te bu ~3.4 milyar exp() çağrısına denk gelir — tüm sinyal değerlendirme akışının en ağır tekil maliyeti olur. Fonksiyon codegate.py'nin allow-list'inde olduğu için hem kullanıcı hem AUTO ajanının ürettiği custom blocklar bunu serbestçe çağırabiliyor; AUTO sürekli mod onlarca-yüzlerce strateji denediği için bu darboğaz sistemli şekilde tetiklenebilir. _(indicators.py:336-353 (calc_nadaraya_watson), tetikleyen: composer.py:1167-1199 (_eval_wrapper → custom block evaluate()), strategy_studio/registry.py:87-92 (_impl_nadaraya_watson), codegate.py:189 (custom blok allow-list'inde))_
- **[ORTA]** sma() saf O(n × period) — kayan toplam yerine her pencerede sıfırdan yeniden toplama — sma(values, period) dış döngüde her `i` için iç döngüde `j in range(i-period+1, i+1)` ile pencereyi baştan toplayıp bölüyor — klasik kayan-toplam (running sum: yeni eleman ekle, eskiyi çıkar) yerine O(n×period) iş yapıyor. Aynı dosyadaki ema() fonksiyonu doğru şekilde O(n) yazılmışken (prev = value*k + prev*(1-k)), sma() bu optimizasyonu almamış. calc_stoch_rsi bunu iki kez zincirleme çağırıyor (sma(stoch_k, k_period), sma(k_smoothed, d_period)) ve bu da her indikatör/chart yenilemesinde tekrar çalışıyor. period sabit ve küçük (tipik 3-200) olduğundan tek başına felaket değil, ama chart_indicators.py üzerinden her sayfa/chart yenilemesinde tam seri üzerinde tekrarlanan, kolayca O(n)'e indirgenebilecek gereksiz bir sabit-çarpanlı yavaşlık. _(indicators.py:85-94 (sma), kullanan: indicators.py:129-130 (calc_stoch_rsi içindeki k_smoothed/d_smoothed hesapları))_
- **[ORTA]** custom_block_store._read_registry() önbelleksiz — composer.load_catalog()'un ~18 sıcak çağrı noktasında her seferinde diskten tam JSON okuma/parse — composer.py:1587-1618'de _read_catalog_raw() catalog.json için (mtime, size) bazlı bir önbellek eklenmiş — yorum satırı bunu açıkça 'load_catalog runs on hot paths (~18 call sites incl. the agent inner loop) and re-read + re-parsed the file every call' diyerek gerekçelendiriyor. Ancak aynı load_catalog() içinde her çağrıda ayrıca `cbs.list_custom()` çağrılıyor (composer.py:1634) ve bu, custom_block_store.py:162'deki _read_registry()'ye gidiyor — orada dosya HER ÇAĞRIDA registry.json'ı yeniden okuyup json.loads ile parse ediyor, hiçbir mtime/size önbelleği yok. Yani catalog.json tarafı optimize edilmişken, aynı fonksiyonun bağımlı olduğu registry.json tarafı optimize edilmemiş kalmış — asimetrik bir düzeltme. Dosyanın kendi docstring'i (custom_block_store.py:305-309) 'these accumulate in bulk (hundreds)' diyerek AUTO'nun ürettiği desc_/agnt_ bloklarının yüzlerce kayda çıkabildiğini belirtiyor; web/routes/{dashboard,strategy,backtest,robustness,chart,studio}.py gibi neredeyse her rotanın load_catalog() çağırdığı göz önüne alınırsa, her HTTP isteğinde büyümüş registry.json'ın tekrar tekrar diskten okunup parse edilmesi önbelleklenmeyen tekrar hesap örneğidir. _(custom_block_store.py:162-206 (_read_registry, list_custom→_read_registry zinciri), çağıran: composer.py:1621-1663 (load_catalog → cbs.list_custom()))_
- **[ORTA]** token_ledger.summary() her çağrıda TÜM JSONL dosyasını baştan okuyup parse ediyor; /tokens/badge her istekte bunu iki kez yapıyor ve 60 sn'de bir + her sayfa yüklemesinde poll ediliyor — summary() dosyayı satır satır açıp her satırı json.loads ile parse ederek toplamları sıfırdan yeniden hesaplıyor — artımlı/kalıcı bir sayaç veya son-pozisyon (offset) bazlı devam etme yok. web/routes/tokens.py:45-46'da tek bir /tokens/badge isteğinde summary() İKİ KEZ çağrılıyor (biri since=SERVER_STARTED_AT ile, biri parametresiz 'all_time' için) — since filtresi de dosyayı kısaltmıyor, yalnızca parse edilmiş satırları filtreliyor, yani aynı dosya iki kez tam taranıyor. Bu endpoint base.html:93-94'te sidebar'da `hx-trigger="load, every 60s"` ile her sayfa açılışında ve açık her sekmede dakikada bir otomatik poll ediliyor. Ledger dosyası (~/.cache/nautilus_web_app/token_usage.jsonl) hiçbir rotasyon/kırpma mekanizmasına sahip değil ve uzun süren AUTO/continuous-mode oturumlarında (saatler süren, çok sayıda LLM çağrısı yapan) sürekli büyüyor; bu da her 60 sn'lik poll'un maliyetinin oturum ilerledikçe artması anlamına geliyor — klasik 'her pollda sıfırdan tam-dosya taraması' antipattern'i. _(token_ledger.py:187-331 (summary), web/routes/tokens.py:43-58 (tokens_badge), web/templates/base.html:93-94 (hx-trigger="load, every 60s"))_
- **[DÜŞÜK]** data.py _bybit_rows()/_index_rows() /data sayfası her açıldığında tüm sembol×kategori×interval hücrelerini önbelleksiz yeniden hesaplıyor — _bybit_rows(), 3 sembol × 3 kategori × 8 interval = 72 hücrenin her biri için _instrument_meta() (Nautilus Instrument nesnesi kurulumu), _read_parquet_stats() (parquet footer okuma) ve nautilus_catalog_bar_state() (dizin glob + her parquet dosyası için pyarrow read_metadata) çağırıyor; _index_rows() de limit=50 varsayılanla benzer şekilde her ticker için birden çok granularity'de aynı işi tekrarlıyor. list_catalog() bunları hiçbir önbellekleme/mtime kontrolü olmadan her /data GET isteğinde sıfırdan çalıştırıyor — oysa aynı dosyadaki _external_catalog_rows() (data.py:1635 civarı) parquet satır sayımını yalnızca gösterilen `limit` satır için yapacak şekilde bilinçli olarak optimize edilmiş, _bybit_rows() ise bu optimizasyonu almamış ve TÜM 72 hücreyi her seferinde hesaplıyor. Not: iş zaten web/routes/data.py:97'de `asyncio.to_thread` ile thread'e alınmış olduğundan event loop'u bloklamıyor (H93 yorumu bunu açıkça belirtiyor) — bu yüzden ciddiyeti düşük, ama yine de sayfa her ziyaret edildiğinde gereksiz tekrarlanan disk I/O'su. _(data.py:1130-1193 (_bybit_rows), data.py:1196-1258 (_index_rows), çağıran: data.py:2085-2124 (list_catalog), web/routes/data.py:88-114 (GET /data))_

## Mimari Review
- **[YÜKSEK]** web/routes/agent_backtest.py içindeki _agent_worker (~1750 satır) monolitik, mimariyi ihlal ediyor ve gerçek davranışla test edilemiyor — web/routes/agent_backtest.py 4767 satırlık bir dosya olup fiilen yalnızca 5 gerçek FastAPI endpoint'i barındırıyor; geri kalanı skorlama, robustness doğrulama, WFO pencereleme gibi saf domain mantığından oluşan ikincil bir domain katmanı haline gelmiş — bu, ARCHITECTURE.md'nin 'Web UI sadece Domain katmanını çağırır' ilkesini ihlal ediyor. Bunun somut belirtisi _agent_worker fonksiyonu (satır ~1888-3640, ~1750 satır, 20 pozisyonel parametre): LLM çağrıları, backtest, robustness, token bütçesi, session log, continuous-mode döngüsü hepsi iç içe closure'larla tek fonksiyonda toplanmış. Bu dolanıklığın kanıtı olarak tests/test_agent_fixes.py:187-200'deki test, fonksiyonu gerçekten çalıştırıp davranışını doğrulamak yerine inspect.getsource() ile kaynak metninde belirli bir string dizisinin geçtiğini kontrol ediyor — yani ekip kendi testlerinde bu fonksiyonun gerçek yürütmeyle test edilemeyecek kadar büyüdüğünü itiraf etmiş durumda. Aynı kök sorun Mimari Review ve Brutal Review tarafından bağımsız olarak, aynı satır aralığına işaret ederek raporlandı. _(web/routes/agent_backtest.py (4767 satır; _agent_worker fonksiyonu satır ~1888-3640 arası, ~1750 satır, 20 parametre))_ _(boyutlar: Mimari Review,Brutal Review)_
- **[ORTA]** web/routes/backtest.py da aynı desenle domain mantığını (plan preview, sinyal önizleme) route handler'larının içine gömmüş — agent_backtest.py'deki desenin daha küçük ölçekli tekrarı: composer/backtest domain katmanına ait olması beklenen strateji-planı üretimi, uyarı tahmini ve sinyal önizleme mantığı doğrudan route dosyasında tanımlı. run() ve describe() handler'ları da 300-500+ satırlık tek fonksiyonlar; orkestrasyon, HTML/HTMX render mantığı ve iş kuralları aynı fonksiyon gövdesinde karışmış durumda. Somut sonuç: plan-preview uyarı mantığını composer.py tarafında da (ör. Strategy Studio'nun kendi önizlemesinde) kullanmak istendiğinde, mantık web/routes/backtest.py içine hapsolmuş olduğundan ya kod kopyalanacak ya da route katmanına bağımlı bir içe aktarma yapılacak — her iki yol da katman sınırını daha da bulanıklaştırıyor. _(web/routes/backtest.py (2644 satır); _local_fallback_breakdown, _predict_plan_warnings, _preview_signals (satır ~1529-1835), async def run() (~537-1059, ~520 satır), async def describe() (~1123-1435, ~310 satır))_

## Code Review
- **[ORTA]** repair_massive_intraday.py: _fixed() fonksiyonunun precision parametresi kullanılmıyor — `def _fixed(values, precision): scale = 1_000_000_000; return [int(round(value*scale))...]` — `precision` argümanı gövdede hiç okunmuyor; fonksiyon her zaman sabit 1e9 ile ölçekliyor. Çağıranlar `_fixed(df[name].tolist(), 2)` (OHLC) ve `_fixed(df["volume"].tolist(), 0)` (volume) farklı precision değerleri geçiriyor, bu da okuyana 'precision'ın kodlamayı etkilediği izlenimini veriyor. Gerçek parquet şeması incelendiğinde (`price_precision`/`size_precision` alanları schema METADATA'sında tutuluyor, ham `fixed_size_binary[8]` kodlaması her zaman 1e9 ölçekli) bu spesifik durumda yanlış sonuç üretmiyor, ama fonksiyonun imzası yanıltıcı: parametre ölü kod ya da unutulmuş bir TODO. İleride biri precision'ı gerçekten farklı davranması gerektiğini varsayıp yanlış varsayımla kod eklerse veri bozulmasına yol açabilir. _(repair_massive_intraday.py:24-26 (_fixed) ve çağrı yerleri satır 33/37)_
- **[ORTA]** repair_massive_intraday.py için hiç test yok — Aynı commit/oturumda eklenen kardeş scriptlerin (download_grouped_daily.py, extract_symbol_ticks.py, stitch_ticker.py, download_flatfiles.py) hepsi için tests/ altında birim testleri var, ama üretim kataloğundaki parquet dosyalarını doğrudan okuyup-üstüne-yazan (bkz. yukarıdaki bulgu) en riskli script olan repair_massive_intraday.py için test yok. `_replace_day`, `_bar_table`, `repair_day` gibi fonksiyonlar geçici bir katalog dizini ile kolayca test edilebilirdi ve yukarıdaki 'yanlış dosya seçimi' hatası böyle bir testle muhtemelen daha ilk yazımda yakalanırdı. _(repair_massive_intraday.py (tüm dosya); karşılaştır: tests/test_download_grouped_daily.py, tests/test_extract_symbol_ticks.py, tests/test_stitch_ticker.py, tests/test_download_flatfiles_*.py)_
- **[DÜŞÜK]** download_grouped_daily.py: workers>1 modunda hata sonrası iptal yalnızca kuyruktaki görevleri durdurur — `GroupedDailyError` (örn. 401/403 yetki hatası) yakalandığında `for f in futures: f.cancel()` çağrılıyor, ancak `Future.cancel()` yalnızca henüz BAŞLAMAMIŞ görevleri iptal eder; `with ThreadPoolExecutor(...) as ex:` bloğundan çıkarken varsayılan `shutdown(wait=True)` zaten çalışmakta olan tüm işçi thread'lerinin bitmesini bekler. `--workers 8 --all` gibi büyük bir koşumda yetki hatası ilk günlerden birinde tespit edilse bile, o an paralel çalışan diğer ~7 istek tamamlanana kadar (ve gereksiz API çağrıları/loglar üretilerek) program kapanmayı bekletir. Kritik değil ama beklenen 'hemen dur' davranışıyla gerçek davranış arasında fark var; dokümantasyon/log bunu belirtmiyor. _(download_grouped_daily.py:311-331 (download fonksiyonu, ThreadPoolExecutor bloğu))_

## Güvenlik (Security) Review
- **[YÜKSEK]** /login uç noktasında brute-force / hız sınırlama koruması yok — server.py:284-318 içindeki /login (POST) uç noktası, paylaşılan tek bir sırla (NAU_ACCESS_TOKEN) kimlik doğruluyor ve _require_auth middleware'i token karşılaştırmasında hmac.compare_digest kullanıyor (zamanlama saldırısına karşı iyi), ancak başarısız denemeler için hiçbir sayaç, kilitleme, gecikme veya hız sınırlama (rate-limit) uygulanmıyor; projede genel olarak da böyle bir mekanizma (slowapi vb.) yok. Uygulama Cloudflare tüneliyle gerçekten internete açık olduğundan, bir saldırgan/otomatik script token alanını sınırsız deneyerek NAU_ACCESS_TOKEN'ı kaba kuvvetle bulmaya çalışabilir; başarısız denemeler loglanmadığı için saldırı fark edilmeden sürebilir. Bu bulgu hem Güvenlik hem End-User Negatif Test review'larında bağımsız olarak aynı kök nedenle raporlandı. _(boyutlar: Güvenlik (Security) Review,End-User Negatif Test)_
- **[ORTA]** Kimlik doğrulama çerezi `secure` bayrağı olmadan ve sunucu taraflı iptal mekanizması olmadan set ediliyor — server.py:289-304 `login_submit` içinde `nau_auth` çerezi `httponly=True, samesite="lax"` ile ama `secure=True` OLMADAN, 30 gün (`max_age=60*60*24*30`) geçerli şekilde set ediliyor. Çerez değeri `_auth_cookie_value` (server.py:71-72) ile sha256(paylaşılan-sır) — yani sabit, oturuma özgü olmayan, sunucu tarafında iptal edilemeyen statik bir bearer-eşdeğeri. Uygulama gerçekten internete (Cloudflare tüneli) açık olduğundan, `secure` bayrağının eksikliği çerezin düz HTTP üzerinden (yanlış yapılandırılmış bir istemci/proxy/redirect senaryosunda) sızma riskini savunma-derinliği açısından artırıyor; ayrıca hiçbir `/logout` rotası yok (server.py ve web/routes/*.py içinde `logout`/`delete_cookie` araması sıfır sonuç verdi) — sızan bir çerez, paylaşılan `NAU_ACCESS_TOKEN` değiştirilene kadar 30 gün boyunca geçerliliğini korur, tek tek iptal edilemez.

## Brutal Review
- **[KRİTİK]** Strategy Studio varsayılan olarak TAMAMEN UYDURMA backtest sonuçları veriyor, kullanıcı hiçbir uyarı görmüyor — strategy_studio/backtest.py:181-332 içindeki StubBacktestAdapter, seed'i strateji konfigürasyonunun hash'inden türeten bir random-walk üretici — Sharpe, DSR, net PnL, max DD, win-rate, profit factor, equity curve, per-symbol tablo dahil HER metrik `random.Random` ile uyduruluyor. web/routes/strategy_studio.py:114-124 (`_adapter_for`) bu stub'ı `STUDIO_BACKTEST=nautilus` ortam değişkeni set edilmedikçe VARSAYILAN motor yapıyor ('The stub is the default everywhere'). web/templates/studio/*.html şablonlarında (footer_metrics, results_pane, per_symbol, ghost, side_panel) bu sayıların gerçek mi sahte mi olduğuna dair TEK bir görsel işaret yok — Sharpe 1.84 yazan bir kutu, gerçek motordan mı stub'tan mı geldiğini kullanıcıya söylemiyor. Daha kötüsü: strategy_studio/ai.py:295-320 `evaluate_trial` — AI'ın önerdiği bir mutasyonu kabul/red eden 'guardrail' mantığı (trade sayısı eşiği, OOS objective kötüleşti mi) — adapter parametresi olarak yine bu stub'ı alıyor; yani gerçek Claude modeli (HttpAnthropicClient, satır 175) gerçek öneriler üretiyor ama bu önerilerin 'iyileşme mi kötüleşme mi' kararı saf gürültü üzerinden veriliyor. Bir operatör env var'ı bilmeden bu ekranı kullanırsa, tamamen rastgele sayılara bakarak strateji kararı verme riski var; bu bir backtesting/trading aracında en tehlikeli sınıf hatadır (sessiz veri sahteciliği). _(strategy_studio/backtest.py:181-332; web/routes/strategy_studio.py:114-138; strategy_studio/ai.py:295-320)_
- **[YÜKSEK]** AUTO'nun robustness kapısı (_robustness_passed) tek bir WFO penceresinde eksik alfa alanı yüzünden TÜM adayı 'failed' sayıyor — bu hata suite'te 2 testi şu anda kırıyor — web/routes/agent_backtest.py:1322-1342: WFO penceresinin `excess_return_fraction` alanı yoksa (float(None) → TypeError → NaN) kod bunu 'skip' değil doğrudan `failed += 1` sayıyor ve `_robustness_passed` fonksiyonu strict/relaxed FARK ETMEKSİZİN False dönüyor (satır 1380: `if failed: return False`). app_constants.py:40-65 `stamp_buy_hold_benchmark` ise bars<2 satır, first close<=0/NaN veya herhangi bir Type/Value/Key/IndexError durumunda `excess_return_fraction`'ı METRICS'E HİÇ YAZMADAN sessizce çıkıyor — yani gerçek üretimde tek bir dejenere WFO penceresi (kısa test dilimi, bozuk ilk fiyat vb.) bütün candidate'i, diğer 3 kriter (IS/OOS, Multi-Symbol, Monte Carlo) tertemiz olsa bile reddettiriyor. Bu regresyonu doğrulayan kanıt: `python -m pytest` HEAD'de (6993ef1) çalıştırıldığında tests/test_regression_anchors_high.py::TestRobustnessPassed::test_clean_three_eval_strict_passes VE ::test_monte_carlo_median_dd_above_limit_ok şu an FAIL veriyor (932 passed, 2 failed) — yani 'temiz 3/4 kriter geçmeli' senaryosunu kilitleyen regresyon testi bozuk durumda ve kimse fark etmemiş. Ayrıca aynı blokta (satır 1330-1342) mantık `failed`i bazen ÇİFT sayıyor (önce 'missing excess' için, sonra positive_ratio=0 olduğu için tekrar) — sonucu değiştirmiyor ama kodun ne kadar gevşek yazıldığını gösteriyor. _(web/routes/agent_backtest.py:1322-1342; tests/test_regression_anchors_high.py:194-226 (şu an FAIL))_

## End-User Negatif Test
- **[ORTA]** İstek gövdesi boyutu hiçbir yerde sınırlanmıyor (DoS / disk doldurma riski) — web/routes/reports.py:681-691 (`post_layout`) `await request.json()` ile gelen ham gövdeyi doğrudan parse ediyor ve `_save_layout` (satır 90-99) bunu diske (`~/.cache/nautilus_web_app/reports_layout.json`) atomik olarak yazıyor. `_view_state_fields` (satır 38-65) yalnızca `pageSize` alanını 0-100000 aralığına kısıtlıyor; `order`, `hidden` ve `filters` sözlüğündeki anahtar/değerlerin uzunluğuna veya sayısına HİÇBİR üst sınır konmamış. Starlette/FastAPI varsayılan olarak istek gövdesi boyutunu sınırlamadığından, tek bir kullanıcı çok büyük (ör. yüzlerce MB) bir JSON gövdesi (dev bir `order` dizisi veya binlerce anahtarlı `filters` sözlüğü) göndererek sunucu belleğini tüketebilir ve/veya `reports_layout.json` dosyasını şişirip her `/reports` sayfa yüklemesinde (`_load_layout`) yeniden okunmasına, dolayısıyla sürekli yavaşlamaya/servis bozulmasına yol açabilir. Aynı sınırsız-gövde deseni projede genel bir mimari eksiklik (hiçbir yerde `max_request_body_size` benzeri bir koruma yok).
- **[ORTA]** LLM'e iletilen serbest metin alanlarında tutarsız uzunluk sınırı — web/routes/strategy.py:37,739-744 — `_MAX_LLM_TEXT_LEN = 4000` sabiti yalnızca `/strategy/blocks/generate` uç noktasında (`label`/`description` için) kontrol ediliyor. Aynı dosyadaki diğer üç LLM-çağıran uç nokta bu sınırı hiç uygulamıyor: `/strategy/suggest` (satır 409-452, `description` alanı doğrudan `propose_composed_strategy`'e `hint` olarak gidiyor), `/strategy/drafts/chat` (satır 530-573, `message` alanı `chat_edit_blocks`'a gidiyor) ve `/strategy/blocks/chat` (satır 979-1030, `message` alanı `chat_edit_block`'a gidiyor). Bir kullanıcı bu üç forma onlarca/yüzlerce KB'lık metin yapıştırıp gönderirse (Form alanları `str = Form("")` ile hiçbir `max_length` olmadan kabul ediliyor), bu metin doğrudan Claude API çağrısına dahil ediliyor — hem gereksiz token/maliyet tüketimine hem de (model bağlam sınırını aşarsa) API hatasına yol açabilir. Alt katmandaki `agent.py` fonksiyonları (`propose_composed_strategy`, `chat_edit_blocks`, `chat_edit_block`) API/network hatalarını nazikçe yakalayıp kullanıcıya TR mesaj döndürüyor (500 çökmesi yok), ama girdi boyutu API'ye gitmeden ÖNCE hiç denetlenmiyor — bu da `/blocks/generate`'teki mevcut korumayla tutarsız ve maliyet/DoS riski taşıyor.
- **[DÜŞÜK]** /data/refresh/index'te hatalı tarih formatı 400 yerine 500 üretiyor ve hata metnini istemciye sızdırıyor — web/routes/data.py:146-180 (`refresh_index`) — `start`/`end` form alanları (satır 151-152) hiçbir format doğrulaması yapılmadan `refresh_row("index", ..., start=start or None, end=end or None)` çağrısına iletiliyor; `data.py:2162-2170`'te bu değerler `datetime.fromisoformat(raw_start/raw_end)` ile parse ediliyor. Kullanıcı `start`/`end` alanına "abc", "2024-13-45" gibi geçersiz bir tarih girerse `ValueError` fırlar; route'taki `try/except` bloğu (satır 163-175) yalnızca `FileNotFoundError`'ı özel olarak yakalayıp geri kalan her şeyi `except Exception as e: raise HTTPException(500, str(e))` ile 500'e çeviriyor. Aynı dosyadaki `web/routes/backtest.py` içinde bu tür girdiler için önceden `_invalid_date_range` ile 400 döndüren açık bir doğrulama var (satır 213-236), ama `data.py`'deki bu uç noktada eşdeğer bir kontrol yok — sonuçta geçersiz kullanıcı girdisi 'sunucu hatası' (500) olarak raporlanıyor ve Python'un ham `ValueError` metni istemciye (`HTTPException(500, str(e))` üzerinden) geri gönderiliyor.

## End-User Pozitif Test
- **[DÜŞÜK]** Data sayfasında 'External Nautilus Catalog (NAU_ev)' paneli aslında NAU_ev'den değil, yerel fallback katalogdan geliyor — `/data` sayfasındaki panel başlığı "External Nautilus Catalog (NAU_ev)" olarak görünüyor ve 16 enstrüman (AAPL, TSLA, MSFT, SPY vb.) bar sayılarıyla listeleniyor — kullanıcı bunun NAU_ev masaüstü kataloğundan geldiğini düşünür. Oysa `data.py` içindeki `EXTERNAL_CATALOGS` listesinin ilk kökü olan `D:\NAU_ev\backend\data\catalog` bu makinede mevcut değil (başlangıçta konsola uyarı da düşüyor: "EXTERNAL_CATALOGS root does not exist ... panel will be empty"). Panelin boş görünmemesinin tek sebebi, koddaki ikinci fallback kök olan `EQUITY_CATALOG_DIR` (bu projenin kendi `ingest_equities.py` ile doldurduğu yerel katalog) devreye girmesi. Sonuç olarak kullanıcı arayüzde gördüğü veriyi "NAU_ev masaüstü kataloğu" sanıp güncelliği/kaynağı hakkında yanlış varsayımda bulunabilir; oysa veri aslında bu projenin kendi yerel ingest çıktısı. Mutlu yolda sayfa çöküyor ya da boş kalmıyor, sadece etiket ile gerçek veri kaynağı örtüşmüyor. _(data.py:77-106 (EXTERNAL_CATALOGS/EQUITY_CATALOG_DIR fallback), web/templates ilgili panel başlığı "External Nautilus Catalog (NAU_ev)" — /data sayfası)_

## Sınır Durumu (Edge Case) Testleri
- **[YÜKSEK]** calc_ewma_vol sıfır/negatif kapanışta ValueError ile çöküyor (vol_target boyutlandırma) — indicators.py satır 396 ve 399: `math.log(closes[i] / closes[i - 1])` çağrısı yalnız PAYDANIN (`closes[i-1] > 0`) pozitifliğini kontrol ediyor, PAYIN (`closes[i]`) değil. Bir bar'ın kapanışı tam 0.0 ya da negatifse (kötü veri satırı, borsadan çıkarılma, ya da vadeli işlemde gerçek negatif fiyat — 2020 WTI örneği gibi) oran sıfır/negatif olur ve `math.log()` `ValueError: math domain error` fırlatır. Doğruladım: `calc_ewma_vol([100]*5+[0.0]+[100]*5, span=5)` gerçekten bu hatayla patlıyor. Bu fonksiyon composer.py `_compute_qty`'de `mode == "vol_target"` dalında hiçbir try/except olmadan çağrılıyor (composer.py ~satır 2036-2042); `vol is None` durumuna düşen 'warmup tamamlanmadı' zarif geri dönüşü asla devreye giremiyor çünkü istisna ondan ÖNCE fırlıyor. Sonuç: vol_target boyutlandırma modunu kullanan bir stratejinin, verisinde tek bir kötü/sıfır kapanış bar'ı geçtiği an tüm backtest/AUTO koşusu (sandbox içinde bile) hataya düşüyor — atr_target ve percent_equity modlarındaki gibi zarif bir düşüşe (fixed trade_size'a) sahip değil. _(indicators.py:396, indicators.py:399, composer.py:2036-2042 (_compute_qty, mode=="vol_target"))_
- **[DÜŞÜK]** _LEARNED_MAX_TOKENS global sözlüğü kilitsiz, iş parçacıkları arası yarış durumuna açık — agent.py satır 783-843: `_LEARNED_MAX_TOKENS` modül seviyesinde paylaşılan bir dict; `_create_message` içinde hem okunuyor (`_LEARNED_MAX_TOKENS.get(key, 0)`) hem check-then-act ile yazılıyor (`_LEARNED_MAX_TOKENS[key] = bigger`), ama hiçbir kilitle korunmuyor — oysa aynı projede state.py (`threading.Lock`), token_ledger.py (`_LOCK`) ve custom_block_store.py (`_STORE_LOCK`/dosya kilidi) tam olarak bu tür paylaşılan durumu kilitliyor. AUTO aynı anda birden çok worker thread'i çalıştırabildiğinden (agent worker thread + olası eşzamanlı çağrılar), iki thread aynı (model, purpose) anahtarını aynı anda büyütürse biri diğerinin öğrendiği değeri ezebilir — çökme değil ama 'öğrenilen tavan' optimizasyonunun ara sıra kaybolup bir sonraki çağrının yine kesilmeye/2x maliyete düşmesine yol açan sessiz bir kayıp. _(agent.py:783 (_LEARNED_MAX_TOKENS), agent.py:793-844 (_create_message))_
- **[DÜŞÜK]** download_flatfiles.mirror() boş nesne listesiyle çağrılırsa IndexError fırlatır — download_flatfiles.py satır 196: `mirror()` fonksiyonu ilk satırda `_preflight(client, bucket, objects[0][0])` çağırıyor; `objects` boşsa (`objects[0]`) çıplak bir `IndexError` fırlatır — modülün kendi hata sözleşmesi olan `FlatFileError` (anlaşılır Türkçe mesaj) değil. Şu an tek çağıran `main()` fonksiyonu önceden `if not objs: ... sys.exit(1)` ile bunu önlüyor, ama `mirror()` genel/public bir fonksiyon (testler doğrudan çağırıyor: tests/test_download_flatfiles.py, tests/test_download_flatfiles_auth.py); fonksiyonun kendisi boş koleksiyon durumuna karşı korumasız, sadece tek bir çağıranın dışarıdan koyduğu kontrole güveniyor. _(download_flatfiles.py:196 (mirror), download_flatfiles.py:274-276 (main — tek koruma burada))_

## Entegrasyon Testleri
- **[YÜKSEK]** Otonom loop (/loop/start) piyasa verisini asla yenilemiyor — sunucu açılışındaki tek seferlik snapshot'a kilitli — server.py'de `_context["bars"]` SADECE `lifespan()` içinde, sunucu ayağa kalkarken bir kez dolduruluyor (server.py:231 ve 247) ve repo genelinde bu sözlüğe başka HİÇBİR yazma noktası yok (`_context[` grep'i yalnızca bu iki satırı buluyor). `/loop/start` (web/routes/loop.py:42) arka plan thread'ini `get_bars()` ile — yani bu sabit açılış snapshot'ıyla — başlatıyor; `loop_runner.run_loop` da `stop_requested` gelene kadar SÜREKLİ aynı `bars_df` referansı üzerinde (agent modunda `run_legacy_backtest_guarded`, catalog modunda `run_backtest_guarded`) tekrar tekrar backtest çalıştırıyor — hiçbir iterasyonda `load_bybit_bars` yeniden çağrılmıyor. Buna karşılık uygulamanın DİĞER tüm veri yolları (web/routes/lab.py:205, backtest.py:724 ve 2492, robustness.py:193, chart.py:108, reports.py:600, agent_backtest.py:2350) her istekte `load_bybit_bars(...)`'ı taze parametrelerle çağırıyor; yani `/loop`, uygulamanın geri kalanıyla aynı 'canlı veri' sözleşmesini paylaşmıyor — bu bir modüller-arası contract uyumsuzluğu.

Sonuç: sunucu günler/haftalar açık kalırsa (autonomous loop tasarımı zaten bunu hedefliyor; base.html:87'deki `engine-dot live` rozetiyle 'canlı' olarak da etiketleniyor), loop her ~5 saniyede bir aynı ~7 günlük eski BTCUSDT verisi üzerinde backtest'i tekrarlıyor: catalog modunda bir tam katalog turundan sonraki her tur bit-bit aynı sonucu üretir (yeni bilgi yok), agent modunda LLM'e geçmiş iterasyonlar üzerinden yeni parametre önerisi istense bile arkasındaki piyasa verisi hiç ilerlemiyor.

Daha da somut bir sözleşme ihlali: server.py:206-244 arasındaki M124 yorumu, Bybit açılışta erişilemezse boş bir DataFrame ile devam edilip 'let the server come up, and let the loop runner run once data arrives' dediğini söylüyor — yani kodun kendi dokümantasyonu, verinin sonradan gelip loop runner'a ulaşacağını varsayıyor. Ama hiçbir kod yolu `_context["bars"]`'ı runtime'da yenilemiyor; dolayısıyla Bybit açılışta erişilemezse, loop runner sonrasında Bybit tekrar erişilebilir hale gelse dahi SONSUZA KADAR boş DataFrame ile çalışmaya devam eder (her iterasyon muhtemelen backtest hatası/boş metrik üretir), ta ki sunucu yeniden başlatılana kadar. Bu, dokümante edilen davranış beklentisi ile gerçek entegrasyon kablolaması arasındaki somut bir uyumsuzluk. _(server.py:194-259 (lifespan, _context), web/routes/loop.py:25-44 (get_bars kullanımı), loop_runner.py:73-197 (run_loop))_

## Statik Analiz & Lint
- **[YÜKSEK]** ruff format --check . başarısız — 14 dosya formatsız, CI kırılma riski — `python -m ruff check .` (proje select=[E,F,I,W,UP]) temiz geçiyor, ama `python -m ruff format --check .` 14 dosyada 'Would reformat' raporluyor: backtest.py, repair_massive_intraday.py, web/mission.py, 6 adet tests/*.py dosyası ve work/ altında 4 dosya. `.github/workflows/ci.yml` `uv run ruff format --check .` adımını çalıştırıyor, yani bu haliyle CI kırmızı çıkar. Kök neden çoğu dosyada dosya-içi KARIŞIK satır sonu: backtest.py'de 1986 CRLF + 108 salt-LF satır tespit edildi (byte seviyesinde doğrulandı); ruff format tüm dosyayı normalize etmeye çalışınca görünürde 'boş' ama aslında satır-sonu değiştiren dev bir diff üretiyor. backtest.py ve web/mission.py bu dalda (fix/auto-gate-and-artifact-identity, main'e göre `git diff --stat`) az önce DEĞİŞTİRİLMİŞ dosyalar — yani format kırılması bu dalın kendi değişikliğinden kaynaklanıyor. repair_massive_intraday.py ise gerçek biçim borcu içeriyor (uzun/kırılmamış satırlar) — CLAUDE.md'nin vaat ettiği 'her Python dosya yazımından sonra otomatik ruff format çalışır' PostToolUse hook'unun bu dosyada hiç çalışmadığını gösteriyor. _(backtest.py; web/mission.py; repair_massive_intraday.py; tests/test_agent_fixes.py ve diğer 5 test dosyası)_
- **[ORTA]** backtest.py _metrics(): commission/süre/slippage hesaplarını saran sessiz 'except Exception: pass' blokları hatalı veya sıfır metrik üretiyor — backtest.py içindeki _metrics() fonksiyonunda commission_total, avg_duration_mins ve slippage hesaplamaları (satır ~673, 687-698, 705-726) gerekçesiz 'except Exception: pass' bloklarıyla sarılı ve hiçbir log/uyarı yazmıyor. positions_df şemasında beklenmeyen bir değişiklik veya engine.trader.generate_order_fills_report() çağrısının başarısız olması durumunda commission_total sessizce 0.0'a, avg_duration_mins NaN'a, slippage_total ise ayırt edilemeyen bir '$0 slippage'a düşüyor — backtest 'başarılı' görünen ama gerçekte yanlış bir P&L/metrik seti üretiyor. Bu, kodun kendi yorumunda daha önce düzeltildiği belirtilen bir sınıf hatanın ($0 slippage'ın artık görünmez olmaması gerekirken tekrar görünmez olması) gerilemesi niteliğinde. Aynı kök sorun Statik Analiz & Lint ve Sessiz Hata & Gözlemlenebilirlik review'larında, örtüşen satır aralıklarına işaret ederek bağımsız olarak raporlandı. _(backtest.py:673, backtest.py:687-698, backtest.py:724 (fonksiyon: _metrics))_ _(boyutlar: Statik Analiz & Lint,Sessiz Hata & Gözlemlenebilirlik)_
- **[DÜŞÜK]** except bloklarında 'raise ... from err' eksik — istisna zinciri örtük kalıyor — Ruff B904: except içinde yakalanan hatanın üstüne yeni bir exception raise edilirken `from err`/`from None` kullanılmıyor, en yoğun olarak strategy_studio modülünde (mutations.py: 75, 115, 173, 207, 232, 287; compiler.py: 83, 157) ve web/routes/strategy_studio.py'de (11 konum) görülüyor; data.py:169 (_acquire_lock, TimeoutError raise) ve web/routes/wiki.py:66'da da var. Python örtük olarak __context__ üzerinden orijinal hatayı zaten gösterir, ama açık `from` olmadan hangi düşük seviye istisnanın asıl tetikleyici olduğu okuyucuya/log'a net biçimde işaretlenmiyor — hata ayıklamada gürültü yaratıyor. _(strategy_studio/mutations.py (6 konum), strategy_studio/compiler.py:83,157, web/routes/strategy_studio.py (11 konum), data.py:169, web/routes/wiki.py:66)_

## Sessiz Hata & Gözlemlenebilirlik
- **[YÜKSEK]** composer.py: MTM equity, gerçek kaynaklar başarısız olunca sessizce sabit STARTING_CASH'e düşüyor — _current_equity() üç katmanlı bir fallback zinciri kullanıyor: (1) portfolio.equity(venue), (2) hesap bakiyelerini tarama, (3) app_constants.STARTING_CASH sabiti. İlk iki katman başarısız olursa geniş 'except Exception: pass' blokları (satır 1949-1950 ve 1993-1994) hatayı tamamen yutuyor ve hiçbir log satırı yazılmıyor — oysa aynı fonksiyondaki 'balances' fallback'i (satır 1986-1989) en azından log.warning yazıyor. Üstelik en sonda self._equity_mode hiçbir zaman set edilmiyor, yani sonucun hangi yoldan geldiği (gerçek equity mi, sabit mi) hiçbir yerde işaretlenmiyor. Fonksiyonun kendi docstring'i, aynı sınıfın DAHA ÖNCE portfolio.equity() dict dönüşünü yanlış ele aldığını ve bunun MTM eğrisini sessizce cash bakiyesine düşürüp 180 günlük bir backtest'te gerçek +$69.70 kârı yerine hayali -%94 max_dd ürettiğini belgeliyor (M-equity bug). Aynı yapı — sessiz except + loglamayan fallback — hâlâ üçüncü katmanda duruyor: Portfolio/account objesi bozulursa veya API değişirse equity eğrisi sessizce STARTING_CASH sabitine kilitlenir, sonuçlar (max_dd, Sharpe, equity_curve) yanlış olur ama 'başarılı' görünür ve hiçbir log satırı bunu göstermez. _(composer.py:1904-2000 (_current_equity))_
- **[DÜŞÜK]** web/routes/backtest.py: sonuç log/snapshot yazma hataları hiç loglanmıyor — _log_backtest ve _save_result_snapshot çağrıları etrafındaki 'except Exception: pass' blokları yorumla gerekçelendirilmiş ('log I/O failure must not hide the result') ama hiçbir logging çağrısı yok. Kullanıcıya sonucun gösterilmesi doğru davranış olsa da, geçmiş/tearsheet kalıcılığı sessizce ve sürekli başarısız olabilir (örn. disk dolu, izin hatası) ve bunu tespit edecek hiçbir iz bırakılmaz — /sessions ve tearsheet ekranları neden boş kaldığını araştırmak için hiçbir log kaydı bulunamaz. _(web/routes/backtest.py:1007-1026)_

## Uçtan Uca (E2E) Testler
- **[YÜKSEK]** Uygulamanın ilk açılış ekranı (GET "/") hiçbir testte hiç çağrılmıyor — Kullanıcının E2E akışının ilk adımı olan 'uygulamayı aç' ekranı — dashboard() route'u — repodaki hiçbir test dosyasında (tests/ ve tests/studio/ dahil, tüm ağaç tarandı) `client.get("/")` şeklinde çağrılmıyor. Bu route load_catalog(), get_state().snapshot() ve get_market_info() çağırıyor ve base.html üzerinden loop_running()/first_studio_strategy_id() gibi template globals'i de tetikliyor. Bu zincirdeki herhangi bir regresyon (ör. state.py, composer.load_catalog, ya da template değişkenlerinden biri exception atarsa) CI'da veya varsayılan `pytest` koşumunda yakalanmaz; kullanıcı sadece boş/500 sayfa ile karşılaşır. Diğer sayfalar (/data, /studio, /tearsheet vb.) için TestClient ile GET testleri varken tam olarak kök sayfa atlanmış. _(web/routes/dashboard.py:21-41 (server.py include_router satır 357))_
- **[YÜKSEK]** Tek gerçek uçtan-uca zincir testi (POST /backtest/run → poll → sonuç) CI'da hiç çalışmıyor — Uygulamanın ana değer önerisi olan 'strateji seç → backtest başlat → ilerlemeyi izle → sonucu gör' akışını HTTP katmanından uçtan uca süren tek test bu dosya. Ancak `@pytest.mark.e2e` ile işaretli ve pyproject.toml'daki `addopts = "-m \"not e2e\""` yüzünden varsayılan `pytest` çalıştırmasından hariç tutuluyor; .github/workflows/ci.yml de sadece `uv run pytest -q tests` çalıştırıyor (e2e marker'ı elle `-m e2e` ile geçilmediği sürece bu test asla tetiklenmiyor). Test ayrıca gerçek Bybit ağ çağrısı yapıyor ve ~2 dakika sürüyor, bu da onu CI'a eklemeyi de pratikte caydırıcı kılıyor. Sonuç: /backtest/run → /backtest/progress/{run_id} → backtest_result.html render zincirindeki bir kırılma (ör. run_id parse hatası, HTMX poll fragment'inin hx-trigger'ı kaybetmesi, verdict-badge render hatası) hiçbir otomatik koşumda tespit edilmez. _(tests/test_backtest_run_progress_result_e2e.py:1-93; pyproject.toml:70-71; .github/workflows/ci.yml:36-37)_
- **[ORTA]** Tek-operatör erişim kapısı (login/cookie middleware) için hiç test yok — NAU_ACCESS_TOKEN ortam değişkeni set edildiğinde devreye giren `/login` formu, `_is_authenticated`/`_auth_cookie_value` cookie doğrulaması ve HTMX istekleri için 401 + `HX-Redirect: /login` davranışını doğrulayan tek bir test yok (repo genelinde NAU_ACCESS_TOKEN'a değinen tek test dosyası test_backtest_run_progress_result_e2e.py, ve o da testi etkilememesi için değişkeni `os.environ.pop` ile SİLİYOR). Prod'da bu operatör bu kapıyı açtığında (tek-operatör deploy senaryosu) — doğru token ile giriş, yanlış token ile 401, cookie'siz HTMX isteğinde otomatik /login yönlendirmesi gibi davranışların hiçbiri regresyona karşı korunmuyor; bir bug kullanıcıyı ya sonsuz login döngüsüne sokabilir ya da kapıyı sessizce devre dışı bırakabilir ve hiçbir test bunu yakalamaz. _(server.py:284-318 (_require_auth middleware, login_form, login_submit))_

## Birim (Unit) Testleri
- **[ORTA]** wiki_helper.py ve web/routes/wiki.py icin sifir birim test — Bu iki moduli (path-traversal korumasi dahil) referans veren tek bir test bile yok - tests/ ve tests/studio/ altinda 'wiki_helper', 'read_wiki_page', 'resolve_slug', 'wiki_by_slug', 'wiki_path' veya '/wiki' gecen hicbir satir bulunmuyor. wiki.py'deki wiki_path route'u kullanicidan gelen rel_path'i (WIKI_ROOT / rel_path).resolve() ile cozup candidate.relative_to(WIKI_ROOT.resolve()) ile '..' kacislarini reddediyor; wiki_helper.py'deki _rewrite_wikilinks ise regex tabanli kod-blogu atlama (fenced/inline code span hesabi) ve frontmatter temizleme gibi gercek is mantigi iceriyor. Su an bu path-traversal reddi, slug cozumleme, kod-blogu icindeki [[wikilink]]'lerin atlanmasi ve bozuk/eksik sayfa durumlari tamamen test edilmeden duruyor; bir regresyon (orn. .resolve() cagrisinin kaldirilmasi veya relative_to mantiginin bozulmasi) mevcut test setiyle asla yakalanmaz. _(wiki_helper.py, web/routes/wiki.py)_
- **[DÜŞÜK]** AUTO'nun aday tekillestirme (_deduplicate_candidate) fonksiyonu ve tukenme yolu test edilmemis — _candidate_fingerprint fonksiyonu tests/test_auto_360_fixes.py icinde iyi test edilmis (exact/family fingerprint ayrimi, komutatif blok sirasi), ama bu fingerprint'leri kullanarak asil tekillestirme kararini veren _deduplicate_candidate fonksiyonunun kendisi hicbir testte cagrilmiyor (grep: 'deduplicat', 'zero_trade_famil' sadece fingerprint testlerinde geciyor). Ozellikle 64 denemeden sonra RuntimeError('candidate fallback pool exhausted after 64 attempts') firlatan tukenme yolu tamamen test disi: _fallback_composed uretici havuzu daralir/tekrarlanirsa AUTO oturumu production'da beklenmedik sekilde crash olur ve bu senaryoyu yakalayacak hicbir test yok. Ayrica replacement mantiginin (seen/zero_trade_families setlerine gercekten girmeyen bir aday uretmesi, session log kaydi) doğrudan dogrulandigi bir test de bulunmuyor. _(web/routes/agent_backtest.py:941-986 (_deduplicate_candidate))_
- **[BİLGİ]** agent_backtest.py icindeki bazi saf yardimci fonksiyonlar dogrudan test edilmemis — _pick_best_exit_from_history, gecmisteki en yuksek skorlu stratejinin exit-blok tipini oncelik listesine (_EXIT_PRIORITY) gore secen saf/deterministik bir fonksiyon; kolayca birim testi yazilabilecek bu fonksiyon icin isim bazli hicbir test bulunamadi (bos history, tum skorlar negatif, isim eslesmesi olmayan durum gibi kenar durumlar dogrulanmiyor). _winner_narrative'in LLM cagrisi basarisiz oldugunda devreye giren deterministik fallback metni (kazanc/kayip 'gained'/'lost' secimi) de dogrudan test edilmiyor. Bunlar buyuk dosyanin (4767 satir) agirlikli olarak route/entegrasyon testleriyle dolayli kapsandigi icin dusuk risk, ama saf fonksiyonlar oldugundan ucuz ve degerli birim test adaylari. _(web/routes/agent_backtest.py (_pick_best_exit_from_history:3640, _winner_narrative:4735))_

## 🔴 En Kritik 3 Madde
- **[KRİTİK]** [Brutal Review] Strategy Studio varsayılan olarak TAMAMEN UYDURMA backtest sonuçları veriyor, kullanıcı hiçbir uyarı görmüyor — strategy_studio/backtest.py:181-332 içindeki StubBacktestAdapter, seed'i strateji konfigürasyonunun hash'inden türeten bir random-walk üretici — Sharpe, DSR, net PnL, max DD, win-rate, profit factor, equity curve, per-symbol tablo dahil HER metrik `random.Random` ile uyduruluyor. web/routes/strategy_studio.py:114-124 (`_adapter_for`) bu stub'ı `STUDIO_BACKTEST=nautilus` ortam değişkeni set edilmedikçe VARSAYILAN motor yapıyor ('The stub is the default everywhere'). web/templates/studio/*.html şablonlarında (footer_metrics, results_pane, per_symbol, ghost, side_panel) bu sayıların gerçek mi sahte mi olduğuna dair TEK bir görsel işaret yok — Sharpe 1.84 yazan bir kutu, gerçek motordan mı stub'tan mı geldiğini kullanıcıya söylemiyor. Daha kötüsü: strategy_studio/ai.py:295-320 `evaluate_trial` — AI'ın önerdiği bir mutasyonu kabul/red eden 'guardrail' mantığı (trade sayısı eşiği, OOS objective kötüleşti mi) — adapter parametresi olarak yine bu stub'ı alıyor; yani gerçek Claude modeli (HttpAnthropicClient, satır 175) gerçek öneriler üretiyor ama bu önerilerin 'iyileşme mi kötüleşme mi' kararı saf gürültü üzerinden veriliyor. Bir operatör env var'ı bilmeden bu ekranı kullanırsa, tamamen rastgele sayılara bakarak strateji kararı verme riski var; bu bir backtesting/trading aracında en tehlikeli sınıf hatadır (sessiz veri sahteciliği). _(strategy_studio/backtest.py:181-332; web/routes/strategy_studio.py:114-138; strategy_studio/ai.py:295-320)_
- **[YÜKSEK]** [Brutal Review] AUTO'nun robustness kapısı (_robustness_passed) tek bir WFO penceresinde eksik alfa alanı yüzünden TÜM adayı 'failed' sayıyor — bu hata suite'te 2 testi şu anda kırıyor — web/routes/agent_backtest.py:1322-1342: WFO penceresinin `excess_return_fraction` alanı yoksa (float(None) → TypeError → NaN) kod bunu 'skip' değil doğrudan `failed += 1` sayıyor ve `_robustness_passed` fonksiyonu strict/relaxed FARK ETMEKSİZİN False dönüyor (satır 1380: `if failed: return False`). app_constants.py:40-65 `stamp_buy_hold_benchmark` ise bars<2 satır, first close<=0/NaN veya herhangi bir Type/Value/Key/IndexError durumunda `excess_return_fraction`'ı METRICS'E HİÇ YAZMADAN sessizce çıkıyor — yani gerçek üretimde tek bir dejenere WFO penceresi (kısa test dilimi, bozuk ilk fiyat vb.) bütün candidate'i, diğer 3 kriter (IS/OOS, Multi-Symbol, Monte Carlo) tertemiz olsa bile reddettiriyor. Bu regresyonu doğrulayan kanıt: `python -m pytest` HEAD'de (6993ef1) çalıştırıldığında tests/test_regression_anchors_high.py::TestRobustnessPassed::test_clean_three_eval_strict_passes VE ::test_monte_carlo_median_dd_above_limit_ok şu an FAIL veriyor (932 passed, 2 failed) — yani 'temiz 3/4 kriter geçmeli' senaryosunu kilitleyen regresyon testi bozuk durumda ve kimse fark etmemiş. Ayrıca aynı blokta (satır 1330-1342) mantık `failed`i bazen ÇİFT sayıyor (önce 'missing excess' için, sonra positive_ratio=0 olduğu için tekrar) — sonucu değiştirmiyor ama kodun ne kadar gevşek yazıldığını gösteriyor. _(web/routes/agent_backtest.py:1322-1342; tests/test_regression_anchors_high.py:194-226 (şu an FAIL))_
- **[YÜKSEK]** [Entegrasyon Testleri] Otonom loop (/loop/start) piyasa verisini asla yenilemiyor — sunucu açılışındaki tek seferlik snapshot'a kilitli — server.py'de `_context["bars"]` SADECE `lifespan()` içinde, sunucu ayağa kalkarken bir kez dolduruluyor (server.py:231 ve 247) ve repo genelinde bu sözlüğe başka HİÇBİR yazma noktası yok (`_context[` grep'i yalnızca bu iki satırı buluyor). `/loop/start` (web/routes/loop.py:42) arka plan thread'ini `get_bars()` ile — yani bu sabit açılış snapshot'ıyla — başlatıyor; `loop_runner.run_loop` da `stop_requested` gelene kadar SÜREKLİ aynı `bars_df` referansı üzerinde (agent modunda `run_legacy_backtest_guarded`, catalog modunda `run_backtest_guarded`) tekrar tekrar backtest çalıştırıyor — hiçbir iterasyonda `load_bybit_bars` yeniden çağrılmıyor. Buna karşılık uygulamanın DİĞER tüm veri yolları (web/routes/lab.py:205, backtest.py:724 ve 2492, robustness.py:193, chart.py:108, reports.py:600, agent_backtest.py:2350) her istekte `load_bybit_bars(...)`'ı taze parametrelerle çağırıyor; yani `/loop`, uygulamanın geri kalanıyla aynı 'canlı veri' sözleşmesini paylaşmıyor — bu bir modüller-arası contract uyumsuzluğu.

Sonuç: sunucu günler/haftalar açık kalırsa (autonomous loop tasarımı zaten bunu hedefliyor; base.html:87'deki `engine-dot live` rozetiyle 'canlı' olarak da etiketleniyor), loop her ~5 saniyede bir aynı ~7 günlük eski BTCUSDT verisi üzerinde backtest'i tekrarlıyor: catalog modunda bir tam katalog turundan sonraki her tur bit-bit aynı sonucu üretir (yeni bilgi yok), agent modunda LLM'e geçmiş iterasyonlar üzerinden yeni parametre önerisi istense bile arkasındaki piyasa verisi hiç ilerlemiyor.

Daha da somut bir sözleşme ihlali: server.py:206-244 arasındaki M124 yorumu, Bybit açılışta erişilemezse boş bir DataFrame ile devam edilip 'let the server come up, and let the loop runner run once data arrives' dediğini söylüyor — yani kodun kendi dokümantasyonu, verinin sonradan gelip loop runner'a ulaşacağını varsayıyor. Ama hiçbir kod yolu `_context["bars"]`'ı runtime'da yenilemiyor; dolayısıyla Bybit açılışta erişilemezse, loop runner sonrasında Bybit tekrar erişilebilir hale gelse dahi SONSUZA KADAR boş DataFrame ile çalışmaya devam eder (her iterasyon muhtemelen backtest hatası/boş metrik üretir), ta ki sunucu yeniden başlatılana kadar. Bu, dokümante edilen davranış beklentisi ile gerçek entegrasyon kablolaması arasındaki somut bir uyumsuzluk. _(server.py:194-259 (lifespan, _context), web/routes/loop.py:25-44 (get_bars kullanımı), loop_runner.py:73-197 (run_loop))_