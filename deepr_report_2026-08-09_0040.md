## Performans Review
- **[ORTA]** `_index_rows()` önbelleksiz — `/data` sayfası her açılışta 250+ dosya-sistemi işlemi yapıyor — 50 ticker × 5 granularity için her hücrede hem `_read_parquet_stats()` hem `nautilus_catalog_bar_state()` (glob + her dosya için metadata okuma) çalışıyor, hiçbir TTL cache yok. Aynı dosyada birkaç satır üstünde `_bybit_rows()` için AYNI sorun zaten 30sn TTL cache ile düzeltilmiş (2026-08-08 DeepR), ama `_index_rows()`'a uygulanmamış. _(data.py:1238-1288; çağıran: web/routes/data.py:124-130, data.py:2247)_
- **[DÜŞÜK]** Ticker registry JSON'u önbelleksiz ve limitsiz — `discover_index_tickers()` ve `_index_rows()` ayrı ayrı `json.loads()` yapıyor; `/backtest/tickers` döndürdüğü listeyi hiç limitlemeden tüm ticker'lar için `<option>` string'i üretiyor. _(data.py:275-321, 1244; web/routes/backtest.py:504-515)_

## Mimari Review
- **[YÜKSEK]** Route modülleri birbirlerinin private (`_`) iç durumuna doğrudan erişiyor — `web/routes/studio.py` tek başına backtest.py'dan `_last_result_get`/`_recent_runs`, strategy.py'dan `_drafts` (ham taslak sözlüğü), agent_backtest.py'dan `_AGENT_LOCK`/`_AGENT_PROGRESS`'i import edip elle iterasyona sokuyor; `SESSION_LOG_DIR` 3 farklı modülün ihtiyaç duyduğu halde tek bir feature dosyasında tanımlı. _(web/routes/studio.py:139-163; sessions.py:34; tearsheet.py:106; backtest.py:1281)_
- **[ORTA]** `_env_float`/`_env_int` yardımcı fonksiyonları 3 dosyada karakter karakter kopyalanmış (backtest_robustness.py, parallel_exec.py, wfo_optimizer.py) — biri bugfix alırsa diğerleri sessizce eski davranışta kalır.

## Code Review
- **[YÜKSEK]** `composer.py` import zamanında registry hatasını yakalamıyor — sunucu açılışını kilitleyebilir — `_load_custom_blocks()` yalnız `import custom_block_store` hatasını yakalıyor, `cbs.list_custom()` döngüsü sarmalanmamış. `_read_registry()` registry.json bozuksa `RegistryUnavailable` fırlatır (kasıtlı — sessizce boş dönmesin diye), bu istisna `import composer`'ın kendisini patlatıp composer'a bağımlı HER route modülünü çökertir — README'nin "bozuk bloklar skip edilir, katalog etkilenmez" garantisiyle çelişiyor (o garanti yalnız tekil .py dosyası bozukluğu için geçerli, registry.json'un kendisi için değil). _(composer.py:1271-1290)_
- **[ORTA]** `backtest.py` içinde ~450 satır ölü kod: `run_backtest_node`/`run_composed_backtest_node` — proje genelinde grep ile hiçbir çağrı yeri bulunamadı, test kapsamı da yok; dosyanın ~%21'i. _(backtest.py:1661-2108)_
- **[ORTA]** `indicators.py` ve `chart_indicators.py`'de SMA/EMA/Wilder-RSI matematiği bağımsız iki kez yazılmış — aynı algoritma, indicators.py'de yapılan bir performans optimizasyonu (O(n·period)→O(n)) chart_indicators.py'ye yansımamış.
- **[DÜŞÜK]** `composer.py`'de `print()` ile `logging` karışık kullanılıyor — 3 hata mesajı doğrudan stdout'a yazılıyor, log altyapısını (seviye filtresi, handler) bypass ediyor. _(composer.py:1280, 1287, 1641)_
- **[DÜŞÜK]** `/login` rate-limit'i Cloudflare tünel arkasında IP başına değil global çalışabilir — `_client_ip()` `X-Forwarded-For`/`CF-Connecting-IP` parse etmiyor, tünel arkasında tüm istekler tek IP altında toplanabilir. _(server.py:99-120)_
- **[DÜŞÜK]** `web/routes/agent_backtest.py` aşırı büyümüş (5043 satır, 82 üst düzey fonksiyon/sınıf) — DeepR #47'de kademeli çıkarım başladı ama route/scoring/gating/budget/logging hâlâ tek dosyada.

## Güvenlik (Security) Review
- **[ORTA]** `NAU_ACCESS_TOKEN` ayarlanmazsa internete açık uygulama sessizce kimliksiz kalıyor (fail-open) — `_ACCESS_TOKEN` boşsa `_is_authenticated()` her zaman True dönüyor; `serve.py` uygulamanın Cloudflare tüneliyle internete açıldığını belgeliyor ama `PM2_HOME` set + token boş kombinasyonunda hiçbir uyarı/reddetme yok — operatör token export etmeyi unutursa tüm uygulama (sandbox dahil) parolasız erişilebilir. _(server.py:84-97, serve.py:1-20)_

## Brutal Review
- **[ORTA]** `codegate.py` sandbox'ında Pow için "değişken üs yasak" kuralı var, aynı sınıftaki LShift için yok — `_check_pow` değişken üssü DoS riski gerekçesiyle reddediyor ama `x << y` (y çalışma zamanı değişkeni) hiç reddedilmiyor; iki işlenen de ≤1M iken bile `<<` sonucu ~2^1M büyüklüğünde sayı üretebilir. _(codegate.py:312-341 vs 74-82, 404)_
- **[ORTA]** `_terminal_message`: `run_id` path parametresi doğrulanmadan dosya yoluna ekleniyor — kardeş modül `sessions.py` aynı kavram için `run_id` formatını (`[0-9a-f]{8}`) doğruluyor, `agent_backtest.py`'deki `_terminal_message` bu adımı atlamış; dosya var/yok bilgisi doğrulanmamış run_id ile sızdırılabiliyor. _(web/routes/agent_backtest.py:4750-4779 vs sessions.py:733,908)_

## End-User Negatif Test
- **[ORTA]** `atr_period` sunucu tarafında doğrulanmıyor — HTML `min` sadece tarayıcı kısıtı, doğrudan POST ile 0/negatif geçilebiliyor; `ComposedStrategySpec.validate()` bu alanı kontrol etmiyor, sonuç Nautilus'un ham `ValueError`'ı sandbox sonrası (kaynak israfıyla) kullanıcıya sızıyor.
- **[ORTA]** `commission_pct` sunucu tarafında sınırlanmıyor — `robustness.py`'de aynı desen için zaten server-side clamp var (yorum: "HTML min/max sadece tarayıcı tarafı"), `backtest.py`'de yok; `commission_pct=999999` gibi bir POST astronomik komisyonla sessizce "geçerli" sonuç üretir.

## End-User Pozitif Test
- **[DÜŞÜK]** Composer sayfasında arayüz dili tutarsız — buton etiketleri İngilizce, tooltip/onay metinleri Türkçe (aynı panelde iki dil karışık).

## Sınır Durumu (Edge Case) Testleri
- **[ORTA]** Yarım borsa günlerinde intraday onarım imkansız — `repair_day()` RTH'nin her zaman tam 390 dakika olduğunu varsayıyor, kısaltılmış seanslar (Şükran Günü öncesi vb.) sert biçimde reddediliyor.
- **[ORTA]** `.bak` yedeği yalnız ilk onarımda alınıyor — art arda onarımlarda geri dönüş noktası eskiyor, ikinci onarımdan önceki durum hiçbir yerde saklanmıyor.
- **[ORTA]** Fiyat verisi doğrulanmadan sabit-noktalı kodlamaya veriliyor — NaN/aşırı büyük değerde `_fixed()` opak `ValueError`/`OverflowError` ile çöküyor, anlamlı hata mesajı yok. _(repair_massive_intraday.py:24-26)_
- **[ORTA]** Eksik alanlı (T/t yok) API satırları sessizce epoch-0 zaman damgasıyla yazılıyor — geçersiz TİP hatası verirken, EKSİK anahtar sessizce kabul ediliyor (tutarsız doğrulama sınırı). _(download_grouped_daily.py:216-227)_
- **[DÜŞÜK]** Katalogda ilgili parquet dosyası yoksa `StopIteration` ile opak çökme (`next()` boş glob üzerinde).

## Entegrasyon Testleri
- **[YÜKSEK]** `repair_massive_intraday.py` yalnız türetilmiş TF barlarını düzeltiyor, kaynak 1-MINUTE barlar dokunulmadan kalıyor — `ingest_equities.build_tf_bars()` TAM OLARAK 1-MINUTE barlardan TF'leri yeniden üretip siliyor; bir operatör repair sonrası normal ingest'i tekrar çalıştırırsa düzeltme sessizce eski veriden yeniden üretilip silinir.
- **[DÜŞÜK]** `custom_block_store.save_custom` kısmi hatada dosya/registry tutarsızlığına karşı korumasız — kardeş `save_custom_batch`'in `_registry_transaction()` rollback'i burada kullanılmıyor.

## Statik Analiz & Lint
- **[BİLGİ]** ruff (E,F,I,W,UP) tüm proje genelinde temiz — `ruff check .` "All checks passed!" (PostToolUse hook zaten her edit sonrası çalıştırıyor).
- **[BİLGİ]** `chart_indicators.py:_series` içinde `zip(times, vals)` strict değil (B905) — şu an pratik risk yok, savunmacı iyileştirme önerisi.

## Sessiz Hata & Gözlemlenebilirlik
- **[ORTA]** `loop_runner.py`: arka plan döngüsünde backtest_log yazma hatası hiç loglanmadan yutuluyor — `_try_log` tüm gövdeyi `except Exception: pass` ile sarmış; `web/routes/backtest.py`'de AYNI hata sınıfı zaten `log.warning` ile düzeltilmiş (2026-08-08 DeepR notu), kardeş kod yolu `loop_runner.py`'de düzeltilmemiş.
- **[ORTA]** `web/routes/backtest.py`: Index enstrümanı tarih çözümlemesinde geniş `except (ValueError, Exception)` gerçek hatayı "tarih formatı yanlış" mesajı arkasında gizliyor — blok ayrıca pandas import edip parquet okuyor; bozuk cache/eksik pyarrow/izin hatası da aynı yanıltıcı mesajı üretiyor, hiçbir yere loglanmıyor.
- **[ORTA]** `web/routes/strategy_studio.py`: açılışta `_reconcile_deployments()` hatası tamamen sessiz — fonksiyonun kendi docstring'i "arkasında node olmayan running satırı bir hatadan daha kötü" diyor, ama import-zamanı `try/except Exception: pass` hiç loglamıyor (dosyanın geri kalanındaki benzer bloklar `log.debug(exc_info=True)` kullanıyor) — reconciliation başarısız olursa tam da önlemeye çalıştığı "sahte YEŞİL rozet" durumuna düşülüyor.
- **[DÜŞÜK]** `custom_block_store.py`: `delete_custom()` bellekten silme hatasını sessizce yutuyor — kardeş fonksiyon `unregister_deleted_from_memory` aynı durumda `log.exception` kullanıyor, burada tutarsız.

## Uçtan Uca (E2E) Testler
- **[YÜKSEK]** Tüm projede gerçek E2E (HTTP zinciri) test tek dosya, o da CI'da build kırmıyor — `test_backtest_run_progress_result_e2e.py` varsayılan koşumdan hariç, CI'da `continue-on-error: true` ile çalışıyor; bu zincirde regresyon olsa PR yine de merge edilebilir.
- **[YÜKSEK]** Uygulamanın amiral gemisi özelliği (AUTO/otonom ajan) hiçbir testte gerçek worker ile uçtan uca sürülmüyor — tüm testler `_agent_worker`'ı `monkeypatch` ile sahteleştiriyor; "Claude öneri → backtest → iterasyon → promotion gate" zincirinin gerçekten çalıştığını doğrulayan hiçbir otomatik adım yok.

## Birim (Unit) Testleri
- **[YÜKSEK]** `delete_custom_batch` hiçbir testte yok — kardeşi `save_custom_batch` özel olarak test edilmiş (rollback davranışı dahil), `delete_custom_batch` için eşdeğer test yok (`grep` boş) — tam da AUTO'nun orphan blok bırakmamak için güvendiği yol.
- **[ORTA]** `data.py`'deki dosya kilidi ve atomic-write yardımcıları (`_cache_lock`, `_atomic_to_parquet`, `_atomic_write_json`) için doğrudan unit test yok — `_cache_lock`'un stale-lock kırma mantığı geçmişte bir kez gerçekten bozulmuş (M121), yine de doğrudan test yok.
- **[ORTA]** `_bybit_gap_frames` (önbellek ortasındaki veri deliklerini bulup dolduran fonksiyon) test edilmemiş — mevcut testler yalnız baştan/sondan genişletmeyi kapsıyor, orta-boşluk senaryosu hiç test edilmiyor.
- **[ORTA]** `repair_massive_intraday.py` tamamen test kapsamı dışı — production parquet dosyalarını backup+replace ile değiştiren bir script, sıfır test kapsamı.
- **[DÜŞÜK]** `chart_indicators.py` sıfır test kapsamına sahip — SMA/EMA/RSI/Bollinger hesaplayan saf fonksiyonlar, kullanan route (`chart.py`) için de test yok.
- **[DÜŞÜK]** `_base_ccy` test edilmemiş ve `_instrument_meta` içinde aynı mantık tekrar (kopya) yazılmış — tek kullanım yerinde gerçek mantık hiç çalıştırılmadan `monkeypatch` ile stub'lanmış.

## 🔴 En Kritik 3 Madde
- **[YÜKSEK]** `composer.py` import zamanında registry hatasını yakalamıyor — registry.json bozulursa `import composer`'a bağımlı HER route modülü çöküp sunucu tamamen ayağa kalkamaz hale gelir. _(Code Review)_
- **[YÜKSEK]** AUTO (amiral gemisi özellik) hiçbir testte gerçek worker ile uçtan uca sürülmüyor — tüm testler `_agent_worker`'ı sahteleştiriyor. _(E2E)_
- **[YÜKSEK]** `repair_massive_intraday.py` yalnız türetilmiş TF barlarını düzeltiyor, normal ingest tekrar çalıştırılırsa düzeltme sessizce silinir. _(Entegrasyon)_
