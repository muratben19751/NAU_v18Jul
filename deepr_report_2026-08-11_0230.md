## Performans Review
- **[YÜKSEK]** NAU-parity blokları her mumda 260 barlık pencereyi sıfırdan hesaplıyor (backtest'in baskın CPU maliyeti) — `block_library_nau.py`'deki `_eval_adx_threshold` / `_eval_stoch_rsi_cross` / `_eval_wave_trend_cross` her `on_bar` çağrısında `_nau_win(...)` ile highs/lows/closes'ın son 260 elemanını kopyalayıp `indicators.calc_adx` / `calc_stoch_rsi` / `calc_wave_trend`'i BAŞTAN çalıştırıyor. `indicators.calc_adx` (satır 168-216) her çağrıda `_tail3` ile 3 dilim kopyası + tr/plus_dm/minus_dm/dx_values için 4 ara liste (toplam ~1000 float) üretiyor; `calc_stoch_rsi` ise 246 iterasyonun her birinde 14 elemanlık dilim alıp min/max hesaplıyor. Bu, klasik bloklardan (`block_library_classic.py`) tamamen farklı bir maliyet sınıfı: onlar Nautilus streaming indicator nesnesi (`ExponentialMovingAverage`, `RelativeStrengthIndex`, `AverageTrueRange`) kullandığı için bar başına O(1).

Bu kutuda ölçtüm (260 barlık pencere, saf Python): calc_adx 114.8 µs/bar, calc_stoch_rsi 125.8 µs/bar, calc_wave_trend 71.5 µs/bar. Senaryo: AUTO'nun 1m Bybit yolunda pencere 730 güne kırpılıyor = ~1.05M bar → TEK bir adx bloğu için sadece gösterge hesabı ~115 saniye, tek stoch_rsi için ~126 saniye. Spec'te iki NAU bloğu varsa tek backtest'te ~4 dakika sadece burada geçiyor. AUTO döngüsü iterasyon başına bir backtest, robustness paketi ise (WFO pencere × aday + IS/OOS + multi-symbol) yüzlerce backtest çalıştırdığı için çarpan devasa. Çözüm yönü: klasik bloklardaki gibi durum taşıyan artımlı (incremental) hesap — Wilder ADX/EMA zaten özyinelemeli, her barda seed'den başlamak gereksiz. _(block_library_nau.py:51-163; indicators.py:115-216)_
- **[YÜKSEK]** `_validate_external_data` async handler gövdesinde doğrudan çalışıyor — tam parquet decode event loop'u kilitliyor — `web/routes/agent_backtest.py:4714` içinde `async def run(...)` gövdesinden `_validate_external_data(...)` DOĞRUDAN çağrılıyor; `asyncio.to_thread` yok. Bu fonksiyon (satır 4500-4530) `for _iv in intervals` döngüsünde her timeframe için `load_external_bars(instrument_id, _iv)` çağırıyor — bu da tüm serinin pandas parquet'ini diske gidip okuyor (1-MINUTE bir equity serisi için milyonlarca satır), ardından pandas maskeleme + `external_data_gap_report` çalıştırıyor. Multi-TF modda 3 interval için üç kez.

Senaryo: kullanıcı AUTO'yu external katalog kaynağıyla multi-TF başlatıyor; POST /agent/run birkaç saniye boyunca event loop'u tutuyor. Bu sırada TÜM sunucu donuyor — `agent_progress.html`, `auto_mission.html`, `backtest_progress.html` gibi 1 saniyelik HTMX poll'ları, açık her sekmedeki her istek bekliyor. Aynı dosyadaki diğer ağır işler (satır 124-130 `list_catalog`, satır 4967 `progress`) bilinçli olarak `asyncio.to_thread` ile sarılmış; bu çağrı o kalıptan kaçmış. Statik tarama ile doğruladım: koroutin gövdesinde (iç fonksiyon değil) duran tek ağır I/O bu. _(web/routes/agent_backtest.py:4714 (fonksiyon: 4450-4593))_
- **[ORTA]** Oturum detay sayfası çok-GB'lık JSONL'in tamamını akıtıyor, üstelik önbelleksiz — `web/routes/sessions.py:311-360` (`_read_events_head_tail`) 64 MB üstü oturum loglarında head 2 MB + tail 2 MB okuduktan sonra ARADAKİ TÜM DOSYAYI satır satır akıtıyor (`fh.seek(len(head))` → `for raw_line in fh` → her satırı UTF-8 decode + regex). Liste sayfasının özetleri `_SUMMARY_CACHE` ile mtime bazlı önbellekleniyor ama detay yolunda HİÇBİR önbellek yok — sayfa her yenilendiğinde tam maliyet yeniden ödeniyor.

Bu kutudaki gerçek veri: `~/.cache/nautilus_web_app/agent_sessions` altında 111 dosya, toplam 12.1 GB; en büyüğü `6a6ab8e4.jsonl` = 4.75 GB, 64 MB üstü 11 dosya var. Sıcak sayfa önbelleğiyle 200 MB'lık orta-geçişi 0.26 s ölçtüm → 4.75 GB için ~6 s; soğuk diskte (NVMe ~200 MB/s) ~25 s, HDD'de dakikalar. Yani `GET /sessions/6a6ab8e4` her açılışta 4.7 GB disk okuması yapıyor ve bu süre boyunca `to_thread` havuzundan bir iş parçacığını tutuyor. En azından `_SUMMARY_CACHE` ile aynı (mtime_ns, size) anahtarlı bir detay önbelleği gerekli — kapanmış oturum dosyaları hiç değişmiyor. _(web/routes/sessions.py:311-360, 726-757)_
- **[ORTA]** `/data` index paneli 50 satır göstermek için tüm ticker evrenini kuruyor ve kalıcı RAM'de tutuyor — `web/routes/data.py:124-130` `list_catalog(index_limit=50)` çağırıyor, ama `data.py:1265-1285`'teki `_index_rows` limit/query'yi SADECE önbellekteki hazır satırlara uyguluyor; `_index_rows_uncached` (satır 1294-1339) registry'deki HER ticker için `_instrument_meta` (Nautilus Equity nesnesi kurulumu) + 5 granularity'nin her biri için `_read_parquet_stats` (path.exists) ve `nautilus_catalog_bar_state` (dir is_dir + glob) çalıştırıyor. Koddaki yorum bunun bilinçli olduğunu söylüyor ama ölçek hesaba katılmamış: `discover_index_tickers` docstring'i kaynağın ~2 GB'lık Polygon grouped-daily dosyası olduğunu belirtiyor, yani ticker sayısı ~10.000 mertebesinde.

10k ticker × 5 granularity = 50.000 hücre. Ölçtüğüm birim maliyetler: `_instrument_meta` 6.8 µs, `nautilus_catalog_bar_state` (miss) 17.8 µs → yalnızca bunlar ~1 s, üstüne 50k `path.exists()` syscall'ı ve 50k+ dict allocation. Sonuç `_index_rows_cache` modül global'inde tutuluyor, yani ~10k satır × 5 bar_type dict'i (on-larca MB) sunucu ömrü boyunca RAM'de kalıyor ve TTL (30 s) dolduğunda tekrar kuruluyor. Filtre/limit'in ticker LİSTESİNE uygulanıp yalnız gösterilecek satırların I/O'sunun yapılması gerekiyor (kardeşi `_external_catalog_rows` zaten tam olarak bunu yapıyor: satır 1810-1811'de limit'i I/O'dan ÖNCE uyguluyor). _(data.py:1265-1339; web/routes/data.py:115-141)_
- **[ORTA]** `_external_catalog_rows` hiç önbelleklenmiyor — kardeş panellerin ikisinde de 30 sn TTL var — `data.py:1765-1885`'teki `_external_catalog_rows` her `/data` GET'inde sıfırdan çalışıyor: (a) her katalog kökü altındaki `data/bar` dizinini tam `iterdir()` ediyor, (b) gösterilen 50 enstrümanın her bar_type dizini için `d.glob('*.parquet')` yapıp HER dosya için `pyarrow.parquet.read_metadata` çağırıyor. Aynı sayfadaki `_bybit_rows` (data.py:1165-1180) ve `_index_rows` (1260-1291) tam da bu maliyet için 30 saniyelik TTL önbelleğine kavuşturulmuş; external paneli bu düzeltmenin dışında kalmış.

Bu kutuda ölçtüm: 96 enstrüman / 293 parquet dosyası ile 72 ms (`list_catalog()` bütünü 255 ms). Kod içindeki yorum (satır 1793) gerçek masaüstü kataloğunun 592 enstrüman olduğunu söylüyor; 1-MINUTE dizinlerinde burada bile enstrüman başına 26 dosya var. 592 enstrüman × 7 timeframe = ~4000 dizin girişi taraması + yüzlerce footer okuması → sayfa başına saniyeler mertebesinde, tamamen tekrar eden disk I/O'su. _(data.py:1765-1885 (list_catalog: 2285))_
- **[ORTA]** `reports._PARSE_CACHE` 20 MB'lık logun tüm parse edilmiş halini süresiz RAM'de tutuyor (~90 MB) — `web/routes/reports.py:274-297`'deki `_parsed_log_records()` backtest_log.jsonl'in tüm satırlarını `json.loads` edip listeyi modül global `_PARSE_CACHE`'te (mtime_ns, size) anahtarıyla saklıyor. Tahliye politikası yok; sunucu ömrü boyunca duruyor.

Gerçek veri: bu kutuda `~/.cache/nautilus_web_app/backtest_log.jsonl` = 20.04 MB (zaten `web/shared.py:249`'daki 20 MB rotasyon eşiğinde). tracemalloc ile ölçtüm: 95 kaydın parse'ı 0.66 s sürüyor ve 90 MB Python heap tutuyor (kayıtlar `spec` + `metrics` iç içe dict'leri taşıdığı için satır sayısı az ama nesne sayısı çok). Yani /reports sayfasına bir kez girilmesi sunucunun kalıcı bellek ayak izini 90 MB artırıyor. Aynı desen `sessions.py:225`'teki `_SUMMARY_CACHE`'te de var (sınırsız, run_id başına bir kayıt) — o daha küçük ama ikisi de projedeki diğer önbelleklerin (`ProgressStore`, `ChatStore`, `reports._DETAIL_CACHE` cap=8) aksine sınırsız. _(web/routes/reports.py:258, 274-297; web/routes/sessions.py:225)_
- **[ORTA]** Token rozeti her yoklamada ledger'ın tamamını İKİ kez katlıyor; kayıt listesi sınırsız büyüyor — `web/templates/base.html:94` `/tokens/badge`'i `every 60s` yokluyor. `web/routes/tokens.py:45-46` bu istekte `token_ledger.summary(since=...)` ve `token_ledger.summary()` olmak üzere İKİ ayrı çağrı yapıyor. `token_ledger._parsed_records` (satır 203-232) artımlı parse ile dosya okumayı çözmüş — ama `summary()` (satır 235+) her çağrıda döndürülen TÜM kayıt listesi üzerinde baştan katlama yapıyor, yani O(n) hesap yoklama başına iki kez tekrarlanıyor ve `since` filtresi de bu tam geçiş içinde uygulanıyor.

Ayrıca `_PARSE_CACHE` içindeki `records` listesi hiç kırpılmıyor; kodun kendi yorumu (satır 211) "nothing here yet deletes/rotates it" diyor. Ledger her LLM çağrısına bir satır yazıyor (`record`, satır 153) ve AUTO oturumları binlerce çağrı üretiyor — şu an 1.04 MB, ama aylar içinde yüz binlerce dict'e ulaşıp hem RAM'i hem 60 saniyelik yoklama maliyetini doğrusal büyütür. Aynı O(n) katlama `web/routes/agent_backtest.py:4414-4415`'te (`async def tokens`) doğrudan koroutin gövdesinde de yapılıyor. _(token_ledger.py:199-232, 235+; web/routes/tokens.py:43-58)_
- **[ORTA]** TF sweep aynı parquet dosyasını interval başına iki kez tam okuyor — `web/routes/backtest.py:2545-2559` (`_run_one`): tarih alanları boşsa önce `_df = _pd.read_parquet(cp)` ile TÜM önbellek dosyası belleğe alınıyor — tek amaç `_df.index[0]` ve `_df.index[-1]`'i öğrenmek. Hemen ardından `load_bybit_bars(...)` çağrılıyor ve o da `data.py:774`'te aynı dosyayı `pd.read_parquet` ile BAŞTAN okuyor. Yani ilk/son bar tarihini bulmak için tam bir kolon decode'u boşa gidiyor.

Senaryo: kullanıcı tarih vermeden BTCUSDT için 8 timeframe sweep başlatıyor; sweep bunları `ThreadPoolExecutor` ile paralel koşturuyor (satır 2604), yani 8 iş parçacığı aynı anda 8 büyük parquet'i iki kez okuyor. 1m BTCUSDT önbelleği milyonlarca satır → yüzlerce MB gereksiz decode ve RAM tepe noktası. `data._read_parquet_stats` (data.py:972) tam bu iş için var ve sadece ~4 KB'lık parquet footer'ını okuyor; burada kullanılmamış. Aynı desen satır 2481 ve 740'ta da var. _(web/routes/backtest.py:2545-2559 (ayrıca 2481, 740))_
- **[DÜŞÜK]** `external_instrument_object` her çağrıda tüm katalog enstrümanlarını yeniden decode ediyor — `data.py:1983-1997` her çağrıda `ParquetDataCatalog(str(root))` kurup `cat.instruments()` ile katalogdaki TÜM enstrüman tanımlarını çözüyor, sonra doğrusal aramayla tekini buluyor — önbellek yok. Oysa hemen üstteki `_external_instrument_meta` (satır 1646-1680) tam olarak aynı `cat.instruments()` geçişini yapıp sonucu `_EXT_INSTRUMENT_META`'da root başına önbelleğe alıyor; yani decode zaten bir kez yapılıp atılıyor.

Senaryo: `backtest_robustness.py:1258` bunu multi-symbol'ün SIRALI dalında sembol başına döngü içinde çağırıyor (`for sym in symbols` → her sembol için 592 enstrümanlık tam decode). `web/routes/backtest.py:887` her external backtest koşusunda, `sandbox.py:135` her sandbox çocuğunda çağırıyor. Nesnelerin de root başına önbelleklenmesi (veya `_external_instrument_meta` ile tek geçişe indirgenmesi) sembol sayısıyla çarpan maliyeti kaldırır. _(data.py:1983-1997; backtest_robustness.py:1258)_
- **[DÜŞÜK]** `chart_indicators._bollinger` hâlâ O(n·period) — dosyanın kendi yorumu aksini iddia ediyor — `chart_indicators.py:97-110`: her `i` için `window = closes[i - period + 1 : i + 1]` dilimi alınıp `sum((x - m) ** 2 for x in window)` ile varyans hesaplanıyor. Aynı dosyanın başındaki yorum (satır 28-40) `_sma`/`_ema`/`_rsi`'nin O(n)'e çekildiğini ve O(n·period) iddiasının "yanlış" olduğunu söylüyor — ama `_bollinger` bu incelemenin dışında kalmış; gerçekten pencereyi her adımda yeniden tarıyor.

Senaryo: `web/routes/chart.py:183` `indicators_for_spec` çağırıyor ve aynı dosyanın `_MAX_WINDOW_CANDLES` sınırı 60.000 mum. bollinger_break bloğu olan bir spec ile geniş bir grafik penceresi açıldığında period=20 için ~1.2M aritmetik işlem + 60.000 liste dilimi allocation'ı yapılıyor. Kare toplamının koşan pencere (running sum of squares) ile tutulması bunu O(n)'e indirir — `_sma` hemen üstünde zaten bu deseni kullanıyor. _(chart_indicators.py:97-110)_
- **[DÜŞÜK]** `_bybit_gap_frames` boşluk tespitini saf-Python döngüsüyle yapıyor; `_load_robustness_index` her /reports isteğinde yeniden parse ediliyor — İki küçük ama kolay kazanç:

(1) `data.py:638-644`: önbellekte boşluk varsa `for cur in ms[1:]` ile numpy int64 dizisi Python'da tek tek geziliyor, her eleman `int()` ile boxing'e uğruyor. 1M barlık bir 1m önbelleği için ölçtüm: çağrı başına 0.10 s. `load_bybit_bars` her çağrıldığında (chart, sweep, robustness, agent Phase-0) yeniden ödeniyor. `np.diff(ms) > step_ms` ile vektörleştirmek maliyeti ~1 ms'e düşürür.

(2) `web/routes/reports.py:306`: `_load_rows` her çağrıda `_load_robustness_index()` çağırıyor ve bu fonksiyon (satır 169-201) ROBUSTNESS_LOG'un tamamını baştan `json.loads` ediyor — hemen yanındaki `_parsed_log_records()` (satır 274) tam olarak bu iş için (mtime_ns, size) anahtarlı bir önbellek kullanıyor, ama robustness logu bu düzeltmenin dışında kalmış. Şu anki dosya 4.9 MB / 7 kayıt (0.03 s) olduğu için etki küçük; ancak kayıt başına ~700 KB olduğundan robustness koşuları biriktikçe doğrusal büyür. _(data.py:616-688; web/routes/reports.py:169-201, 306)_

## Mimari Review
- **[YÜKSEK]** agent_backtest.py bir HTTP route modülü değil, uygulamanın çekirdek motoru (5227 satır, %84'ü domain mantığı) — C:\myAI_Projects\NAU_v18Jul\web\routes\agent_backtest.py dosyasındaki ilk route handler 4400. satırda (`@router.get("/tokens")`). Ondan önceki ~4400 satır tamamen alan mantığı: skorlama (`_score` 1259), sıralama/eleme (`_rank_and_filter` 1428), robustluk kapısı (`_robustness_passed` 2581, `_run_full_robustness` 2958), WFO pencere doğrulama (`_valid_wfo_windows` 2571), mühürlü holdout (`_split_holdout` 2817, `_sealed_holdout_stats` 2841), SQLite indeks (`_index_insert` 2874), LLM bütçe yönetimi (`_enforce_token_budget` 882), aday parmak izi/dedup (`_candidate_fingerprint` 1124) ve 840 satırlık `_agent_worker` (3247). Sonuç: (a) 5 fazlı otonom pipeline'ın tamamı FastAPI sunum katmanında yaşıyor, HTTP dışı hiçbir tüketici (CLI, zamanlanmış iş, worker süreç) bunu web katmanını yüklemeden kullanamıyor; (b) 12 test dosyası ve conftest bu domain mantığına 63 ayrı `from web.routes.agent_backtest import ...` ile ulaşıyor. Modül docstring'i (55-61. satır) bölünmemeyi açıkça `tests/test_lock_nesting.py`'nin AST taramasına bağlıyor — ama o test `_TARGETS` adlı, içinde `"web/routes/agent_backtest.py"` string'i olan bir sözlük (tests/test_lock_nesting.py:22-36); dizin glob'una çevrilmesi önünde teknik engel yok. Yani mimari bir dondurma kararı, düzeltilebilir bir test kısıtına dayandırılmış. _(web/routes/agent_backtest.py:1-4399 (route'lar 4400+); tests/test_lock_nesting.py:22)_
- **[YÜKSEK]** Bağımlılık yönü tersine: izolasyon/motor katmanı (sandbox.py) web route modülünü import edip private global'ini mutasyona uğratıyor — `sandbox._robustness_child` — yani ayrı bir işlemde (child process) çalışan izolasyon giriş noktası — şunu yapıyor: `import web.routes.agent_backtest as ab` → `ab._IPC_Q = q` → `ab._run_full_robustness(...)` (sandbox.py:366-379). HTTP servis etmeyen bir worker süreci, sırf robustluk suite'ini çağırabilmek için tüm FastAPI route modülünü (APIRouter, web.shared, ProgressStore, oturum log dizini dahil) yüklüyor ve başka bir modülün private modül-global'ini (`_IPC_Q`) dışarıdan set ediyor. Statik import grafiğinde bu iki gerçek döngü üretiyor: `sandbox -> web.routes.agent_backtest -> sandbox` ve `parallel_exec -> sandbox -> web.routes.agent_backtest -> parallel_exec`. Doğru yön tam tersi olmalı: robustluk motoru katmanın altında durmalı, route (ve sandbox) onu çağırmalı. Bugünkü hâliyle motorun davranışını değiştirmek route modülünü düzenlemeyi, route modülünü bölmek ise child-process yolunu kırmayı gerektiriyor. _(sandbox.py:347-384; parallel_exec.py:120-186)_
- **[ORTA]** Strategy Studio'nun ayrı LLM istemcisi tek choke point'i ve token muhasebesini tamamen baypas ediyor — strategy_studio/ai.py:60-174'teki `HttpAnthropicClient` httpx ile doğrudan `https://api.anthropic.com/v1/messages`'e gidiyor (satır 125-126) ve web/routes/strategy_studio.py:175'te `LLM = HttpAnthropicClient()` olarak modül seviyesinde bağlanıyor. Oysa llm_dispatch._create_message_once kendi docstring'inde 'the single choke point through which all app LLM calls pass' diyor: token_ledger kaydı, `_observe_llm` telemetrisi, `_admit_llm_request` bütçe admission'ı, `_check_llm_cancelled` iptali, kredi tükenmesi fallback'i, truncation retry + öğrenilen tavan hepsi orada. Studio istemcisi bunların hiçbirini kullanmıyor; model seçimi, max_tokens, transient-retry (429/5xx/timeout) ve truncation-retry mantığını sıfırdan yeniden yazıyor ve `token_ledger.record` çağırmıyor (`grep token_ledger strategy_studio/` boş). Sonuç: Studio'nun her AI önerisi ve her AI loop iterasyonu (geçersiz JSON'da 2, truncation'da 2 çağrı daha) hiçbir yerde muhasebeleşmiyor; `GET /agent/tokens`, `/tokens/badge` ve `/tokens/table` yalnız `token_ledger.summary()` okuduğu için Studio harcaması sıfır görünüyor — token/maliyet raporu sistematik olarak eksik. Ayrıca model gösterimi de kopuk: sidebar `engine_model_label()`/`agent.model_label()` gösterirken Studio `NAUTILUS_STUDIO_LLM_MODEL` veya hardcoded `claude-sonnet-4-6` kullanıyor. Modül docstring'i bunu 'INTEGRATION POINT' diye işaretlemiş ama entegrasyon hiç yapılmamış. _(strategy_studio/ai.py:60-172; web/routes/agent_backtest.py:4400-4415)_ _(boyutlar: Mimari Review, Entegrasyon Testleri)_
- **[ORTA]** server.py ile web/routes arasında çift yönlü bağımlılık: 54 adet fonksiyon-içi `from server import ...` — server.py router'ları modülün en altında (508-541) import ediyor ve satırda kendi yorumu bunu itiraf ediyor: `# noqa: E402 (late import: routers import from server, circular)`. Karşı yönde 15 route dosyasında toplam 54 kez fonksiyon gövdesinden `from server import templates` / `get_market_info` / `get_bars` yapılıyor (en yoğunu web/routes/strategy.py'de 15, web/routes/backtest.py'de 11). Yani kompozisyon kökü (entrypoint) yaprak modüller tarafından import ediliyor. Bunun somut bedeli: (a) Jinja ortamı ve `get_market_info` gibi bağımlılıklar enjekte edilmiyor, global olarak çekiliyor — bir route'u server.py'yi yüklemeden test etmek/yeniden kullanmak mümkün değil; (b) `loop.py:25` bu kanaldan `get_bars`'ı, yani bir alan servisini, entrypoint üzerinden alıyor. Standart çözüm (templates'i web/shared veya bir `deps.py`'ye taşıyıp `Request.app.state` ya da FastAPI `Depends` ile vermek) uygulanmamış. _(server.py:508-541; web/routes/strategy.py, web/routes/backtest.py (54 çağrı noktası))_
- **[ORTA]** Kalıcı depolama kökü 8 modülde bağımsız olarak hardcoded; env override yok, studio.db ise repo kökünde — `Path.home() / ".cache" / "nautilus_web_app"` ifadesi hiçbir ortak sabite bağlı olmadan 8 ayrı yerde tekrar ediliyor: data.py:56 (CACHE_DIR), web/shared.py:234 (_CACHE_DIR), composer.py:583 (CATALOG_FILE), custom_block_store.py:54 (STORE_DIR), token_ledger.py:41 (LEDGER_PATH), web/routes/agent_backtest.py:230 (_AGENT_INDEX_DB), web/routes/reports.py:35 (REPORTS_LAYOUT). Hiçbirinde ortam değişkeni ile yeniden yönlendirme yok — veri dizinini taşımak, ikinci bir instance çalıştırmak veya testleri izole etmek için her modülü ayrı ayrı yamamak gerekiyor. Bedeli test kodunda görünür durumda: tests/conftest.py:29-36, tek bir `SESSION_LOG_DIR` için `web.shared`, `web.routes.agent_backtest` ve `web.routes.sessions` olmak üzere ÜÇ ayrı binding'i monkeypatch etmek zorunda ve docstring'i bunun neden gerektiğini uzun uzun açıklıyor. Ayrıca strategy_studio/store.py:25 tek başına farklı davranıp `DB_PATH = Path(__file__).resolve().parents[1] / "studio.db"` ile çalışma zamanı verisini KAYNAK AĞACINA yazıyor — kalan her şey ~/.cache altında. _(data.py:56; web/shared.py:234; token_ledger.py:41; strategy_studio/store.py:25; tests/conftest.py:29)_
- **[ORTA]** Merkezi yapılandırma yok: ~45 ortam değişkeni, 5 farklı ön ek, 18 dosyada import anında okunuyor — Env değişkenleri beş ayrı isimlendirme ailesine dağılmış (NAU_*, NAUTILUS_*, AGENT_*, STUDIO_*, MASSIVE_/POLYGON_*) ve her modül kendi okumasını yapıyor: llm_client.py'de 12, web/routes/agent_backtest.py'de 12, openrouter_backend.py'de 7, llm_dispatch.py'de 5 çağrı. Tek bir şema/doğrulama/varsayılan tablosu, tek bir dokümante liste yok; app_constants.py yalnızca `env_float`/`env_int` yardımcılarını paylaşıyor, isimleri değil. Daha ciddi olanı: bazı okumalar import anında sabitleniyor ve kompozisyon kökü bir route modülünün içinde yaşıyor — web/routes/strategy_studio.py:128-176 arasında `ADAPTER`, `TRIAL_ADAPTER`, `ENGINE_SWEEP_MAX_RUNS`, `OPTIMIZER`, `RUNNER`, `LLM` hepsi modül seviyesinde, `os.environ` okunarak kuruluyor. Bu desen zaten bir kez ısırmış: `_optimizer()` fonksiyonunun (147-159) tüm varlık sebebi, `OPTIMIZER`'ın import anında yakaladığı `TRIAL_ADAPTER` ile sonradan değiştirilen `TRIAL_ADAPTER`'ın ayrışması ve sweep'lerin sessizce yanlış motorda koşması. _(web/routes/strategy_studio.py:114-176; llm_client.py; web/routes/agent_backtest.py:129-227)_
- **[ORTA]** Katman sınırlarını delen private (alt tire) import'lar yaygın; en kötüsü runtime katmanının web route'undan fonksiyon çekmesi — Modüller birbirlerinin `_` ile başlayan iç fonksiyonlarını rutin olarak import ediyor, yani hiçbir modülün gerçek bir kamusal API'si yok. Örnekler: data.py:1370 veri katmanından motor katmanına uzanıyor (`from backtest import _bars_from_df, _make_bybit_bar_type, _make_bybit_instrument`); web/routes/backtest.py:683 ve 2454, web/routes/chart.py:122, web/routes/lab.py:176, web/routes/agent_backtest.py:2348 hepsi `from data import _bybit_cache_path` ile veri modülünün dosya-yolu iç detayına bağımlı; parallel_exec.py:120/147 `from sandbox import _child_stdio_guard, _build_instrument_bar_type`; backtest_robustness.py:1236 ve sandbox.py:132-150 `from backtest import _make_*` ailesini çekiyor. En ters olanı loop_runner.py:50: bir runtime/arkaplan modülü `from web.routes.backtest import _log_backtest` yapıyor — üstelik o ad web/routes/backtest.py:57'de sadece `web.shared.log_backtest`'in takma adı. Yani runtime katmanı, ihtiyacı olan kalıcılık yardımcısına ulaşmak için gereksiz yere belirli bir HTTP route modülünden geçiyor. _(loop_runner.py:50; data.py:1370; web/routes/backtest.py:683; parallel_exec.py:120)_
- **[ORTA]** data.py tek modülde dört veri kaynağı + önbellek + kilit + sunum satırı üretimi (2451 satır) — data.py aynı anda şunları barındırıyor: Bybit REST istemcisi ve sayfalama (555-690), NFS üzerindeki index tick CSV'lerinin okunması/agregasyonu (335-518), harici salt-okunur Nautilus katalogları (1646-2234), Nautilus ParquetDataCatalog yazımı (1347-1645), dosya kilidi + atomik parquet/json yazımı (138-222) ve TTL'li bellek içi önbellekler (1165-1346). Bunların üstüne SRP'yi asıl kıran katman şu: modül doğrudan /data sayfasının view-model'ini üretiyor. `_bybit_rows_uncached` (1188-1250) `interval_label`, `state` ("ingested"/"available"/"not-yet-ingested"), `warnings`, `cache_path` gibi tamamen sunuma ait alanlar döndürüyor; `list_catalog(source, limit, query)` (2259) ise sayfalama/arama gibi UI kaygılarını veri erişim katmanına taşıyor. Veri kaynağı eklemek veya /data sayfasının tasarımını değiştirmek aynı 2451 satırlık dosyaya dokunmayı gerektiriyor. _(data.py:1188-1250, 2259-2301)_
- **[DÜŞÜK]** Sunum katmanı, alan katmanının serbest metin log satırlarını regex/anahtar kelime ile geri-mühendislik yapıyor — web/mission.py:34-80'deki `_step_kind(msg)`, AUTO kokpitindeki konsol satırlarını renklendirmek için `_add_step` ile yazılmış insan-okunur mesajları küçük harfe çevirip içinde "robustness", "monte carlo", "walk-forward", "holdout", "loading catalog data", "cropped", "bar", "block", "strateji", "claude" gibi anahtar kelimeler arıyor — sıralamanın kendisi de anlam taşıyor (docstring bunu kabul ediyor). Yani olay tipi (`kind`) üretildiği yerde yapısal bir alan olarak taşınmıyor, tüketildiği yerde metinden tahmin ediliyor. web/routes/agent_backtest.py'deki herhangi bir `_add_step` mesajının sözcüğü değiştiğinde (ya da Türkçeleştirildiğinde) kokpitin sınıflandırması sessizce bozulur; hiçbir tip kontrolü veya test dışı mekanizma bunu yakalamaz. Aynı desen agent_backtest.py:647'deki `_PROGRESS_NOISE` regex'inde de var — modül kendi ürettiği metinleri yine regex ile filtreliyor. _(web/mission.py:34-80; web/routes/agent_backtest.py:647)_
- **[DÜŞÜK]** Paketleme/isim alanı yok: 37 kök modül düz yerleşimde, ETL script'leri uygulama modülleriyle aynı seviyede, aynı ad birden fazla modülde — pyproject.toml'da `[build-system]` bölümü ve paket/`packages` yapılandırması hiç yok; buna rağmen CLAUDE.md `pip install -e ".[dev]"` diyor ve testler kurulu paket yerine tests/conftest.py:7'deki `sys.path.insert(0, ...)` ile çalışıyor. Kökte 37 .py modülü var ve bunların 7'si uygulama tarafından hiç import edilmeyen, yalnızca birbirini import eden ayrı bir toplu-veri hattı (download_flatfiles, download_grouped_daily, download_massive, ingest_equities, stitch_ticker, repair_massive_intraday, extract_symbol_ticks) — bir `etl/` paketi altında sınırlanmaları gerekirken uygulama çekirdeğiyle aynı isim alanını paylaşıyorlar. Ek olarak aynı taban ad birden fazla modülde tekrarlanıyor: `backtest` üç kez (backtest.py, web/routes/backtest.py, strategy_studio/backtest.py), `data` iki kez (data.py, web/routes/data.py), `tearsheet` iki kez (web/tearsheet.py, web/routes/tearsheet.py), ayrıca web/routes/studio.py ile web/routes/strategy_studio.py birbirine yakın URL uzayını (`/studio` ve `/studio/{id}`) paylaşıyor. Import satırlarına bakarak hangi katmandan bahsedildiğini anlamak mümkün değil. _(pyproject.toml:1-33; tests/conftest.py:7; proje kökü (37 modül))_

## Code Review
- **[KRİTİK]** codegate: `**=` ve `<<=` (AugAssign) üs/kaydırma denetimlerini atlıyor — sunucu süreci kilitlenebilir — `validate_generated_code` içindeki AST yürüyüşü `_check_pow` ve `_check_lshift`'i YALNIZCA `isinstance(node, ast.BinOp)` olduğunda çağırıyor (codegate.py:489-492). Oysa `ast.AugAssign` de `_ALLOWED_NODES` içinde ve BinOp DEĞİL, yani `x **= 999999` / `x <<= 999999` hiçbir denetimden geçmiyor. Canlı doğrulandı: her ikisi de `validate_generated_code`'dan KABUL alıyor. Bu, `_check_pow`'un kendi docstring'inde anlattığı tam senaryodur — tek bir CPython bytecode GIL'i tutar, `_LoopBudgetInjector` döngü olmadığı için hiçbir tick koymaz, `agent._test_execute_generated`'in `t.join(timeout=2.0)` guard'ı GIL tutan thread'i kesemez. Kod bu yolda web-server SÜRECİNİN İÇİNDE exec ediliyor (web/routes/strategy.py:246 `_preview_signals`, strategy.py:893-894 `POST /blocks/save-custom`, agent.py:990), yani form üzerinden gelen bir blok kodu tüm uygulamayı SIGKILL'e kadar donduruyor. `MAX_LITERAL_MAGNITUDE` de korumuyor: 999999 sabiti eşiğin altında. _(codegate.py:486-494 (validate_generated_code döngüsü); istismar yolları web/routes/strategy.py:246, 893)_
- **[YÜKSEK]** custom_block_store: memoize edilen registry sözlüğü paylaşılıyor ve yerinde mutasyona uğratılıyor — `_read_registry()` parse edilmiş dict'i `_REGISTRY_CACHE`'e koyup AYNI nesneyi döndürüyor (custom_block_store.py:224). `_registry_transaction()` (satır 438) ve `delete_custom()` (satır 505-515) bu nesneyi yerinde değiştiriyor (`reg[name] = ...`, `del reg[name]`). İki somut sonuç: (1) Doğruladım — bir transaction diske YAZMADAN sonlanırsa (ör. `save_custom_batch`'in `validate()` kolu, ya da `delete_custom`'da `_write_registry` I/O hatası) dosyanın mtime/size'ı değişmediği için cache geçersizleşmiyor ve süreç ömrü boyunca diskte OLMAYAN bir blok kaydını (ya da diskte VAR olan bir kaydın yokluğunu) servis ediyor; `composer.load_catalog` bunu katalog budamasında girdi olarak kullandığı için modülün kendi docstring'inin önlemeye çalıştığı 'yanlış registry görünümü' hatası geri geliyor. (2) `list_custom()` (satır 345-352) `_STORE_LOCK`'u sadece okuma için tutup dict'i kilit DIŞINDA geziyor; `_registry_guard` altındaki eşzamanlı bir yazıcı aynı nesneyi değiştirdiğinde `RuntimeError: dictionary changed size during iteration` alınır. Çözüm: `_read_registry` bir kopya döndürmeli (ya da cache'e derin/ayrı kopya konmalı). _(custom_block_store.py:180-238, 421-457, 501-528, 334-352)_
- **[ORTA]** download_massive: indirmeye başlamadan ÖNCE mevcut bar dizinleri siliniyor, hata durumunda geri dönüş yok — `download_minute_bars` ilk API çağrısından önce tüm hedef ticker'ların bar dizinlerini siliyor (download_massive.py:205-208: `for d in bar_root.glob(f'{tk}.{venue}-*'): shutil.rmtree(d, ignore_errors=True)`). Sonrasında `_get` 6 denemede yanıt alamazsa, yıl `NOT_AUTHORIZED` dönerse (ücretsiz plan penceresi dışında — modülün kendi docstring'inde beklenen bir durum) veya ticker `except MassiveError` (satır 268-284) ile atlanırsa, silme çoktan olmuştur: `download()` 'hiçbir ticker'da bar yok — TF/manifest yazılmadı' deyip çıkar ve katalogda ÖNCEDEN başarıyla ingest edilmiş veri kalıcı olarak yok olur. Bu katalog aynı zamanda web uygulamasının okuduğu `data.EQUITY_CATALOG_DIR` (satır 200); sonuç olarak /data panelinden, Lab/Studio picker'larından ve o sembolü kullanan kayıtlı Studio stratejilerinden enstrüman kaybolur (`load_external_bars` → `ValueError: external catalog has no bars for ...`). Yanlış `--years` aralığı veya süresi dolmuş bir anahtarla tek bir koşum çalışan bir enstrümanı siler. Kardeş modüller bu sınıfı zaten çözmüş: `repair_massive_intraday._replace_day` `.bak` tutuyor, `download_flatfiles` atomik `.part→replace` kullanıyor. Silme en az bir yıl başarıyla indirildikten sonraya ertelenmeli veya eski dizin yedeklenmeli. _(download_massive.py:205-208, 316-322)_ _(boyutlar: Code Review, Entegrasyon Testleri)_
- **[ORTA]** repair_massive_intraday: `_fixed()`'in `precision` parametresi hiç kullanılmıyor — `def _fixed(values: list[float], precision: int)` imzası fiyat için 2, hacim için 0 alıyor (satır 63, 68) ama gövdede `precision` bir kez bile okunmuyor; ölçek daima `_FIXED_SCALE = 1e9`. Çağrı yerleri okundunda 'fiyat 2 basamak, hacim tam sayı olarak kodlanıyor' izlenimi veriyor — oysa hacim de 1e9 ile ölçekleniyor. Nautilus'un sabit-nokta gösterimi gerçekten 1e9 olduğu için şu an davranış doğru, fakat parametre yanıltıcı ölü koddur: ileride biri `precision`'a göre ölçek değiştirmeye kalkarsa ya da farklı bir precision geçerse sessizce hiçbir şey olmaz. Ya kaldırılmalı ya da gerçekten kullanılmalı. _(repair_massive_intraday.py:34-56)_

## Güvenlik (Security) Review
- **[ORTA]** htmx ve Chart.js CDN'den yükleniyor: SRI/CSP yok ve çevrimdışında arayüz tamamen ölü — web/templates/base.html:9-15 htmx'i unpkg.com'dan, Chart.js'i cdn.jsdelivr.net'ten, fontları fonts.googleapis.com'dan `integrity`/`crossorigin` olmadan çekiyor; server.py'de Content-Security-Policy, X-Frame-Options, X-Content-Type-Options, Referrer-Policy gibi hiçbir güvenlik başlığı yok (yalnız _limit_request_body ve _require_auth middleware'leri var). İki yönlü sonuç: (a) Güvenlik — unpkg/jsdelivr tarafında tedarik zinciri ele geçirilmesi veya MITM, uygulamanın origin'inde tam JS yürütmesi demek; htmx tüm etkileşim katmanı olduğu için strateji/veri/LLM yüzeyinin tamamı düşer ve serve.py docstring'ine göre uygulama Cloudflare tüneliyle internete de açılıyor. Sürüm sabitlemesi tek başına içerik bütünlüğü sağlamaz; bir CSP olsaydı etkiyi en azından daraltırdı. (b) Kullanılabilirlik — internet kesildiğinde, kurumsal proxy/DNS bu alan adlarını engellediğinde veya çevrimdışı bir makinede sayfa HTML'i normal görünür (CSS yerel) ama htmx yüklenmediği için hiçbir düğme, polling veya sekme çalışmaz; base.html:12'deki `htmx.config...` satırı `htmx is not defined` ile patlayıp sonraki inline script'leri de bozar ve kullanıcıya tek bir uyarı gösterilmez. Tutarsızlık projenin kendi içinde: lightweight-charts.js zaten web/static/ altına vendored edilmiş. Çözüm aynı: htmx ve Chart.js'i (ve fontları) yerelleştir, üstüne CSP + temel güvenlik başlıkları ekle. _(web/templates/base.html:9,11,13)_ _(boyutlar: Güvenlik (Security) Review, Brutal Review, End-User Negatif Test, End-User Pozitif Test)_
- **[ORTA]** Yansıyan XSS: hata gövdelerinde kullanıcı girdisi kaçışsız HTML'e gömülüyor (htmx bu gövdeleri DOM'a basıyor) — Birden çok hata yolunda ham istek verisi f-string ile HTMLResponse gövdesine escape edilmeden yazılıyor: web/routes/strategy.py:877 (`Invalid name: {name}` — Form alanı, dal tam olarak ad geçersizken çalışıyor), strategy.py:958 (`Custom block bulunamadı: {name}` — URL path parametresi), web/routes/agent_backtest.py:4639 (`invalid date: {_v}` — range_start/range_end form değeri), agent_backtest.py:4658 (`{model_label(model)}`, bilinmeyen id'yi `OR · <girdi>` olarak aynen döndürüyor), agent_backtest.py:4508-4535 (`{instrument_id}` ve ham `{_gap_exc}` istisna metni — ayrıca dosya yolu/iç modül adı sızdırıyor) ve ikinci-derece sink olarak web/routes/backtest.py:2692 (`state['error']` `<pre>` içinde kaçışsız). Normalde htmx 4xx gövdelerini swap etmez, ancak web/templates/studio.html:480-486 özellikle /agent/run için `d.shouldSwap = true; d.isError = false;` yapıyor ve base.html:12'de `htmx.config.allowScriptTags = true` ayarlı — yani `range_start=<img src=x onerror=...>` gibi bir değer işaretlemeyi (ve script etiketlerini) doğrudan kokpite bastırıyor. Yanıtlar text/html olduğu için cross-site form POST ile üst-seviye gezinmede de payload uygulamanın origin'inde çalışır. Proje içinde tutarsızlık açık: reports.py:541 ve `_detail_error` tam bu nedenle `markupsafe.escape` kullanıyor; bu sink'ler o düzeltmenin dışında kalmış. _(web/routes/strategy.py:877,958,1091; web/routes/agent_backtest.py:4639,4658; web/routes/backtest.py:2692)_ _(boyutlar: Güvenlik (Security) Review, End-User Negatif Test)_
- **[ORTA]** CSRF koruması yok; durum değiştiren POST'lar korumasız — Uygulamada CSRF token / origin-referer kontrolü hiçbir yerde yok; tek dolaylı savunma `nau_auth` çerezinin `samesite="lax"` olması (server.py:417). Bu iki sebeple yetersiz: (1) NAU_ACCESS_TOKEN boşken (varsayılan) çerez hiç gerekmiyor, dolayısıyla SameSite hiçbir şey korumuyor — operatörün tarayıcısında açılan herhangi bir kötücül sayfa `http://127.0.0.1:8111/...`'e otomatik form POST atarak POST /agent/run (para harcatan LLM turu), POST /loop/start, POST /strategy/blocks/save-custom, POST /studio/{id}/deploy, POST /data/catalog/write çağırabilir; 127.0.0.1'e bağlı olmak bu senaryoyu engellemez. (2) Yanıtlar HTML döndüğü için aynı istek yukarıdaki yansıyan XSS sink'lerini de üst-seviye gezinmede tetikler. Ek olarak hiçbir yanıtta X-Frame-Options/frame-ancestors yok, yani clickjacking ile aynı işlemler tıklama üzerinden de tetiklenebilir. _(server.py:399-427 (cookie/gate), web/routes/agent_backtest.py:4596, web/routes/loop.py:23, web/routes/strategy.py:855)_

## Brutal Review
- **[YÜKSEK]** Bağımlılık kilidi yok, [build-system] yok, sürüm tabanları serbest — CI her koşumda yeniden çözüyor ve bunu kendi yorumunda itiraf ediyor — pyproject.toml'da sadece nautilus_trader==1.230.0 sabit; anthropic, openai, pandas, pyarrow, numpy, ddgs, fastapi, uvicorn, jinja2, markdown hepsi `>=` tabanlı. uv.lock repoda YOK (kontrol ettim), setup.py/setup.cfg yok ve pyproject'te `[build-system]` bloğu da yok. CI `uv sync --extra dev` çalıştırıyor ve workflow'un kendi yorumu şunu yazıyor: '`uv sync` resolves from pyproject.toml (and uv.lock if committed). Add `--frozen` once uv.lock is committed.' — yani sorun biliniyor, çözülmemiş. Somut sonuç: bu repoda tek satır kod değişmeden, sırf pandas/pyarrow/fastapi yeni sürüm yayınladığı için main kırmızıya dönebilir; ve dün yeşil olan bir commit'i bugün aynı ortamda yeniden üretmek mümkün değil. Bir backtest laboratuvarı için bu ekstra can sıkıcı: sayısal sonuçların (Sharpe, drawdown) tekrar üretilebilirliği pandas/numpy sürümüne bağlı ve o sürüm hiçbir yerde sabitlenmemiş. regression_baseline.json (125 KB) tam da bu tekrar-üretilebilirliğe dayanıyor. _(pyproject.toml:1-33 ; .github/workflows/ci.yml:22-25)_
- **[ORTA]** LLM/kullanıcı kodu web sunucusu sürecinde exec ediliyor; duvar saati ve bellek sınırı yok, timeout sonrası thread sızıyor — Blok kodu sunucu sürecinin İÇİNDE üç ayrı yerde exec ediliyor: agent.py:973-992 (`_test_execute_generated`, POST /strategy/blocks/save-custom → web/routes/strategy.py:855-898 üzerinden form ile gelen kod), composer.py:407 (`_load_module_from_path`, sunucu açılışında ve her registry rebuild'inde) ve web/routes/strategy.py:265 (`_preview_signals`). codegate'in AST allowlist'i + safe_builtins + read-only modül proxy'leri üçünde de ortak, ama zaman/bellek koruması ortak DEĞİL ve hiçbiri yeterli değil: (a) agent yolundaki `t.join(timeout=2.0)` daemon thread'i ÖLDÜRMÜYOR — kullanıcı 'timed out' hatası alır ama kod arka planda çalışmaya devam eder; tekrarlanan gönderimlerle sunucu OOM'a taşınabilir. (b) composer loader ve `_preview_signals` hiçbir duvar-saati sınırına sahip değil, yalnız `compile_with_loop_budget`'a güveniyorlar. (c) Bütçenin kendisi zayıf: `LOOP_BUDGET_STEPS = 5_000_000` yalnız döngü ADIMI sayıyor (bellek/duvar saati değil) ve `_LoopBudgetInjector.visit_FunctionDef` bunu her `evaluate()` çağrısının başında SIFIRLIYOR — `_preview_signals` 150 bar döndüğü için tek bir HTTP isteği teorik olarak 750 milyon adım çalıştırabiliyor, iptal noktası olmadan, FastAPI'nin paylaşımlı thread havuzunda; aynı şekilde her adımda `s = s + s` / `x = [0]*1000000` yapan bir döngü bütçeye takılmadan GB'larca bellek harcıyor. Backtest yolu bunu sandbox.py'nin öldürülebilir alt süreciyle kurtarıyor; preview, save-custom ve loader kurtarmıyor. _(agent.py:990 ; composer.py:407 (_load_module_from_path) ; web/routes/strategy.py:265 (_preview_signals))_ _(boyutlar: Brutal Review, End-User Negatif Test)_

## End-User Negatif Test
- **[ORTA]** Backtest butonları 4xx yanıtlarında sessizce ölüyor: HX-Toast yok, htmx swap etmiyor, global responseError dinleyicisi yok — Backtest yüzeyindeki en doğal kullanıcı yolları hiçbir geri bildirim üretmeden bitiyor, çünkü ilgili 4xx yanıtları `HX-Toast` başlığı taşımıyor ve htmx 1.x 2xx dışı yanıtları DOM'a basmıyor (uygulamada global `htmx:responseError` dinleyicisi de yok). Somut yollar: (1) Spec seçici varsayılan `<option value=''>— new (describe below) —</option>` seçiliyken '▶ Run Saved'e basmak → POST /backtest/run `spec_id=''` → web/routes/backtest.py:620-625 `HTMLResponse('Spec not found.', 404)` → ekranda hiçbir şey olmuyor (aynı şey başka sekmede silinmiş spec ve boş spec_id ile gönderilen robustness formu için de geçerli). (2) Compose'da strateji kaydedip katalog satırındaki ▶ handoff'a basmak: `fragments/save_result.html:13-31` yalnız `#catalog` ve `#drafts`'ı OOB tazeliyor, `#spec-picker` (`fragments/backtest_body.html:45`) hiçbir yerde OOB güncellenmiyor, dolayısıyla `studioBacktestSpec()` (studio.html:491) `sel.value = specId` yaptığında o `<option>` DOM'da olmadığı için select boş değere düşüyor ve aynı sessiz 404'e çıkıyor. (3) PRO'daki hero '▶ Run Backtest' butonu (backtest_body.html:340-347) her zaman /backtest/describe'a POST ediyor; açıklama <10 veya >4000 karakterse backtest.py:1222-1234 HX-Toast'suz 400 dönüyor, spinner sönüyor, sebep söylenmiyor — üstelik kayıtlı spec seçilmişken de bu buton describe'a gittiği için 'kayıtlı stratejiyi seç + büyük Run'a bas' yolu tamamen hiçlikle sonuçlanıyor. Düzeltme deseni aynı dosyada zaten var: `_busy_response` (212-223) ve `_date_error_response` (245-249) HX-Toast koyuyor; `backtest_scripts.html:129-131` yorumu bu hata sınıfının ('Analizi Çalıştır' silently 404'd) başka bir yerde düzeltildiğini anıyor. _(web/routes/backtest.py:620-625; web/templates/fragments/backtest_body.html:45-59)_ _(boyutlar: End-User Negatif Test, End-User Pozitif Test, Uçtan Uca (E2E) Testler)_
- **[ORTA]** /chart/data geçmişe dönük pencere ile sınırsız Bybit backfill başlatıyor (istek saatlerce asılı kalıyor) — `web/routes/chart.py:62-81` yalnızca `(end_ts - start_ts)` genişliğine bakan bir mum-sayısı tahmini yapar (`_MAX_WINDOW_CANDLES`). Ancak `data.py:796-797`'de istek başlangıcı önbellek başlangıcından eskiyse `_fetch_segment(start_ms, cached_start_ms)` çağrılır — yani KÜÇÜK bir pencere bile önbelleğin başına kadar olan TÜM boşluğu doldurmaya çalışır. Örn. `?interval=1&start_ts=<2020>&end_ts=<2020+2 gün>`: tahmin ~2.9k mum olduğu için kapıdan geçer, sonra 2020→bugün arası 1 dakikalık backfill başlar; `_fetch_bybit_page` her sayfada ağ isteği + 429/5xx'te 2-32 sn uykuyla yeniden dener ve toplam sayfa sayısına hiçbir üst sınır yoktur. Bu sırada `_cache_lock` tutulduğu için aynı seriyi okuyan backtest/robustness koşuları da bloke olur. Tetiklenmesi için kötü niyet gerekmez: /reports ekranında eski bir koşunun bir işlemine tıklamak `_reloadForTrade` (web/static/chart.js:219) üzerinden tam olarak bu URL'i üretir. _(web/routes/chart.py:62-99; data.py:738-807)_
- **[ORTA]** Strategy Builder "▶ Run backtest": hızlı tekrar tıklama sınırsız paralel koşu yaratıyor — `web/routes/strategy_studio.py:841-878` her POST'ta yeni bir `run_id` üretip `store.create_run(...)` + `background_tasks.add_task(_execute_run, ...)` yapar; ne oturum/strateji bazlı "zaten çalışıyor" kontrolü ne de kapasite semaforu vardır (karşılaştırma: /backtest ve /robustness'ta oturum bazlı 409 guard, /agent/run'da `_AUTO_RUN_SLOTS` semaforu var). İstemci tarafında da koruma yok: `web/templates/studio/page.html:34-36` düğmesinde `hx-sync` veya `hx-disabled-elt` yok. Kullanıcı düğmeye üst üste bastığında (veya sekmeyi çoğaltıp bastığında) her tık bir arka plan görevi kuyruklar; `STUDIO_BACKTEST=nautilus` ayarlıyken bunların her biri gerçek bir motor koşusudur ve hepsi FastAPI'nin ortak thread havuzunda çalışır — havuz dolduğunda tüm senkron rotalar (ki /studio sayfası da senkron) yanıt vermez hale gelir. Ayrıca her tık studio.db'ye kalıcı bir çöp koşu satırı bırakır. _(web/routes/strategy_studio.py:841-878; web/templates/studio/page.html:34-36)_
- **[DÜŞÜK]** Blok parametrelerinde katalogdaki min/max sunucu tarafında uygulanmıyor — geçersiz period sessizce ölü blok üretiyor — `POST /strategy/drafts` (web/routes/strategy.py:356-401, özellikle 368-384) parametreyi yalnızca tipine göre `int()`/`float()` ile çeviriyor, çevirme başarısızsa default'a düşüyor; `block_meta.py`'deki `_BUILTIN_META` her parametre için tanımlı `min`/`max` (rsi period 2..50, donchian period min 5, ma_cross fast min 2) yalnızca HTML input attribute'u olarak kullanılıyor, sunucuda hiç kontrol edilmiyor. `ComposedStrategySpec.validate()` de (composer_spec.py:150-229) blok parametrelerini meta'ya karşı doğrulamıyor; yalnız `slow > fast` gibi çapraz kısıtlar var. Ayrıca aynı uçta `role = form.get('role', 'entry')` hiçbir whitelist'ten geçmiyor. Somut kırılmalar (doğrudan POST/curl ile): `p_period=0` + donchian_channel → `max([])` ValueError (block_library_nau.py:191-194); `p_lookback=0` + price_breakout → `max([])` ValueError (block_library_classic.py:80-82); `p_fast=0` + ma_cross (slow=5 ile çapraz kontrolü geçiyor) → ZeroDivisionError (block_library_classic.py:35-36). Üçü de `_eval_block`'un geniş `except`'i tarafından yutuluyor (composer.py:896-905) ve blok her barda None dönüyor: hata değil, **0 işlemli, sessizce geçerli görünen bir backtest**. Diğer senaryoda (period=100000, negatif değer) hata ancak backtest sırasında Nautilus göstergesi patlayınca, tam bir koşu harcandıktan sonra anlaşılmaz bir mesajla ortaya çıkıyor. Proje bu sınıfı başka yerde bilinçle kapatmış (robustness.py:119-128 `_clamp_commission_pct`, 'HTML min/max yalnız tarayıcı tarafı' notuyla), burada kapatmamış. _(web/routes/strategy.py:356-401; block_meta.py:20-181)_ _(boyutlar: End-User Negatif Test, Sınır Durumu (Edge Case) Testleri)_
- **[DÜŞÜK]** /lab/run geçersiz tarihi sessizce yok sayıp varsayılan pencerede koşuyor — `web/routes/lab.py:532-539`'daki `_parse_date` her hatada `except Exception: return None` döner ve `None`, worker'da "tarih verilmemiş" ile aynı anlama gelir. Kullanıcının girdiği (veya bir betiğin/eski tarayıcının gönderdiği) hatalı tarih hiçbir uyarı üretmeden düşer, koşu istenen aralık yerine varsayılan pencerede çalışır ve sonuç ekranında "istediğim tarih aralığı uygulanmadı" bilgisi hiçbir yerde görünmez. Sessiz yanlış-yorumlama, hatalı sonucun doğru sanılmasına yol açtığı için buradaki en riskli davranış biçimi; aynı uygulamada /backtest, /agent ve /studio backtest uçlarının üçü de aynı durumu 400 ile reddediyor. _(web/routes/lab.py:508-541)_
- **[DÜŞÜK]** AUTO 'hint' ve Studio 'ask' metinlerinde uzunluk sınırı yok (diğer tüm LLM uçları 4000 karakterle sınırlı) — `web/routes/backtest.py:330` ve `web/routes/strategy.py:45`'te tanımlı `_MAX_LLM_TEXT_LEN = 4000`, describe/plan/chat/suggest/blocks-generate uçlarının hepsinde uygulanıyor. Buna karşılık `POST /agent/run`'ın `hint` alanı (`web/routes/agent_backtest.py:4599`, prompta `agent.py:832`'de giriyor) ve `POST /studio/{id}/ai/suggest`'in `ask` alanı (`web/routes/strategy_studio.py:1305`) hiçbir uzunluk kontrolünden geçmiyor; tek sınır 2 MiB'lik genel gövde sınırı. Yapıştırılan büyük bir metin AUTO'da her turda yeniden prompta gömülür — sürekli modda onlarca tur boyunca — ve token bütçesini/faturayı kullanıcının farkına varmayacağı şekilde şişirir; ayrıca `hint` `web_research_strategies` üzerinden arama sorgusuna da giriyor. _(web/routes/agent_backtest.py:4599, 4779; web/routes/strategy_studio.py:1303-1305)_

## End-User Pozitif Test
- **[YÜKSEK]** Backtest sekmesinin katalog durumu sayfa render'ında bir kez okunuyor; ilk strateji kaydedildikten sonra 'No saved strategies yet' kalıyor — `web/templates/fragments/backtest_body.html:7-13` katalog boşken tüm Backtest gövdesini (enstrüman seçici, tarih alanları, doğal-dil describe akışı dâhil) bir empty-state ile değiştiriyor; geriye kalan '→ Open Strategy Composer' butonunun hedefi `/studio`, yani kullanıcının zaten üzerinde olduğu sayfa. `catalog` değişkeni sunucu tarafında sayfa render'ında bir kez okunuyor (web/routes/studio.py:221) ve hiçbir akış bu bloğu tazelemiyor. Senaryolar: (a) Kullanıcı Compose'da ilk stratejisini kaydeder (banner '✓ SAVED' der, 05 · Saved Strategies OOB dolar), Backtest sekmesine geçer → hâlâ 'No saved strategies yet.'; (b) SIMPLE sihirbazı doğal dille strateji üretip `append_to_catalog` ile kataloğa yazdığında (web/routes/backtest.py:1468) da hiçbir liste tazelenmiyor, oysa Adım 5 ekranı '✓ <isim> kataloğunuzda kayıtlı — PRO modda Saved Strategies altında bulacaksınız' diye söz veriyor (studio_simple.html:466-470); PRO'ya geçen kullanıcı boş liste görüyor ve üstelik `#result` düğümü sihirbaz slotuna taşınmış olduğu için az önceki sonucunu da göremiyor. Tek çözüm tam sayfa yenilemesi. (SIMPLE sihirbazı `{% if not catalog %}` ile singleton düğümleri kendisi ürettiği için yeni kullanıcı orada tıkanmıyor — yani telafi yalnız bir yüzeyde var.) _(web/templates/fragments/backtest_body.html:7-13, web/templates/fragments/studio_simple.html:206-214, web/routes/backtest.py:1468)_ _(boyutlar: End-User Pozitif Test, Uçtan Uca (E2E) Testler)_
- **[YÜKSEK]** Dashboard'da Start'a bastıktan sonra Stop butonu disabled kalıyor — `web/templates/dashboard.html:21-30` Start/Stop butonlarının `disabled` durumunu yalnızca sayfa render'ında `{% if running %}` ile belirliyor. Her iki form da `hx-target="#loop-status"` ile sadece rozeti değiştiriyor, `fragments/loop_status.html` ise yalnızca badge + iter sayısı döndürüyor (OOB swap yok). Senaryo: kullanıcı MODE=agent seçip ▶ Start'a basar → rozet "RUNNING" olur, iterasyonlar akmaya başlar, ama ■ Stop hâlâ disabled, ▶ Start hâlâ tıklanabilir ve MODE seçicisi hâlâ açık. Kullanıcı başlattığı döngüyü sayfayı F5'lemeden durduramıyor; ayrıca tekrar Start'a basabildiği için (sunucu tarafı guard yakalasa da) UI yalan söylüyor. _(web/templates/dashboard.html:21-34, web/templates/fragments/loop_status.html)_
- **[ORTA]** SIMPLE sihirbazının "Improve with AI" adımı backtest sonucunu AI'a hiç göndermiyor — PRO yüzeyindeki "💬 Improve via chat" butonu `hx-include="#gen-form"` ile `bt_pnl_pct / bt_sharpe / bt_max_dd / bt_n_trades / bt_win_rate / rob_*` gizli inputlarını da gönderiyor (`backtest_body.html:443-444`, `:367`). SIMPLE sihirbazının Adım 5'teki `swImprove()` ise `POST /backtest/chat/new`'e yalnız `description, symbol, category, interval` gönderiyor (`studio_simple.html:496-499`). `chat_new` bu alanları `_collect_chat_context` ile bekliyor; boş gelince AI, iyileştirmesi istenen backtest sonucunu (PnL, Sharpe, drawdown, işlem sayısı, robustness verdict) hiç görmüyor. Sihirbazın tüm vaadi "az önce koştuğun sonucu iyileştir" olduğu halde kullanıcı jenerik, sonuca bakmayan bir cevap alıyor — ve bunun neden yüzeysel olduğunu anlamasının hiçbir yolu yok. _(web/templates/fragments/studio_simple.html:474-504)_
- **[ORTA]** SIMPLE Adım 2'deki MAX butonu seçilen zaman dilimini yok sayıp hep 1 saatlik kapsamı dolduruyor — `studio_simple.html:124` şunu yapıyor: `(document.querySelector('.sw-quick.active')||{dataset:{}}).dataset.swInterval || '60'`. Ama `.sw-quick.active` seçicisi belge sırasında İLK eşleşmeyi döndürür ve bu, Coin satırındaki aktif buton (varsayılan BTCUSDT, `data-sw-symbol` taşır, `data-sw-interval` taşımaz). Dolayısıyla `dataset.swInterval` hep `undefined` → `'60'`. Senaryo: kullanıcı Timeframe olarak "5 min" seçer, Advanced'i açıp MAX'a basar → tarih kutuları 1 SAATLİK serinin kapsamıyla dolar. 5 dakikalık önbellek genelde çok daha kısa olduğundan koşu ya beklenenden az veriyle çalışır ya da "Insufficient data" ile ölür; kullanıcı kendi seçtiği tarihlerin nereden geldiğini bilmediği için hatayı teşhis edemez. _(web/templates/fragments/studio_simple.html:119-125)_
- **[ORTA]** SIMPLE sihirbazı Bybit kategorisini her zaman "linear" olarak sabitliyor, coin listesi ise spot/linear karışık — Adım 2'deki hızlı coin butonları `bybit_symbols | map(attribute="symbol") | unique` ile üretiliyor (`studio_simple.html:92`) — yani `list_catalog_bybit_symbols()`'un döndürdüğü `{symbol, category}` çiftlerinden kategori bilgisi kasıtlı olarak atılıyor (`data.py:1449-1478` spot/linear/inverse ayırıyor). Buna karşılık hem koşu (`swRun`, satır 413 ve 429) hem MAX (satır 122) hem robustness varsayılanı `category: "linear"` gönderiyor. Senaryo: önbellekte yalnız `spot` altında bulunan bir sembolü hızlı butondan seçen kullanıcı MAX'a basınca `/data/range?...&category=linear` 404 döner ve buton "veri yok" yazar (veri aslında var); ardından Run backtest, indirilmiş spot verisi yerine linear'ı kullanmaya çalışır. Kullanıcıya listelenen şey ile koşulan şey aynı değil. _(web/templates/fragments/studio_simple.html:88-99, 410-432)_
- **[ORTA]** Sol menüdeki "Strategy Builder" taze kurulumda kalıcı ölü bağlantı, ipucu bile görünmüyor — `base.html:50-57` Strategy Builder linkini `first_studio_strategy_id()` boşsa `href="#"` + `nav-disabled` yapıyor ve açıklamayı `title` özniteliğine koyuyor: "Henüz kayıtlı bir strateji yok — scripts/seed_studio.py veya Strategy Builder'da yeni oluştur". İki sorun: (1) `app.css:124` bu sınıfa `pointer-events: none` veriyor, dolayısıyla tarayıcı `title` tooltip'ini HİÇ göstermiyor — kullanıcı sadece soluk, tepkisiz bir menü öğesi görüyor, nedenini öğrenemiyor. (2) İpucunun önerdiği "Strategy Builder'da yeni oluştur" imkânsız: `web/routes/strategy_studio.py` içinde strateji YARATAN hiçbir uç yok (yalnız mevcut `{strategy_id}` üzerinde işlem yapan uçlar) ve UI'da hiçbir "yeni" butonu yok; tek yol repodaki `scripts/seed_studio.py`'yi elle koşturmak. Menüdeki 9 öğeden biri ilk kullanıcı için tanımsız süreyle erişilemez ve dairesel bir talimat veriyor. _(web/templates/base.html:50-57, web/static/app.css:124, web/routes/strategy_studio.py)_
- **[DÜŞÜK]** README'deki sayfa haritası güncel değil — tarif edilen /strategy ve /backtest sayfaları artık menüde yok — README "Çalıştırma" bölümü (satır 64-68) kullanıcıya `/` , `/agent`, `/strategy`, `/backtest`, `/data` sayfalarını tarif ediyor ve "Strategy Composer" bölümünde "**`/strategy`** sayfası şu adımlarla çalışır: 02 · Add Signal Block … 04 · Save to catalog" diyor. Gerçekte `web/routes/strategy.py:326` ve `web/routes/backtest.py:526` artık 307 ile `/studio`'ya yönleniyor, sol menüde bu iki ad hiç geçmiyor ve akış SIMPLE/PRO/AUTO mod anahtarının arkasında (PRO > Compose sekmesi). README'yi takip eden yeni kullanıcı `/strategy` yazınca kendini üç modlu, varsayılanı SIMPLE sihirbazı olan bambaşka bir ekranda buluyor ve tarif edilen numaralı adımları göremiyor. README ayrıca menüdeki Reports, Session Logs, Strategy Lab, Strategy Builder sayfalarından hiç bahsetmiyor. _(README.md:64-68, 91-100)_
- **[DÜŞÜK]** Aynı BTCUSDT için üç modda üç ayrı varsayılan Bybit kategorisi — AUTO kokpitinin brief'inde CATEGORY varsayılanı `spot` (`studio.html:188-191` — üstelik listede `linear` önce yazılı ama `spot` seçili), PRO Backtest formunda `linear` (`backtest_body.html:97-100`), SIMPLE sihirbazında ise kategori seçimi hiç yok, kod `linear` sabitliyor. Sunucunun açılışta önden ısıttığı seri de `linear` (`server.py:190`). Senaryo: kullanıcı SIMPLE'da BTCUSDT/1h koşar, sonra aynı fikri AUTO'da denemek ister ve START'a basar — farkında olmadan farklı bir enstrüman (spot vs perpetual) üzerinde koşmuş olur, sonuçlar karşılaştırılamaz. Ekranda ikisinin aynı "BTCUSDT" olmadığına dair bir uyarı yok. _(web/templates/studio.html:186-192, web/templates/fragments/backtest_body.html:97-100)_

## Sınır Durumu (Edge Case) Testleri
- **[KRİTİK]** repair_massive_intraday: 1-DAY barında UTC/ET gün sınırı kayması — komşu günün günlük barı siliniyor, onarılan gün ikizleniyor — `_replace_day` (repair_massive_intraday.py:77-94) değiştirilecek pencereyi `start = pd.Timestamp(day, tz="UTC")` … `end = day+1 UTC` olarak, yani UTC gece yarısı sınırlarıyla hesaplıyor. Oysa TF barları `ingest_equities.resample_tf` içinde ET (America/New_York) indeks üzerinde `resample('1D', label='right', closed='right')` ile üretiliyor (ingest_equities.py:251-253): pandas 3.0.3 ile doğruladım — 2024-03-14 seansının (09:31–16:00 ET) günlük barı **2024-03-15 00:00 ET = 2024-03-15 04:00 UTC** damgasını alıyor.

Senaryo: `python repair_massive_intraday.py --ticker QQQ --date 2024-03-14`.
(a) 03-14 seansının ESKİ günlük barı 03-15T04:00Z'de duruyor → `ts >= end` olduğu için `keep` filtresinde KALIYOR; yeni tablo aynı damgayı tekrar eklediği için 1-DAY parquet'inde aynı ts_event'ten İKİ satır oluşuyor.
(b) 03-13 seansının günlük barı 03-14T05:00Z'de, yani [start,end) penceresinin İÇİNDE → siliniyor ve yerine hiçbir şey konmuyor (replacement yalnız 03-14 seansını içeriyor).
Sonuç: bir günü onarmak önceki günün günlük barını yok ediyor ve onarılan günü ikizliyor; üstelik `.bak` yalnız son onarımı geri alıyor. 1-MINUTE/5/15/60/4-HOUR TF'leri etkilenmiyor (etiketleri aynı UTC gününde kalıyor), sorun sadece "D" TF'ine özgü — bu yüzden gözden kaçması çok kolay. _(repair_massive_intraday.py:77-94 (+ ingest_equities.py:251-253))_
- **[YÜKSEK]** Bybit varsayılan fiyat hassasiyeti 2 hane — cent altı semboller backtest'te 0.00'a yuvarlanıyor — `_BYBIT_SPECS` yalnız BTCUSDT/ETHUSDT/SOLUSDT/XRPUSDT içeriyor; listede olmayan HER sembol `_BYBIT_DEFAULT_SPEC = (2, 3, "0.01")` alıyor (backtest.py:204-241). `_bars_from_df` de her barı `Price(float(o[i]), pp)` ile kuruyor (backtest.py:422-425). Nautilus ile doğruladım: `Price(0.000009, 2)` → `0.00`.

Senaryo: kullanıcı Data/Lab ekranından PEPEUSDT, SHIBUSDT, FLOKIUSDT gibi cent altı bir sembol seçiyor (`list_bybit_instruments` tüm işlem gören evreni sunuyor ve `load_bybit_bars` herhangi bir canlı sembolü çekiyor). Tüm OHLC 0.00 oluyor; `_compute_qty` `price <= 0` dalına düşüp sabit lot'a geri dönüyor, `_max_dd_from_series`/pnl anlamsız çıkıyor ve koşum HATA VERMEDEN "geçerli" bir sonuç raporluyor.

Bunun teorik olmadığının kanıtı kod tabanının kendisinde: chart_indicators.py:113-115 yorumu "düşük fiyatlı enstrümanlar (SHIB/PEPE ~1e-5..1e-8) için round(v,4) çizgiyi 0.0'a düzlüyordu" diyor — grafik tarafı düzeltilmiş, motor tarafı düzeltilmemiş. Sembol için hassasiyet bilinmiyorsa varsayılana düşmek yerine reddetmek ya da fiyat büyüklüğünden hassasiyet türetmek gerekir. _(backtest.py:204-241, 422-425)_
- **[ORTA]** WFO amaç fonksiyonunda güven sönümlemesi negatif skorlarda ters çalışıyor — az işlemli kaybeden aday kazanıyor — `objective_value` skoru `val *= n / (n + WFO_TRADE_CONF_K)` ile sönümlüyor (wfo_optimizer.py:242, 284); K=20. Amaç, az işlemle şişmiş skorları bastırmak. Ama çarpan 0<conf<1 olduğu için NEGATİF skorlarda etki tersine dönüyor: değeri sıfıra YAKLAŞTIRIYOR, yani iyileştiriyor.

Somut örnek: iki aday da fold başına sharpe = -1.0 üretiyor. 5 işlemli aday → -1.0 × 0.20 = **-0.20**. 200 işlemli aday → -1.0 × 0.909 = **-0.909**. GA turnuvası (`_tournament_idx`, `ga_next_population` elit seçimi) yüksek skoru seçtiği için 5 işlemli, istatistiksel olarak anlamsız adayı seçiyor. Bu durum erken jenerasyonlarda ve düşen piyasa dilimlerinde (fold objelerinin çoğu negatif) kural değil istisna değil, tipik. Sönümleme yalnız `val > 0` iken uygulanmalı ya da negatiflerde `val / conf` biçiminde cezalandırmalı. _(wfo_optimizer.py:226-284)_

## Entegrasyon Testleri
- **[YÜKSEK]** Restart sonrası 'running' kalan studio_runs / optimize_runs / ai_loops satırları hiç uzlaştırılmıyor — `_reconcile_deployments()` (web/routes/strategy_studio.py:1602-1632) yalnızca `deployments` tablosunu uzlaştırıyor; `studio_runs`, `optimize_runs` ve `ai_loops` için karşılığı yok (strategy_studio/store.py'de `live_deployments()` var ama `live_runs`/`live_loops` yok). Bu üç iş de Starlette `BackgroundTasks` ile çalışıyor (satır 877, 1135, 1461), yani sunucu restart'ı / pm2 restart'ı / process crash'i satırı `running` durumunda bırakıyor ve onu bitirecek kimse kalmıyor. Somut sonuçlar: (1) `route_loop_start` satır 1449'daki `if active and active['status'] == 'running'` kontrolü o strateji için KALICI olarak 422 'a loop is already running' döndürür — DB elle düzeltilmeden AI loop bir daha başlatılamaz; (2) `_footer_metrics.html:3-4` `run.status == 'running'` iken `hx-trigger="every 2s"` ile sonsuza kadar `/studio/{id}/runs/latest` poll eder (her açık sekmeden); (3) optimizer paneli aynı şekilde. Deployment tarafı için bu tam olarak düşünülüp çözülmüş (`reconcile_orphans` + docstring'inde 'yeşil rozet yalan söyler' gerekçesi), ama aynı crash-only mantığı kardeş üç tabloya uygulanmamış. _(web/routes/strategy_studio.py:1602-1632, 1449; strategy_studio/store.py)_
- **[YÜKSEK]** ANTHROPIC_BASE_URL varsayılanı çalışmayan yerel proxy'ye (localhost:6655) işaret ediyor ve README ile çelişiyor — `llm_dispatch._build_client()` satır 452: `proxy_url = os.environ.get("ANTHROPIC_BASE_URL", "http://localhost:6655")` ve istemci `Anthropic(base_url=proxy_url, api_key=proxy_key)` olarak kuruluyor. Yani kullanıcı yalnızca `ANTHROPIC_API_KEY` set ederse (README.md:26'nın 'anthropic SDK ile doğrudan (ya da ANTHROPIC_BASE_URL proxy'si üzerinden)' ifadesinin doğrudan-dediği durum) tüm LLM çağrıları api.anthropic.com'a değil, büyük olasılıkla ayakta olmayan yerel bir Hyperspace proxy'sine gider ve her çağrı ConnectionError ile düşer. Hata da 'kredi tükendi' değil jenerik bir bağlantı hatası olduğu için `_is_credit_exhausted` tetiklenmez, çağıranların graceful fallback'i devreye girer ve koşu 'Random ... (Claude unavailable)' kompozisyonlarıyla normal görünerek devam eder. Aynı `ANTHROPIC_API_KEY` değeri strategy_studio/ai.py:84 tarafından ise gerçek api.anthropic.com'a x-api-key olarak gönderiliyor — bir proxy anahtarı üçüncü taraf endpoint'e gider. İki LLM entegrasyonu aynı env değişkenini okuyup farklı endpoint'lere yolluyor. _(llm_dispatch.py:445-454; strategy_studio/ai.py:84,126; README.md:26)_
- **[YÜKSEK]** load_bybit_bars'ın exclusive cache lock'u okuma yolunu da kilitliyor; 120 s timeout gerçek tutma süresinden (~10 dk) kısa — `load_bybit_bars` satır 764'te koşulsuz olarak `with _cache_lock(cache_path.with_suffix('.lock'))` bloğuna giriyor — cache tamamen yeterli olsa, hiç ağa çıkılmayacak olsa bile. Kilit blok içinde `_fetch_segment` / `_bybit_gap_frames` ile ağ I/O yapılıyor (satır 788-806). `_cache_lock` varsayılan `timeout=120.0` (data.py:138) ama fonksiyonun kendi docstring'i 'long backfills (3 years of 1m hold the lock ~10 min)' diyor (satır 144-147, `stale_after` gerekçesi). Yani /data'dan tetiklenen (`refresh_row` → `asyncio.to_thread`, web/routes/data.py:158) uzun bir backfill sırasında aynı (category, symbol, interval) serisini isteyen HER çağıran — loop_runner'ın iterasyon başı `load_bybit_bars` refresh'i (loop_runner.py:117), /backtest, /lab, studio adapter'ın `_default_loader`'ı, parallel_exec worker process'leri — önce 120 saniye bir thread'i bloke eder, sonra TimeoutError alır. Salt-okuma için lock'suz hızlı yol yok. _(data.py:764, 138-190; web/routes/data.py:144-170)_
- **[ORTA]** load_index_bars ve load_external_bars read-modify-write yaparken hiç lock almıyor; _atomic_to_parquet'in temp adı yalnızca PID'e dayalı — `_atomic_to_parquet` (data.py:193-204) temp dosyayı `f"{path.name}.tmp-{os.getpid()}"` olarak adlandırıyor — aynı süreçteki iki THREAD için bu ad aynıdır. Bybit yolu bunu `_cache_lock` altında çağırdığı için güvenli (satır 818), ama iki çağrı korumasız: (a) `load_index_bars` satır 497 — üstelik burada tam bir read-modify-write var (satır 431 `read_parquet` → satır 492 `concat` → satır 497 yazma), iki eşzamanlı /data/refresh/index ya da /backtest/sweep aynı ticker için birbirinin taradığı günleri kaybettirir; (b) `load_external_bars` satır 2079. `load_external_bars` gerçekten eşzamanlı çağrılabiliyor: studio `NautilusBacktestAdapter.load_bars` (strategy_studio/backtest.py:867) loader'ı bilinçli olarak kilit DIŞINDA çağırıyor, ayrıca web/routes/lab.py:161 ve web/routes/agent_backtest.py:2317 kendi thread'lerinden çağırıyor. Karşılaştırma noktası: `custom_block_store._write_registry` (satır 241-254) tam da bu nedenle benzersiz sibling temp adı kullanıyor ve gerekçesini yorumda yazıyor. _(data.py:193-204, 497, 2079)_
- **[ORTA]** STUDIO_RUNNER ayarlı değilken deployment 'running' olarak işaretleniyor ve arayüzde hiçbir uyarı yok — `RUNNER` varsayılan olarak None (web/routes/strategy_studio.py:169-173, `STUDIO_RUNNER=paper` değilse). `_runner_pickup` satır 1595-1597: `if RUNNER is None: time.sleep(1.0); store.set_deployment_status(deploy_id, 'running'); return` — yani hiçbir TradingNode kurulmadan satır RUNNING olur. `_deployments.html` bu durumu ayırt etmiyor; yeşil `st-running` rozetini ve Pause/Stop butonlarını gösteriyor, `route_deployment_action` (1672) RUNNER None iken sadece DB durumunu çevirir. Aynı projede backtest motoru için bu dürüstlük sorunu bilinçli olarak çözülmüş: `engine_is_stub` (satır 298) ve `_footer_metrics.html:6` / `_results_pane.html:2`'deki '⚠ SİMÜLE — gerçek backtest değil' rozeti. Runner için karşılığı yok — kullanıcı hiç çalışmayan bir 'paper deployment'ı canlı sanır. _(web/routes/strategy_studio.py:169-173, 1586-1599; web/templates/studio/_deployments.html)_
- **[ORTA]** Equity kataloğu import anında bağlanıyor ve manifest/instrument meta önbelleği hiç geçersizleşmiyor — `data.py:104`: `if EQUITY_CATALOG_DIR.exists() and ... : EXTERNAL_CATALOGS.append(...)` — modül import zamanında bir kez. `ingest_equities.py` / `download_massive.py` ayrı bir CLI süreci olarak bu kökü ilk kez oluşturursa, çalışan web sunucusu onu asla görmez; restart şart. Daha ince olan ikinci kısım: `_external_instrument_meta` (satır 1646-1680) ve `_external_manifest` (satır 1683-1703) sonuçlarını süresiz olarak `_EXT_INSTRUMENT_META` / `_EXT_MANIFEST` dict'lerinde tutuyor; gerekçe docstring'de 'the external catalog is read-only reference data, so it never changes' — bu varsayım NAU_ev kökü için doğru olabilir ama projenin KENDİ ingest'inin yazdığı EQUITY_CATALOG_DIR için yanlış. Buna karşılık `_external_catalog_rows` ve `list_external_instruments` her çağrıda dizini yeniden tarıyor. Sonuç tutarsız bir durum: yeni ingest edilen ticker panelde/picker'da GÖRÜNÜR, ama `split_adjusted` / `dividend_adjusted` / `coverage_gaps` (satır 1968-1977) bayat/boş manifest'ten okunur — yani UNADJUSTED uyarısı ve kapsama boşluğu rozeti, tam da yeni gelen veri için susar. _(data.py:104, 1646-1680, 1683-1703)_

## Statik Analiz & Lint
- **[YÜKSEK]** CI'daki `ruff format --check .` adımı kırık — biçimlendirilmemiş test dosyası — Projede yapılandırılmış lint aracı ruff 0.15.21 (pyproject.toml `[tool.ruff]`). `ruff check .` temiz geçiyor (exit 0), ANCAK CI'ın ikinci adımı olan `ruff format --check .` başarısız: `python -m ruff format --check tests/` → exit 1, "Would reformat: tests\test_auto_degradation_honesty.py". Fark 146. satırda gereksiz parantezli satır kırma ve 200. satır civarında boş satır normalizasyonu (28 ekleme / 30 silme, kod kaybı yok). Dosya şu an untracked (`?? tests/test_auto_degradation_honesty.py`); commit edildiği anda .github/workflows/ci.yml'deki `uv run ruff format --check .` adımı kırmızıya döner ve pipeline'ın kalan adımları (compileall, pytest, e2e) hiç çalışmaz. Düzeltme tek komut: `python -m ruff format tests/test_auto_degradation_honesty.py`. _(tests/test_auto_degradation_honesty.py:146)_

## Sessiz Hata & Gözlemlenebilirlik
- **[YÜKSEK]** Manuel robustness suite'inin `full_error` alanı hiçbir yerde okunmuyor — gerçek backtest hatası "trade yok" olarak gösteriliyor — `sandbox.py:534-544` içindeki `_manual_suite_child`, sonuç sözlüğüne `"full_error": full_result.error` koyuyor ve `sandbox.py:453` docstring'i bunu sözleşmenin parçası olarak ilan ediyor. Ancak `web/routes/robustness.py:231-248` sonucu kurarken bu anahtarı HİÇ almıyor; projede `full_error` üzerinde tek bir okuma yok (grep: sadece sandbox.py:453 ve 542). Senaryo: Monte Carlo için koşulan tam backtest (`sandbox.py:512-520`) hata verirse `mc_result` `{"error": "No trade data."}` olarak kalıyor (`sandbox.py:521`) ve `robustness.py:227`'deki `if suite.get("error") and "wfo_windows" not in suite` koşulu da tetiklenmiyor (çünkü `wfo_windows` var). Kullanıcı WFO ve IS/OOS tablolarını dolu, Monte Carlo'yu "trade verisi yok" olarak görüyor — oysa backtest çökmüş. Hem hata mesajı hem sunucu log satırı kayboluyor; sonuç "kısmen degrade" olduğu halde başarılı bir koşu gibi sunuluyor. _(web/routes/robustness.py:227-248 ve sandbox.py:521-544)_
- **[YÜKSEK]** `load_catalog` bozuk strateji kayıtlarını sessizce düşürüyor — `n_broken` sayılıp hiç loglanmıyor — `composer.py:668-689`: katalogdaki her kayıt için `except Exception: n_broken += 1` (673-674) ve validate hook'u patlarsa yine `n_broken += 1` (682-683). `n_broken` yalnızca 686. satırdaki yeniden-yazma korumasında kullanılıyor; hiçbir yerde loglanmıyor, çağırana bildirilmiyor, UI'a taşınmıyor. Senaryo: catalog.json'daki bir kayıt bilinmeyen alan/şema kayması yüzünden `from_dict`'te patlarsa, o strateji /strategy ve /reports listelerinden sessizce yok oluyor; kullanıcı 30 stratejiden 28'ini görüyor ve bunu fark edecek tek bir iz bile yok. Asimetri belirgin: hemen üstteki custom-block registry okuma hatası (657-663) düzgün `logging.warning` basıyor, bozuk-kayıt yolu basmıyor. `load_catalog` ~18 çağrı noktasında (ajan iç döngüsü dahil) kullanıldığı için sessiz kayıp her yüzeye yayılıyor. _(composer.py:665-689)_
- **[ORTA]** Strategy Lab: backtest log yazımı hatası tamamen yutuluyor, koşu başarılı görünüyor — `web/routes/lab.py:335-345` içinde `result.log_ts = _log_backtest(...)` çağrısı `except Exception: pass` ile sarılı — ne log, ne progress adımı, ne kullanıcıya uyarı. Senaryo: disk dolu / dosya kilidi / izin hatası durumunda Lab paneli `✓ N trade · PnL +X USDT` diyerek yeşil biter, ama koşu /reports'a hiç düşmez ve tear sheet linki (`result.log_ts`) boş kalır — kullanıcı sonucun kaydedildiğini sanır. Aynı `_log_backtest` çağrısı diğer iki yerde bilinçli olarak gözlemlenebilir kılınmış: `web/routes/backtest.py:1058-1066` `logging.warning(..., exc_info=True)` basıyor (DeepR 2026-08-08 notuyla birlikte, tam da bu sebeple), `web/routes/agent_backtest.py:2020-2021` ise `_add_step(run_id, "⚠ Could not write log: ...")` yazıyor. lab.py bu düzeltmeden geride kalmış tek kopya. _(web/routes/lab.py:335-345)_
- **[ORTA]** Komisyon ayrıştırmasında kalem bazlı sessiz sıfırlama — P&L maliyeti olduğundan düşük raporlanıyor — `backtest.py:690-715`: dıştaki `except` bir DeepR düzeltmesiyle artık `logging.warning` basıyor, ancak içteki iki handler hâlâ sessiz. `_parse_commissions` listedeki her kalem için `except Exception: pass` (696-699) yapıyor — 5 komisyon kaleminden 2'si ayrıştırılamazsa toplam sessizce eksik çıkıyor; skaler yolda ise `except Exception: return 0.0` (701-704) komisyonu doğrudan sıfır sayıyor. Kritik nokta: bu iç handler'lar hatayı zaten yuttuğu için dıştaki loglayan handler HİÇ tetiklenmiyor. Sonuç: `commission_total` (ve dolayısıyla net PnL dökümü) olduğundan iyi görünür, hiçbir uyarı üretilmez. Bu, aynı fonksiyonun 709-715 ve 746-752 satırlarında "$0 slippage 'kayıp yok' diye okunuyordu" gerekçesiyle düzeltilen hatanın birebir aynı sınıfı. _(backtest.py:690-715)_
- **[ORTA]** Fills raporu alınamazsa trade çıkış sebepleri sessizce kayboluyor — `backtest.py:1595-1598`: `engine.trader.generate_order_fills_report()` patlarsa `reason_fills_df = None` atanıyor, tek satır log yok. Bu frame `_extract_trades`'e `fills_df` olarak gidiyor ve orada `_fills_lookup` (`backtest.py:1001-1021`, kendisi de `except Exception: pass` içeriyor) boş sözlük döndürüyor. Sonuç: her trade'in `exit_kind` (`sl`/`tp`) ve `entry_reason`/`exit_reason` alanları boş kalıyor; tear sheet'te "bu trade neden kapandı" sütunu tüm satırlarda boş görünür — sanki strateji hiç SL/TP kullanmamış gibi. Metrikler ve grafik normal olduğu için bozulma fark edilmez. Karşılaştırma: aynı fonksiyonda ayrıştırılamayan pozisyon satırları için bilinçli bir sayaç + `logging.warning` var (`backtest.py:1142-1150`); bu yol o standarda uymuyor. _(backtest.py:1595-1598 ve backtest.py:1012-1020)_
- **[ORTA]** `_external_instrument_meta`: geçici okuma hatası kalıcı olarak boş cache'leniyor — `data.py:1657-1680`: `ParquetDataCatalog(...).instruments()` çağrısı patlarsa `meta = {}` (1676-1677, log yok) ve bu boş sözlük fonksiyondan çıkmadan `_EXT_INSTRUMENT_META[key] = meta` ile modül seviyesindeki cache'e yazılıyor (1679). Docstring cache'i "read-only reference data, so it never changes" diye gerekçelendiriyor — ama BAŞARISIZLIK da aynı kalıcılıkla cache'leniyor. Senaryo: sunucu açılışında dosya kilidi/geçici I/O hatası yaşanırsa, süreç ömrü boyunca bir daha denenmiyor; dış katalog satırları `asset_class`, `price_precision`, `quote_currency` alanları olmadan ("—") görünüyor ve bu, "katalogda bu bilgi yok" ile ayırt edilemiyor. İç döngüdeki `except Exception: continue` (1674-1675) de tek tek enstrümanları sessizce atlıyor. _(data.py:1657-1680)_
- **[ORTA]** Blok önizlemesi: evaluate() çökmesi "sinyal üretmiyor" olarak gösteriliyor — `web/routes/strategy.py:300-318`: bar döngüsünde `except Exception: sig = None` (307-310) ve tüm önizleme fonksiyonu için `except Exception: return {}` (317-318). İkisinde de log yok. Senaryo: kullanıcının (ya da LLM'in) yazdığı custom block'un `evaluate()`'i bir param/veri kombinasyonunda istisna atıyorsa, önizleme grafiği düz bir "hiç sinyal yok" çizgisi gösteriyor. Kullanıcı bloğun mantığını yanlış sanıp değiştirmeye çalışıyor; gerçek sebep (TypeError/IndexError) hiçbir yerde görünmüyor. Aynı sorunun ikinci kopyası plan önizlemesinde: `web/routes/backtest.py:2067-2072` `_preview_signals` hatasında sessizce `chart_data = None` yapıyor. _(web/routes/strategy.py:300-318)_
- **[ORTA]** Grafik gösterge hesabı hatada sessizce boş dönüyor — `web/routes/chart.py:176-185`: `indicators_for_spec(spec, times, closes)` bir istisna atarsa `except Exception: pass` ile geçiliyor ve `indicators` başlangıç değeri olan `{"overlays": [], "panes": []}` payload'a konuluyor (187). Yorum "indicator computation must not block the chart" diyor ama tek satır log bile yok. Senaryo: bir spec'in gösterge hesabı patlarsa (ör. bilinmeyen blok tipi, NaN dizisi), grafik mumları çizer, gösterge katmanı hiç görünmez — kullanıcı "bu stratejide gösterge yok" sonucuna varır. Dıştaki genel handler (`chart.py:193-199`) log basıyor, iç handler basmıyor. _(web/routes/chart.py:176-185)_

## Uçtan Uca (E2E) Testler
- **[YÜKSEK]** Kritik entegrasyon/E2E testleri ortama bağlı olduğu için varsayılan koşumda ve CI'da hiç çalışmıyor (sessiz skip) — Projedeki tek gerçek uçtan uca test — tests/test_backtest_run_progress_result_e2e.py (POST /backtest/run → progress poll → sonuç render) — pratikte hiçbir zaman koşmuyor. İki katmanlı engel: (1) `pyproject.toml`'daki `addopts = -m "not e2e"` yüzünden varsayılan koşumda dışlanıyor; (2) `-m e2e` ile açıkça çalıştırılsa bile `spec_id` fixture'ı (satır 41-48) `composer.load_catalog()` boşsa `pytest.skip` ediyor — katalog dosyası `Path.home()/.cache/nautilus_web_app/strategy_catalog.json`, yani repo dışında, ve temiz bir CI runner'da (ci.yml: windows-latest + actions/checkout) yok; ondan önce koşan `pytest -q tests` adımı da kataloğu gerçekten yazmıyor (testler `append_to_catalog`'u mock'luyor veya `CATALOG_FILE`'ı tmp_path'e monkeypatch ediyor). Sonuç: e2e adımı 1 test toplar, skip eder, pytest 0 ile çıkar, ci.yml'deki 3 denemeli retry `$LASTEXITCODE -eq 0` görüp `exit 0` yapar — 'artık bir kırılma Actions'ta görünür' denen zincir aslında hiç sorulmuyor; yeşil, çünkü hiç çalışmıyor. Aynı desen ikinci bir kritik yolda daha var: tests/test_sandbox.py:88 `TestExternalRecipe` sınıfı `skipif(not _external_catalog_available())` ile korunuyor; makinede QQQ.NASDAQ dış kataloğu yoksa Equity enstrüman kurulumu, `size_precision == 0` ve `QQQ.NASDAQ-1-DAY-LAST-EXTERNAL` bar-type üretimi hiç test edilmiyor ve skip sessiz olduğu için kimse fark etmiyor. Her iki durumda da eksik olan aynı şey: ortamdan bağımsız fixture (repo içi mini katalog / seed spec) ya da CI'a bir seed adımı. _(tests/test_backtest_run_progress_result_e2e.py:41-48 ; .github/workflows/ci.yml)_ _(boyutlar: Uçtan Uca (E2E) Testler, Birim (Unit) Testleri)_
- **[YÜKSEK]** /data sayfasında "veri çek" hataları hiç görünmüyor — E2E akışının ilk adımı sessizce başarısız — E2E akışının ilk adımı /data'dan bar çekmek. web/templates/fragments/data/instrument_row.html'deki her timeframe hücresi bir form: `hx-post="/data/refresh/bybit"` (veya `/refresh/index`, `/catalog/write`), `hx-target="#{{ rid }}" hx-swap="outerHTML"`. web/routes/data.py bu uçlarda `HTTPException` fırlatıyor: 400 desteklenmeyen symbol/category/interval (157-165), 404 FileNotFoundError (202-203), 500 diğer her şey — ör. Bybit erişilemez, ağ hatası, disk hatası (164-165, 204-205, 269-277). Bu yanıtlar non-2xx olduğu için htmx swap etmez; /data sayfasında `studio.js` (tek `htmx:responseError` dinleyicisi olan dosya) yüklenmiyor ve bu uçlar HX-Toast başlığı da koymuyor. Kullanıcı hücreye tıklar, kısa bir busy state görür, sonra hücre eski "fetch" haline döner — veri gelmediğini ancak sonraki backtest'te "No cached bars for BTCUSDT/linear/60" hatasıyla anlar. tests/test_data_page.py yalnızca render seviyesinde kontrol yapıyor, bu hata yolunu hiç sürmüyor. _(web/routes/data.py:157-165,193-205,265-277 ; web/templates/fragments/data/instrument_row.html)_
- **[ORTA]** nautlab_sid çerezi 1 saatte sertçe düşüyor; uzun oturumda sonuç paneli, taslaklar ve tekil-koşu koruması kayboluyor — web/shared.py:60 `_SESSION_COOKIE_MAX_AGE = 3600`. `session_id(request, response)` çağrıldığında kayan yenileme yapıyor (satır 70-77) ama /studio (web/routes/studio.py:149 + 253-254) ve /lab (web/routes/lab.py:492 + 503-504) çerezi YALNIZCA yoksa yazıyor — yani sayfa yenilense bile süre uzamıyor. Kayan yenileme sadece /strategy/* draft uçlarında var (web/routes/strategy.py:389,413,464,623,731,1201 → `_sid_cookie`). Uçtan uca senaryo: AUTO'nun varsayılan bütçesi 4 saat (web/templates/studio.html:238 `value="4"`), backtest+robustness turları da uzun. Kullanıcı /studio'yu açıp 1 saatten fazla Backtest/AUTO sekmelerinde çalışırsa çerez düşer; bir sonraki istekte yeni bir sid basılır ve (a) `last_result_get(sid)` boş döner → sayfa yenilendiğinde son backtest sonucu ekrandan kaybolur (web/routes/backtest.py:99-107), (b) Compose sekmesindeki taslak block'lar boşalır (web/routes/strategy.py:87-100, `_DRAFTS[sid]`), (c) oturum başına tek-aktif-koşu koruması sıfırlanır (web/routes/backtest.py:178-192) → aynı tarayıcıdan ikinci bir eşzamanlı backtest başlatılabilir, ki 409+toast koruması tam olarak bunu engellemek için yazılmış. _(web/shared.py:60 ; web/routes/studio.py:253-254 ; web/routes/lab.py:503-504)_
- **[ORTA]** Index (US-Index) uçtan uca yolu varsayılan kurulumda ölü ve uyarısız — data.py:71-76'da `INDEX_ROOT` varsayılanı bir macOS yolu: `/Users/i034216/Z/us_indices/values_v1`. Bu Windows/Linux kutuda asla var olmaz ve `NAUTILUS_INDEX_ROOT` ayarlanmadıkça Index kaynağıyla ilgili her adım kırılır: /data'daki "Discover tickers" 404 döner (web/routes/data.py:222-223), /backtest/tickers `<option value=''>ticker discovery failed: ...</option>` basar (web/routes/backtest.py:563-566) ve Index kaynağıyla backtest "No bars for {ticker}" ile biter (backtest.py:969-972). Dikkat çekici olan: aynı dosyada `EXTERNAL_CATALOGS` için modül yüklenirken açık bir uyarı loglanıyor (data.py:107-117, "root does not exist ... panel will be empty") ama INDEX_ROOT için eşdeğer uyarı yok — yani UI'da üç kaynaktan biri sessizce çalışmaz durumda duruyor ve operatör bunu ancak deneyerek anlıyor. _(data.py:71-76)_
- **[ORTA]** Tarayıcı seviyesinde E2E yok: sihirbaz, mod geçişi ve describe→run zinciri tamamen korumasız JS'te yaşıyor — Ürünün ana akışının önemli bir bölümü sadece istemci JS'inde: SIMPLE sihirbazının 5 adımlık gating'i ve `swRun()`/`swReliability()` çağrıları (web/templates/fragments/studio_simple.html:395-455), SIMPLE↔PRO↔AUTO mod geçişinde #result/#robustness-result/#chat-thread singleton düğümlerinin DOM'da taşınması (`swMoveNodes`, web/templates/studio.html:354-374) ve describe bitince backtest'i tetikleyen zincir (`data-bt-chain-url` → `htmx.ajax`, web/templates/fragments/backtest_scripts.html:112-119) ile tek-servis kilidi (`_mark_gen_chained`, web/routes/backtest.py:1143-1158). Bunların hiçbirini otomatik bir test sürmüyor: tests/ altındaki HTTP seviyeli testler (test_studio_ui_fixes.py, test_describe_backtest.py, test_agent_run_form.py) tek tek uçları çağırıyor, tarayıcı yok. Var olan Playwright betikleri debug/_e2e_auto.py, _e2e2.py, _e2e3.py, _e2e4.py — elle çalıştırılan, sabit `http://127.0.0.1:8111`'e bağlanan, çıktısını repo köküne `_e2e.log`/`_auto_shot.png` olarak yazan tek seferlik betikler; pyproject.toml `[tool.ruff] extend-exclude` listesinde "debug" olduğu için lint'ten de CI'dan da tamamen dışarıda. Yani "START → STOP → tekrar START" gibi kullanıcının fiilen şikâyet ettiği (debug/_e2e4.py docstring'i) senaryoların hiçbir kalıcı regresyon koruması yok. _(debug/_e2e_auto.py ; pyproject.toml (extend-exclude: debug))_

## Birim (Unit) Testleri
- **[KRİTİK]** safe_builtins() ve safe_module_proxy() runtime savunma katmanının sıfır testi var — `codegate.py:284 safe_builtins()` ve `codegate.py:234-276 _ReadOnlyModuleProxy/safe_module_proxy()`, docstring'lerinin kendi ifadesiyle "bir codegate kaçağının runtime'da eval/exec/open/getattr'a ulaşamaması" ve "AST'deki attribute-store yasağının tek savunma hattı olmaması" için var. Bu ikisi `agent.py:977-983` ve `composer.py:371`'de üretim exec namespace'ini kuruyor. `grep -rn "safe_builtins\|safe_module_proxy" tests/` → 0 sonuç. Yani `"eval" not in safe_builtins()`, `proxy.pi = 0` → AttributeError, `proxy.system` → AttributeError gibi tek satırlık ve neredeyse bedava testlerin hiçbiri yok. `_ALLOWED_BUILTINS`'e bir isim eklenmesi veya `__setattr__` override'ının kaybolması sessizce geçer. _(codegate.py:234-303)_
- **[YÜKSEK]** Klasik blok evaluator'larının çoğu (özellikle stateful _eval_atr_stop) doğrudan birim testsiz — `block_library_classic.py`'deki `_eval_ma_cross`, `_eval_rsi_threshold`, `_eval_price_breakout`, `_eval_momentum`, `_eval_ema_cross`, `_eval_macd_cross`, `_eval_atr_stop` isimleri tüm test ağacında SADECE `tests/test_composer_public_surface.py`'nin golden-name frozenset'inde geçiyor (doğrulandı: `grep -rln "_eval_ma_cross" tests/` → yalnız o dosya). Yani bu fonksiyonların davranışı değil, yalnızca `composer.<name>` üzerinden erişilebilir OLMASI test ediliyor. En riskli olanı `_eval_atr_stop` (block_library_classic.py:204): `_prev_state[atr_hi_i]/[atr_lo_i]` üzerinden trailing-stop tepe/dip takibi yapıyor, pozisyon yönüne göre dallanıyor, flat iken state'i sıfırlıyor. Bir yön/karşılaştırma hatası (örn. `hi - atr*mult` yerine `hi + atr*mult`) hiçbir birim testi kırmaz — sadece backtest PnL'i sessizce bozulur. Aynı şekilde `ma_cross`/`momentum`'daki edge-trigger semantiği (`prev = _prev_state.get(idx, diff)` ile ilk barda tetiklenmeme garantisi) hiç pinlenmemiş; yalnızca `volume_spike` (tests/test_volume_blocks.py) ve nau blokları (adx/donchian/wavetrend/stoch, tests/test_guards_and_indicators.py:319) gerçek birim testine sahip. _(block_library_classic.py:29-240)_
- **[YÜKSEK]** ComposedStrategySpec.validate()'in pozisyon-boyutlandırma dalları ve build_spec clamp'leri test edilmemiş — `composer_spec.py:150 validate()`'in test edilen kısmı yalnızca isim/entry-blok/atr_period dalları (`tests/test_fixes.py:304-374`) ve custom-blok rol çelişkisi (`tests/test_auto_360_fixes.py:490`). Test EDİLMEYEN dallar: `percent_equity` için `math.isfinite` kontrolü ve `pct > 100` (kaldıraçsız üst sınır) reddi, `atr_target` için `trade_size_atr_risk <= 0`, `fixed_usdt` için `trade_size_usdt <= 0`, `vol_target` için `trade_size_vol_target <= 0` / `trade_size_vol_span < 2` / `trade_size_capital <= 0` üçlüsü. `grep -rn "trade_size_vol_target" tests/` sonucunda bu alanlar yalnızca round-trip serileştirme testinde (test_regression_anchors.py:615-640) geçiyor, hiç doğrulama testi yok. Ayrıca `build_spec` (composer_spec.py:251) — docstring'inin kendisi "eski strategy.py clamp'i vol_target'ı hariç tutuyordu ve vol_target stratejilerini sessizce fixed'e düşürüyordu, burada düzeltildi" diyor — testlerde yalnızca `tests/test_catalog_mutation_and_locking.py:61`'de fixture kurucusu olarak kullanılıyor; whitelist/clamp davranışının (entry_logic, order_type, sl_type, tp_type, trade_size_mode geçersiz değerde varsayılana düşme) tek bir assert'i yok. `_as_bool` (composer_spec.py:240 — HTML form ""/"0"/"false"/"off" → False dönüşümü) hiçbir testte geçmiyor; bu fonksiyondaki bir regresyon işaretlenmemiş checkbox'ları `use_bracket=True`/`allow_short=True` yapar. _(composer_spec.py:150-330)_
- **[YÜKSEK]** wfo_optimizer'ın genetik algoritma çekirdeği tamamen testsiz; docstring'deki determinizm sözleşmesi pinlenmemiş — `wfo_optimizer.py:292-358` — `ga_plan`, `ga_initial_population`, `_tournament_idx`, `ga_next_population` ve `values_to_dict`/`_sample_dim`/`_current_values` fonksiyonlarının hiçbiri testlerde geçmiyor (`grep` ile doğrulandı, 0 referans). Bunlar seed'li bir `rng` verildiğinde tamamen deterministik ve saf; docstring'ler açıkça test edilmesi gereken sözleşmeler yazıyor: "same rng state → same population (determinism contract)", "elite strict-greater kuralıyla seçilir (eşitlikte düşük indeks) — sıralı ve paralel yollar aynı kazananı üretir", `ga_plan` için "GEN = max(1, round(n_samples/POP)), yarımı YUKARI yuvarlar (20/8 → 3)", boş uzayda `(1,1)`. Bu davranışların hiçbiri assert edilmiyor. `_tournament_idx`'teki eşitlik-kırma veya `ga_next_population`'daki elit seçimi bozulursa optimizasyon sonuçları sessizce paralel/sıralı arasında ayrışır — ve `tests/test_parallel_exec.py`'nin paralel-sıralı parite testleri WFO seviyesinde olduğu için bu GA-içi ayrışmayı yakalamayabilir. _(wfo_optimizer.py:292-358)_
- **[ORTA]** Testler geliştiricinin gerçek ~/.cache custom block deposuna bağlı: biri oraya yazıyor, ikisi içeriğine göre FAIL/farklı davranıyor — tests/test_guards_and_indicators.py'deki üç test `custom_block_store.STORE_DIR` / `REGISTRY_FILE`'ı monkeypatch etmeden gerçek `~/.cache/nautilus_web_app/custom_blocks` deposunu kullanıyor. (1) Satır 416 `test_params_mutation_stays_in_copy` `cbs.save_custom('t_isolation_x', ...)` ile geliştiricinin gerçek deposuna `.py` dosyası ve registry kaydı YAZIYOR; temizlik yalnız `finally` içinde ve `except Exception: pass` ile yutuluyor, yani test ortada patlarsa artık dosya kalıcı — kazayla olduğu belli, çünkü test `tmp_path` fixture'ını parametre olarak alıyor ama hiç kullanmıyor. (2) Satır 102 `test_existing_catalog_math_blocks_no_nameerror` ve satır 291 `test_stored_blocks_all_survive_the_ceiling` `cbs.list_custom()` ile aynı gerçek depoyu gezip sonunda `assert checked >= 1` diyor: temiz bir makinede veya CI'da bu testler skip değil FAIL veriyor; dolu bir makinede ise deponun içeriğine göre farklı sayıda ve tamamen keyfi (LLM üretimi) kodu exec ediyorlar — iki geliştiricide aynı testin ne doğruladığı farklı, yani deterministik değiller. Doğru desen projede zaten üç kardeş testte mevcut (tests/test_ohlc_blocks.py:41-42, tests/test_volume_blocks.py:159-160, tests/test_perf_equivalence.py:199-200: `monkeypatch.setattr(cbs, 'STORE_DIR', tmp_path)` + REGISTRY_FILE patch'i) ve tests/conftest.py yalnız `SESSION_LOG_DIR`'i otomatik izole ediyor, blok deposunu değil. Çözüm: birkaç temsilci bloğu repo içi fixture yapmak ve depo yollarını her testte izole etmek. _(tests/test_guards_and_indicators.py:416)_ _(boyutlar: Birim (Unit) Testleri)_
- **[ORTA]** run_multi_symbol'ün genelleme skorlaması ve etiketleme eşikleri testsiz — `backtest_robustness.py:1078-1334` — `run_multi_symbol` kullanıcıya doğrudan bir hüküm gösteriyor: "✓ Generalizable" / "⚠ Limited" / "✗ Symbol specific". Bu hüküm saf ve kolay test edilebilir bir mantıktan çıkıyor: geçerli sonuç filtresi (`not error and n_trades >= 5 and excess_return_fraction is not None`), `pass_rate = n_positive/n_valid`, eşikler `>= 0.7` / `>= 0.4`, `n_valid == 0` → "— (yetersiz veri)", ve `avg_sharpe` yalnızca geçerli sonuçlardan. `grep -rn "run_multi_symbol" tests/` → 0 referans. Kaynak yorumunun kendisi filtrenin bir kez 3'ten 5'e çıkarıldığını (`# 3→5`) not ediyor — yani bu sabitler değişiyor ve hiçbir test onları korumuyor. Eşiklerden veya `n_trades >= 5` filtresinden birinin bozulması, veri eksikliği yüzünden hiç test edilememiş bir stratejiyi "Generalizable" olarak etiketleyebilir. _(backtest_robustness.py:1078-1334)_
- **[ORTA]** agent._coerce_catalog_blocks, _validate_composed'ın clamp mantığının kopyası ama testsiz — `agent.py:2090-2128 _coerce_catalog_blocks`, kendi docstring'inde "_validate_composed'daki (agent.py:725) coercion mantığıyla aynı" diyor: aynı int/float min-max clamp'leri, aynı options whitelist'i, aynı `ma_cross/ema_cross/macd_cross` için `slow > fast` düzeltmesi (`params["fast"], params["slow"] = 10, max(slow, 30)`), aynı `atr_stop → role="exit"` zorlaması. Fakat `_validate_composed` testlerde 9 kez geçerken `_coerce_catalog_blocks` hiç geçmiyor (0 referans). İki kopyadan yalnızca biri test altında olduğu için, sohbetle taslak düzenleme yolunda (chat_edit_blocks) clamp'lerin ayrışması sessiz kalır — LLM'den gelen aralık dışı bir parametre katalog aralığına kırpılmadan spec'e girebilir. _(agent.py:2090-2128)_
- **[DÜŞÜK]** Oturum-başına-tek-koşu guard'ının üç kopyasından yalnızca biri test edilmiş — Aynı "sid → run_id, bitmiş koşuyu temizle" guard'ı üç yerde ayrı ayrı yazılmış: `web/routes/backtest.py:178 _session_active_kind` / `:195 _session_set_active`, `web/routes/lab.py:52 _session_lab_busy` / `:66 _session_lab_set_active`, `web/routes/robustness.py:40 _session_robustness_busy` / `:54 _session_robustness_set_active`. lab.py'nin yorumu duplikasyonu bilinçli bir tercih olarak açıklıyor. Testler yalnızca backtest.py kopyasını kapsıyor (`tests/test_studio_ui_fixes.py:204-268`: aktif koşu tespiti, bitmiş koşunun temizlenmesi, bilinmeyen run_id). lab.py ve robustness.py kopyaları için tek bir test yok — bu ikisinde bir hata, kullanıcının aynı oturumda paralel LAB/robustness koşusu başlatabilmesine (veya tam tersi, bitmiş bir koşu yüzünden kalıcı olarak kilitlenmesine) yol açar ve suite yeşil kalır. _(web/routes/lab.py:52-68, web/routes/robustness.py:40-57)_

## 🔴 En Kritik 3 Madde
- **[KRİTİK]** codegate: `**=` ve `<<=` (AugAssign) üs/kaydırma denetimlerini atlıyor — sunucu süreci kilitlenebilir _(codegate.py:486-494 (validate_generated_code döngüsü); istismar yolları web/routes/strategy.py:246, 893)_
- **[KRİTİK]** repair_massive_intraday: 1-DAY barında UTC/ET gün sınırı kayması — komşu günün günlük barı siliniyor, onarılan gün ikizleniyor _(repair_massive_intraday.py:77-94 (+ ingest_equities.py:251-253))_
- **[KRİTİK]** safe_builtins() ve safe_module_proxy() runtime savunma katmanının sıfır testi var _(codegate.py:234-303)_
