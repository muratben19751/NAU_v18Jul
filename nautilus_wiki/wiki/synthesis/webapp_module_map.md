---
title: nautilus_web_app Modül Haritası
type: synthesis
sources:
  - https://github.com/nautechsystems/nautilus_trader
  - sources/02_architecture_docs.md
last_updated: 2026-08-08
summary: nautilus_web_app'in her Python modülünün Nautilus wiki karşılığını gösteren canlı harita; kod ↔ doküman geri-beslemesini formalize eder. Birleşik /studio (SIMPLE wizard + PRO + AUTO Mission Control kokpiti), ana kabuğa gömülen Strategy Builder, salt-okunur canvas görünümü (graph.py) ve mission.py render modeli dahil.
key_concepts:
  - nautilus_kernel
  - data_engine
  - strategy_and_actor
  - order_flow_pipeline
  - v1_to_v2_migration_lessons
  - crash_only_design
  - environment_contexts
  - backtesting_guide
  - strategy_studio
  - auto_mission_control
related:
  - wiki/synthesis/v1_to_v2_migration_lessons.md
  - wiki/synthesis/backtesting_guide.md
  - wiki/synthesis/index_backtest_via_equity_proxy.md
  - wiki/synthesis/auto_mission_control.md
---

# nautilus_web_app Modül Haritası

Bu sayfa `nautilus_web_app` repo kodunun Nautilus wiki karşılıklarını **tek yerde** listeler. Amaç: Karpathy-tarzı LLM knowledge base pattern'inin son bacağını kapatmak — kod → wiki → kod. Uygulama artık her modülünün en üstünde bir `Wiki References` bloğu taşır; bu sayfa da ters yönden aynı köprüyü sunar.

## Genel şema

Uygulama Nautilus'un **düşük seviyeli** yolunu kullanır ([[backtesting_guide]] "BacktestEngine seç eğer" satırlarına uyar). [[backtest_node]] + [[parquet_data_catalog]] yüksek seviyeli yoluna yakınsayacak biçimde yapılandırılmıştır, ancak hâlâ tek-instrument + in-process.

## Modül karşılıkları

### Web katmanı (FastAPI)

| Modül | Rol | Wiki karşılığı |
|---|---|---|
| `server.py` | Lifespan + router mount. **Tek-operatör paylaşılan-secret auth (2026-08-08)**: `NAU_ACCESS_TOKEN` env değişkeni set edilmişse `_require_auth` middleware her isteği `nau_auth` cookie'sine (token'ın sha256'sı) karşı kontrol eder; `/login` GET/POST açık kalır, yanlış/eksik token → redirect. Uygulama daha önce internete tünellenip (bkz. `serve.py` satırı — Cloudflare tüneli) **sıfır auth**'la duruyordu; DeepR review'unda en kritik bulgu buydu. Aynı geçişte `templates.env.filters["nl2br"]` eklendi (chat balonlarındaki `\|e\|replace('\n','<br>')\|safe` zinciri Jinja `Markup.replace()`'in kendi argümanını da escape etmesi yüzünden XSS'e açıktı — bkz. [[nau_deepr_toplu_sertlestirme_2026_08]]). **Aynı DeepR'ın ikinci turunda (2026-08-08) `/login` brute-force koruması eklendi**: `_login_rate_limited`/`_record_login_failure` — IP başına 60 sn'lik pencerede 5 başarısız denemeden sonra `POST /login` 429 döner (kilit korumalı bellek-içi sayaç, tek-operatör/tek-process için harici bağımlılık gerekmiyor); her başarısız/rate-limitli deneme artık `_log.warning` ile loglanıyor (öncesinde hiç loglanmıyordu). **Auth çerezi artık `secure=True` ve `/logout` var**: `nau_auth` daha önce `secure` bayrağı olmadan 30 gün geçerli set ediliyordu ve iptal etmenin tek yolu `NAU_ACCESS_TOKEN`'ı değiştirmekti; `POST /logout` çerezi temizleyip `/login`'e yönlendiriyor, sidebar'da `auth_enabled()` template global'i true iken görünen bir Logout düğmesi var (auth kapalıyken gösterilmiyor). Çerez statik (sha256(token)) olduğundan logout yalnız BU tarayıcıyı çıkışa zorluyor — aynı çerezi tutan başka bir tarayıcıyı geçersiz kılmıyor, onun için token rotasyonu şart. **`_limit_request_body` global gövde boyutu sınırı**: hiçbir route istek gövdesini sınırlamıyordu (`web/routes/reports.py` `post_layout` en belirgin örnek — `await request.json()` sınırsız kabul ediyordu); tek bir ASGI middleware'i tüm route'ları kapsıyor — hem `Content-Length` başlığı çok büyükse route hiç çalışmadan 413 döner, HEM `receive()` sarmalanarak gerçek okunan byte sayısı da sayılır (chunked/yalan Content-Length'e karşı). İkinci katman şart: birçok route (`post_layout` dahil) `await request.json()`'ı geniş `except Exception` ile sarıyor, receive() içindeki 413'ü kendi jenerik 400'üne çevirirdi — middleware `call_next` sonrası `exceeded` bayrağını tekrar kontrol edip route'un cevabını 413 ile eziyor. Varsayılan 2 MiB, `NAU_MAX_REQUEST_BODY_BYTES` ile ayarlanabilir. **Tüm auth gate'i artık test edilir (2026-08-08)**: `_require_auth`'ın HTMX-vs-tarayıcı yönlendirme ayrımı, `_is_authenticated`/`_auth_cookie_value`, login/logout, rate-limit, secure çerez — hiçbiri test edilmiyordu; bkz. `tests/test_require_auth_middleware.py`, `tests/test_login_rate_limit.py`, `tests/test_auth_cookie_logout.py`. | [[nautilus_kernel]] (küçük ölçekte "compose then run"), [[event_driven_architecture]], [[nau_deepr_toplu_sertlestirme_2026_08]] |
| `serve.py` | **Süreç sarmalayıcısı — uygulamayı KURMAZ, yalnızca başlatır (2026-07-27'de git'e alındı)**. PM2'nin beklediği "tek .py giriş noktası" desenini karşılar (`interpreter: python`, `fork_mode`; kardeş proje `quant/serve.py` ile aynı kalıp). `--host` (varsayılan `127.0.0.1`) + `--port` (varsayılan `8111`) alır ve `uvicorn.run("server:app", reload=False)` çağırır. **`reload=False` bilinçlidir**: strateji üretimi bellekte durum tutan bir worker thread'de koşar, reload onu öldürür. **Yalnızca loopback'e bağlanır** — dışarı açılış Cloudflare tüneli üzerinden (`nautilus.muratben.com` → `http://127.0.0.1:8111`), böylece LAN'daki başka bir cihaz doğrudan erişemez. Tünel ingress'i `localhost` değil `127.0.0.1` yazmalıdır: uvicorn yalnız IPv4 dinler, `localhost` önce `::1`'e çözülüp bağlantı reddedilir | [[nautilus_kernel]] ("compose then run"un dış halkası: kurulum `server.py`'de, koşum burada) |
| `web/routes/data.py` | GET /data — instrument catalog. **GET /data/range (2026-08-01)**: tek serinin önbellek/katalog kapsamı `{start,end}` — tüm backtest yüzeylerindeki tarih çiftlerinin **MAX** butonunun arka ucu (`data.coverage_range`: bybit/index parquet FOOTER stats, external katalog DOSYA ADLARI; hiç bar decode edilmez; seri yoksa 404 → buton "veri yok" gösterir) | [[parquet_data_catalog]], [[bar_aggregation_and_type_syntax]], [[index_backtest_via_equity_proxy]], [[precision_modes]] |
| `web/routes/studio.py` | **GET /studio — Composer + Backtest'i tek sekmeli sayfada birleştiren giriş noktası (2026-07-21)**. İki sayfanın context'inin UNION'ını `studio.html`'e render eder (üst-seviye sekmeler `[Compose][Backtest][Analyze]`, AYRI `.lab-tab`/`labTab()` namespace — backtest'in iç `.bt-tab`/`btTab()` mekanizmasına dokunmaz). Tüm HTMX eylemleri değişmeden mevcut `/strategy/*` + `/backtest/*` endpoint'lerine gider. İki halin tek kalıcı bağı `strategy_catalog.json` (anahtar `spec_id`): Backtest sekmesindeki katalog picker (`<select name="spec_id">` + catalog satırı "▶ Bununla backtest") **Composer→Backtest devrini nihayet canlandırır** — eski `preferred_spec_id` deep-link'i hesaplanıyor ama render edilmiyordu (ölü devir). Sayfa gövdeleri partial'lerde paylaşılır (`compose_body.html`, `backtest_body.html`, `backtest_scripts.html`) — hem eski route'lar (artık redirect) hem studio include eder. **Mode switch SIMPLE ↔ PRO ↔ AUTO (2026-07-22)**: `studio.html` üstünde 3-buton mod anahtarı (`labMode()`, localStorage `studioMode` kalıcı; canlı loop varsa `active_run_id` AUTO'ya düşürür). **SIMPLE** = `fragments/studio_simple.html` 5-adımlı sihirbaz (Strategy→Market→Backtest→Reliability→Finish; ortalama kullanıcı için hazır şablon kartları + saved-spec picker + kendi-tarifin, mevcut `/backtest/describe|run` + `/robustness/run` endpoint'lerine `htmx.ajax` ile gider — YENİ endpoint yok). Canlı düğümler `#result`/`#robustness-result`/`#chat-thread` **SINGLETON** (progress fragmentleri hx-target'ı hardcode eder) → `swMoveNodes()` Simple'a girişte düğümleri sihirbaz slotlarına REPARENT eder, çıkışta Pro evlerine iade eder (polling/chain/chart-reinit her yerde aynen çalışır). **PRO** = eski Compose/Backtest/Analyze yüzeyi; **AUTO** = otonom loop cockpit'i (System B `/agent/run`). **Adım gating (2026-07-23)**: sihirbazda backtest ZORUNLU adım — `swState.btDone/robDone` tamamlanma bayrakları (final sonuç `[data-bt-pnl-pct]` panelinden; hata fragmenti bu attribute'u taşımaz → hatalı koşu adım açmaz), `swGoto(n>=4)` btDone'suz 3. adıma geri düşer, `#sw-next-3` disabled + rail 4-5 kilitli (`.sw-rail-step:disabled`); Reliability AÇIKÇA opsiyonel etiketli (not + "Skip → Finish"/"Continue → Finish" dinamik buton); yeni koşu başlarken bayraklar sıfırlanır. Merkezi `bt:run-state` CustomEvent'i (backtest_scripts) sihirbazın buton/hint durumunu besler. **Market adımı katalog-odaklı (2026-07-23)**: sihirbazın coin quick-button'ları sabit BTC/ETH/SOL listesi yerine `bybit_symbols` context'inden (`_bybit_symbols()` → cache katalogu; boşsa statik fallback) unique sembollerle render edilir (USDT soneki etikette kırpılır); TF butonları 8 seçeneğe çıktı: 1/5/15/30 dk + 1/4/12/24 saat (kod `1/5/15/30/60/240/720/D`; 45m Bybit'te yok — atlandı) **`llm_badge()` (2026-08-02):** şablonlara `llm_model_label`/`llm_model_id` verir — SIMPLE ve PRO yüzeyleri uygulama varsayılanını kullandığı için rozet orada statiktir; AUTO'da model koşu başına seçildiğinden rozet ayrı yaşar (`#mc-model-slot`, idle'da picker'ı yansıtır, koşarken cockpit fragment'i OOB ile koşan modeli yazar — canlı olan kazanır). Kararlar: [[model_secici_ve_gorunurluk]] **AUTO brief varsayılanları + açık açılış (2026-08-03):** brief döngü boştayken AÇIK gelir ve fiilen koşulan ayarlarla dolu (QQQ.NASDAQ · spot · 1H · Strict · 4 iterasyon · continuous açık · guidance). İki alan sabit yazılmaz: DATE RANGE açılışta MAX düğmesine basılarak `/data/range`'den doldurulur (sabit tarih katalog büyüdükçe yalan söylerdi) ve MODEL `AUTO_DEFAULT_MODEL` (`or:moonshotai/kimi-k3`) ama `_mc_default_model()` seçili değerin CANLI listede olduğunu doğrular, yoksa `""` (Claude) fallback — aksi halde hiçbir seçeneği işaretlenmemiş bir `<select>` kalır ve tarayıcı ilk seçeneği seçili sayar. Canlı koşuda çekmece kapalı kalır (`mcInitBrief`, `active_run_id`). `POST /agent/run`'ın kendi Form varsayılanları DEĞİŞMEDİ (BTCUSDT/linear/5/continuous kapalı) — onlar form dışı çağrıların sözleşmesi. Detay: [[auto_mission_control]] **Picker context (2026-08-02):** `_llm_models()` yanında `_llm_or_free_only()` ve `_llm_or_paid_extras()` de context'e girer — şablon OpenRouter grubunu "ücretsiz (N)" / "tümü (N)" diye dürüstçe başlıklandırsın ve elle eklenen paralı uçları ayrı optgroup'ta göstersin diye (sessiz filtre, sessiz gizlemenin tekrarı olurdu). | [[strategy_and_actor]], [[backtesting_guide]] | **AUTO sekmesi Mission Control'e geçti (2026-08-02)**: `studio.html`'in AUTO paneli artık sayfa-seviyesi konfigürasyon formu değil — form 420px'lik **brief slide-over**'ına (`#mc-brief-form`, `mcBrief()`), START/STOP AUTO üst barına taşındı; koşan döngü tek ekranlık kokpitte izlenir. Form hâlâ değişmemiş `POST /agent/run`'a gider (+ `view=mission`). Üst bardaki START/STOP ve maliyet okumaları `#agent-result`'ın DIŞINDA (forma aitler) olduğundan kokpit fragment'i onları `hx-swap-oob` ile tazeler — ikinci bir poll açılmaz. **Yükseklik ölçülür, sabitlenmez**: `mcFit()` kokpitin `getBoundingClientRect().top`'unu okuyup `--mc-offset` yayınlar, CSS `calc(100vh - var(--mc-offset))` kullanır (kabuk yüksekliği yoğunluk kurallarına ve dar ekranda sarmalanmaya göre değişir; hardcode edilen sayı ilk yoğunluk değişikliğinde bozulurdu). Detay: [[auto_mission_control]]. **4xx gövdeleri ekrana ulaşmıyordu (2026-08-02):** htmx 1.9 varsayılanda 4xx yanıtını swap ETMEZ, bu yüzden `POST /agent/run`'ın ZATEN VAR OLAN hata mesajları (geçersiz tarih, eksik enstrüman, 50-koşu limiti 429) AUTO ekranına hiç ulaşmıyor, START'a basmak hiçbir şey yapmamış gibi görünüyordu. `studio.html`'de `htmx:beforeSwap` 4xx'te `shouldSwap = true` yapar — YALNIZ `/agent/run` için, çünkü kendi kendini sonlandıran `/agent/progress` yoklamasının 4xx gövdesi kokpitin yerine geçmemeli.
| `web/routes/strategy.py` | Strategy composer + block CRUD. **GET / (kök) artık 307 → `/studio`'ya redirect (2026-07-21)** — Composer, birleşik Studio'nun "Compose" sekmesi oldu; `/strategy/*` HTMX endpoint'leri (save/suggest/drafts/blocks/chat) aynen çalışır. `POST /strategy/save` artık `composer.build_spec()` çağırır (iki spec-constructor'ı tek servise indi). **Multi-timeframe trend filtresi** advanced-options'ta açık: `trend_filter`/`trend_interval`/`trend_ema_period` spec alanlarına yazar (ana TF'de işlem + üst TF'de EMA trend onayı; `composer.ComposedStrategy` ikincil bar feed'ini subscribe eder, look-ahead güvenli). `_sid`/`COOKIE` artık `web.shared.session_id`/`SESSION_COOKIE`'ye ince alias. **Blokları AI ile düzenleme — çok-turlu sohbet (2026-07-21)**: sıfırdan-üret+kaydet+sil'in yanına mevcut bir bloğu/listeyi sohbetle düzenleme yolu eklendi (`_BLOCK_CHAT`/`_DRAFTS_CHAT = ChatStore()` ayrı örnekler — payload şemaları farklı, çakışmaz). **A — custom block KODU** (3 endpoint): `POST /blocks/{name}/chat/new` (`cbs.get_custom`→None ise 404, builtin ise 400; kod `cbs.module_path(name).read_text(encoding="utf-8")` — get_custom kodu döndürmez; ilk user mesajına kod+meta gömülü → `chat_edit_block` `asyncio.to_thread`), `POST /blocks/chat` (oku-kopyala → `chat_edit_block` → `commit`; `changed` ise working_meta/code + `_preview_signals` grafiği güncellenir), `POST /blocks/chat/save` (**save-custom'ın 409 kontrolünü ATLAR** — aynı isimle üzerine yazmak edit'in amacı; `validate_generated_code` + `_test_execute_generated(require_max_lookback=True)` defense-in-depth tekrar → `cbs.save_custom` → `unregister_custom_block` → `register_custom_from_disk` → `HX-Redirect /strategy?saved_block=<name>`). Dependent uyarısı (`_dependent_spec_names`: bu tipi kullanan kayıtlı strateji adları) kaydetmeden önce gösterilir — edit silmez ama bağlı stratejilerin backtest'i sessizce değişir (engelleme yok). **B — strateji draft LİSTESİ** (3 endpoint): `POST /drafts/chat/new` (boş draft → 400 uyarı; liste `_blocks_to_dicts` ile gömülü → `chat_edit_blocks`), `POST /drafts/chat` (turn), `POST /drafts/chat/apply` (working_blocks → `_DRAFTS[sid] = [SignalBlock(**b) …]` → drafts_list render, OOB strategy_preview dahil). Ortak: LLM çağrısı ASLA lock altında değil (`ChatStore.commit` oku-kopyala deseni); expired conv → nazik fragment; `_preview_signals(code, meta, role_hint)` yardımcısı `generate_custom_block`'un BTC-1m sinyal-önizleme bloğundan çıkarıldı (kod tekrarı önlendi — `generate` + A-chat/save paylaşır) **`_MAX_LLM_TEXT_LEN` (4000) artık 4 LLM route'unun hepsinde (2026-08-08)**: eskiden yalnız `/blocks/generate` kontrol ediyordu; `/suggest`, `/drafts/chat`, `/blocks/chat` aynı serbest-metin alanlarını sınırsız Claude'a geçiriyordu — üçü de artık LLM çağrısından ÖNCE 400 dönüyor. | [[strategy_and_actor]], [[order_flow_pipeline]], [[bar_aggregation_and_type_syntax]], [[nau_deepr_toplu_sertlestirme_2026_08]] |
| `web/routes/backtest.py` | **GET / (kök) artık 307 → `/studio`'ya redirect (2026-07-21, `?spec_id=` korunur)** — Backtest formu birleşik Studio'nun "Backtest" sekmesi oldu; `/backtest/*` HTMX endpoint'leri (run/sweep/describe/plan/chat/result/progress) aynen çalışır. `_LAST_RESULT` artık **session-scoped** (`sid→slot`, LRU 500; `_last_result_get`/`_last_result_set`) — eski process-global tek-slot çok-kullanıcıda last-writer-wins idi; run-worker sid'i request-anında yakalar. Çok-turlu sohbet artık ortak `web.shared.ChatStore` kullanır (el-yapımı `_CHAT_STORE`/`_CHAT_LOCK`/`_chat_evict_locked` silindi; `_CHAT = ChatStore()`). `/backtest/describe` spec'i `composer.build_spec()` ile kurar. **/backtest** birleşik tarif-odaklı panel: stratejiyi doğal dille tarif et → `POST /backtest/describe` Claude'a entry/exit için AYRI custom blok(lar) yazdırır (`propose_condition_breakdown` → koşul-başına `propose_custom_block` → blok-seviyesi OR/AND spec), sonra seçilen zaman dilimlerine göre zincirler: **2+ TF → `POST /backtest/sweep`** (aynı spec'i her TF'de **paralel** koşup —`ThreadPoolExecutor` + `force_subprocess`, `NAUTILUS_PARALLEL` kill-switch— karşılaştırma tablosu; cache-yok TF atlanır, en-iyi sütun yeşil; sweep başlığında ŞU AN koşan TF + `n/n` ilerleme; her satırda **tarih aralığı sadece yıl olarak** (`date_from[:4]`/`date_to[:4]` — aynı yıl `2023`, farklı `2021–2024`), Sharpe NaN hover-tooltip ile açıklanır; **B&H % referans sütunu** (`buy_hold_pct` — aynı dönemde al-tut getirisi, sönük renk), PnL % pozitif=yeşil/negatif=kırmızı, **tüm metrik sütunları genel-kabul eşik-renkli**: Max DD (<%10 yeşil · <%30 sarı · ≥%30 kırmızı; DD pozitif yüzde, düşük iyi → min en iyi), Sharpe (≥1 yeşil · ≥0 sarı · <0 kırmızı), Win% (≥55 yeşil · ≥40 sarı · <40 kırmızı), PF (≥1.5 yeşil · ≥1 sarı · <1 kırmızı)), **1 TF → `POST /backtest/run`** (tam sonuç + equity + robustness OOB). **Asset type toggle** (Bybit / Index-Equity): radio seçimine göre Bybit symbol+category+TF-checkbox grubu veya Index ticker+granularity-checkbox grubu (1m/5m/15m/1h/1d, çoklu seçim) gösterilir; `granularity: list[str]` olarak alınır, `gran_first` ile `/run` chain'e geçer. **Sembol datalist katalog-odaklı**: `list_catalog_bybit_symbols()` (Bybit) + `_catalog_index_symbols()` (POLYGON/diğer) katalog `data/bar/` dizininden dinamik okur — sabit `BYBIT_SYMBOLS` tuple'ına bağımlılık kalmadı. - **"Önce AI ile iyileştir" backtest-farkındalıklı (2026-07-20)**: `POST /backtest/plan` artık `bt_pnl_pct/bt_sharpe/bt_max_dd/bt_n_trades/bt_win_rate/bt_spec_name/bt_best_tf` form alanlarını alır. Backtest/sweep tamamlanınca `htmx:afterSettle` JS'i `data-bt-*` attribute'larını (backtest_result.html + sweep_progress.html) okuyup formun hidden input'larına yazar. `propose_refined_description(desc, backtest_metrics)` bu metriklere bakarak Sharpe düşükse trend filtresi, işlem az ise AND koşulunu gevşet gibi **hedefli** iyileştirme önerir. Plan cache key'e `bt_pnl_pct + bt_n_trades` eklendi — aynı tarif farklı backtest sonuçlarıyla farklı öneri üretir. **Robustness-farkındalık (2026-07-21)**: `POST /backtest/plan` ayrıca `rob_overfitting/rob_verdict/rob_wfo_eff/rob_oos_sharpe/rob_stability` alanlarını alır; bunlar `robustness_result.html`'in kök `<div data-rob-summary>`'sindeki `data-rob-*` özetinden (overfitting_score, verdict, wfo_efficiency, OOS Sharpe, stability_label) okunur — fragment kendi içindeki inline script ile `#robustness-result` swap'ında doğrudan hidden input'lara yazar (`htmx:afterSettle` yalnız `#result`'a bağlı olduğundan robustness kanalı ayrı beslenir). `propose_refined_description(desc, backtest_metrics, robustness)` yeni 3. argümanla **overfitting-farkındalıklı** öneri üretir: overfitting skoru <0.4 ya da OOS Sharpe zayıfsa parametreleri sadeleştir/aşırı-optimizasyondan kaçın, robustsa (≥0.7) güçlendirme öner. **Cache düzeltmesi (2026-07-20→21)**: `refined_result` ARTIK hiç cache'lenmez — yalnız `bd` (blok planı) `(desc, allow_short)` anahtarıyla cache'lenir; her buton basışında taze AI önerisi üretilir (kök neden: eskiden robustness sonrası öneri değişmiyordu çünkü robustness verisi plan endpoint'ine hiçbir kanaldan gitmiyordu — çözüm `data-rob-*` + hidden input köprüsü, `data-bt-*`/`btFillPanels` desenini izler). Yeni backtest sonucunda `btFillPanels` eski `rob_*` hidden input'larını temizler (eski robustness'ın yeni backtest'e sızmasını önler). **Çok-turlu sohbet endpoint'leri (`/backtest/chat/new` + `/backtest/chat`, 2026-07-21)**: tek-atışlık `/backtest/plan`'ın YANINA (onu bozmadan) "AI ile iyileştir"i sohbete çeviren iki endpoint. State **sunucu tarafında** `_CHAT_STORE: dict[conv_id → {messages, context, last_refined, ts}]` + `_CHAT_LOCK` + TTL(30dk)/kapasite(100) eviction (`_chat_evict_locked`) — `ProgressStore`'un hafif hâli (sohbetin "done" kavramı yok). **Lock disiplini**: LLM çağrısı ASLA lock altında değil → oku-kopyala → `asyncio.to_thread(chat_refine)` (lock'suz) → tekrar-al-ve-yaz (arada evict edilmiş olabilir). `chat/new` `plan_preview` form imzasını paylaşır (`_collect_chat_context` ile `bt_*`/`rob_*` → context), ilk user mesajına metrikler `_format_metrics_block` ile gömülür (ekranda ham `display` gösterilir); `conv_id=uuid4hex[:8]`. Bilinmeyen/süresi-dolmuş `conv_id` → nazik "sohbet süresi doldu" fragment'i (crash yok). Sonuç `fragments/chat_thread.html`'e render — mesaj balonları + güncel net tarif `#refined-description` textarea'sı + "✅ Tamam — Blokları Oluştur" butonu (`/backtest/describe`'a AYNEN bağlı, plan_preview ile aynı üretim zinciri). — kullanıcı denetimli, boş/yarım tarifle gereksiz LLM çağrısı önlenir; **ana yol tek `▶ Backtest Çalıştır` birincil butonudur — plan opsiyonel bir ön-adımdır**) tek `propose_condition_breakdown` çağrısıyla yorumlanır, koşullar YEREL heuristikle built-in bloklara eşlenir (`_match_builtin`/`_INDICATOR_TO_BUILTIN`), tuzaklar tahmin edilir (`_predict_plan_warnings`: allow_short kapalıyken short tarifi, 3+ AND→~0 işlem, size_precision=0 equity, Bollinger-cross) ve **uyarılar koşul listesinin üstünde** gösterilir (grafik kaldırıldı). Sonuç `(desc, allow_short)` anahtarıyla 5dk TTL cache'lenir; **in-flight dedup** (`_PLAN_INFLIGHT`, `asyncio.Future`) eşzamanlı aynı-anahtar isteklerin tek LLM çağrısını paylaşmasını sağlar; LLM hatası cache'lenmez (recovery maskelenmez), eviction FIFO. `allow_short` form parse'ı `_parse_bool_form` ile açık (`"0"`/`"false"` → False; `bool("0")==True` tuzağı önlenir). **Layout**: `grid-2 equal bt-airy` — sol form (numaralı 3 adım: **① Enstrüman & veri · ② Stratejini anlat · ③ Çalıştır**), sağ sütun **sekmeli tek panel** (2026-07-20 UX yeniden tasarımı): **`Sonuç · ✨ AI Plan · Robustness · Geçmiş`** sekmeleri (`.bt-tabs` sticky şerit, `btTab(name)` JS). **Sonuç** sekmesi = `#result`; **AI Plan** sekmesi = `#plan-preview` (+ `#plan-empty` boş-durum kartı — "Önce AI ile iyileştir" butonu `onclick` ile `btTab('plan')`'e geçer, plan dolunca `htmx:afterSwap` `#plan-empty`'yi gizler, plan_preview "✕ Kapat" boşaltıp `btTab('result')`'a döner, "✅ Tamam" `#result`'a yazınca otomatik Sonuç sekmesi); **Robustness** sekmesi = `#robustness-section`; **Geçmiş** sekmesi = tıklanır `recent_runs` satırları (`hx-get /backtest/result/{run_id}` ile birebir geri-yükleme). Sonuç sekmesinde trade-sayısı rozeti (koşarken `⟳`). Sonuç gelince ekran otomatik Sonuç sekmesine odaklanır (`scrollIntoView`) ve hero üstünde verdict/özet kartı (`.verdict-card`) görünür. **Robustness/plan form doldurma `btFillPanels(root)` ile** (`data-rob-*`/`data-bt-*` → form): hem her `#result` swap'ında (`htmx:afterSettle`) HEM `DOMContentLoaded`'da çağrılır — sunucu-render `last` sonucu htmx:afterSettle tetiklemediğinden ilk yüklemede robustness `spec_id` boş kalıp "Analizi Çalıştır" sessizce 404 dönüyordu (2026-07-20 fix, CDP ile doğrulandı). Snapshot store `bt_results/<run_id>.json` (`save_result_snapshot`/`load_result_snapshot`, `web.shared`) equity/trade listesi dahil tam sonucu saklar — jsonl log yalnız skalar metrik tuttuğundan geçmiş birebir yeniden açılabilir. `#result`/`#plan-preview`/`#robustness-section` id'leri + `data-bt-chain-*` zinciri korunur (bkz. değişiklik günlüğü 2026-07-20 "sekmeli sağ sütun" + "AI Plan sekmesi"). Plan preview **dolu olunca `position:sticky; top:70px`** (topbar altı) — alttaki (daha uzun) sonuç/robustness/geçmiş yığını kayarken AI önerisi görüş alanında kalır; boşken sticky değil (yükseklik yok → sonuçların üstüne binmez, `#plan-preview:not(:empty)`). **Sol/sağ sütun DOM dengesi**: sol `.stack` sağ `.stack`'ten ÖNCE kapanmalı — eksik `</div>` sağ sütunu sol sütunun içine nested edip `grid-2`'nin ikinci hücresini yok eder (sağ yarı boş, plan-preview form altına düşer); iki `.stack` de `grid-2`'nin DOĞRUDAN çocuğu olmalı. **Strateji üretim ilerlemesi** (`fragments/describe_progress.html`) `#result`'a swap edilir ve **artık akış-içinde (`#result` sağ sütununda, sabit) render edilir** — eskiden kökündeki `.bt-gen-float` class'ı onu `position:fixed` ile ekranın sağ-üst köşesine "toast" gibi sabitliyordu; 2026-07-20'de kaldırıldı (kullanıcı "sonuç sabit kalsın, toast olmasın" dedi) → `.bt-gen-float` class'ı 3 fragman dalından ve CSS'ten silindi. `every 1s` self-poll ve chain (üretim bitince `#result`'ı `/backtest/run|/sweep` sonucuyla değiştirir) aynen korunur. **Üretim durumu BELLEKTE** (`_GEN_PROGRESS`, `ProgressStore(20)`); poll `state=None` alırsa (`_gen_state_view` `None` → kayıt yok) fragment artık boş div basıp paneli sessizce YOK ETMEZ — aynı akış-içi panelde **"üretim yarıda kesildi, sunucu yeniden başladı"** uyarısı (sarı "⚠ Kesildi" badge) gösterir ve `hx-trigger` üretmeyerek polling'i durdurur (`{% if state is none %}` dalı). **Kök neden (2026-07-19, deneysel kanıt)**: dev'de `uvicorn --reload` proje kökündeki HER `*.py` değişiminde sunucuyu yeniden başlatır; üretim ~15-20 sn süren bir worker thread'de çalıştığından, o sırada izlenen bir `.py` değişirse (eşzamanlı editör kaydı / ruff otomatik format) worker + `_GEN_PROGRESS` uçar ve panel eskiden sessizce kaybolurdu. Azaltma: `--reload-exclude "$PWD/nautilus_wiki|.claude|tests"` (MUTLAK yol şart — uvicorn göreli adı/glob'u eşleştirmez, yalnız var olan dizini `path.parents` ile recursive dışlar) + uzun üretim için `--reload`'sız çalıştırma (bkz. `server.py` docstring). Plan preview düğmeye basınca sağ sütunun tepesinde belirir, backtest çalışınca sonuç hemen altındaki `#result`'a gelir. **allow_short toggle** formda (varsayılan AÇIK) → describe worker spec'e taşır (MARGIN hesap). **Canlı ilerleme flow'u**: `/backtest/progress/{run_id}` worker'ın serbest mesajlarını kanonik Nautilus fazlarına eşler (`_derive_bt_phases`/`_BT_PHASES`). `_normalize_intervals` ortak; `intervals_csv` hx-vals dizi kodlamasına güvenmeden çoklu-TF taşır. **Bybit cache bounds keşfi metadata-only (2026-07-20)**: tarih penceresi tespiti için artık FULL parquet read yerine pyarrow `ParquetFile` row-group METADATA istatistikleri (min/max timestamp) okunur — 3M+ satır taranmaz; index kolonu `path_in_schema`'da `"index"` içeren kolon olarak bulunur, stats yoksa `columns=[]` hafif fallback read'e düşer. Tarih girilmezse varsayılan başlangıç penceresi eski 7 gün yerine `max(cache_start, cache_end−365d)` ile **365 GÜN cap**'lenir (Bar construction loop 3M+ satır churn etmesin diye). **Progress sıralaması (2026-07-20)**: Index/Equity dalında `_progress("Reading data · …")` çağrısı artık parquet read'den ÖNCE tetiklenir (UI "read" fazını hemen gösterir — eski davranışta çağrı okuma sonrasındaydı, UI "Reading data · parquet cache" adımında takılı görünüyordu); Bybit dalında da metadata okumadan önce `_progress("Reading data · parquet cache · …")` eklendi. **Session-başına tek-aktif-koşu guard'ı (2026-07-23)**: `_ACTIVE_RUNS: {sid → (kind, id)}` registry — `/run`+`/describe`+`/sweep` aynı oturumda canlı bir run/gen/sweep varken **409 + HX-Toast** döner (HTMX 2xx-dışını swap etmez → mevcut progress paneli korunur, ikinci submit resetleyemez); registry self-clearing (worker `finally` done → sonraki sorguda düşer, evict/restart = inaktif) + 500-girdi eşiğinde fırsatçı prune (store lookuplar registry lock'unun DIŞINDA — lock nesting yok). **Tarih validasyonu (2026-07-23)**: `_invalid_date_range` — start>end veya bozuk format worker başlamadan **400 + HX-Toast** (bybit/index/ext çiftleri; boş = full-cache serbest); istemci tarafında `btDateRangeError` + `htmx:beforeRequest` guard'ı submit'i tarayıcıdan çıkmadan iptal eder + `#bt-date-error` inline mesaj. **Metrik grid kompakt (2026-07-23)**: `backtest_result.html`'de Risk·Trades·P&L/Cost bölümleri `.metric-sections-row` sarmalayıcısıyla YAN YANA (sütun oranı 2fr:3fr:3fr ∝ 4/5/6 hücre), her bölüm `mg-2`/`mg-3` sabit sütunla 2 satıra sarar (dar hücre: padding 5px 8px, değer 13px); `body.page-studio .metric-grid` auto-fit override'ını specificity ile geçen ek seçici şart; <900px'te tekrar alt alta **MAX butonları (2026-08-01)**: backtest_body'nin üç kaynak tarih çifti (bybit/index/external), SIMPLE sihirbazının Advanced çifti ve Lab formu artık `nlMaxRange` (app.js) ile `/data/range`'ten önbellek kapsamını doldurur; boş bırakmak eski 'full cache' davranışıdır, MAX yalnız aralığı görünür kılar. **Log yazımı snapshot'tan ÖNE alındı (2026-08-02):** `log_backtest()` dönen `ts` `result.log_ts`'e yazılır ve `_result_viewmodel` onu kopyalar; snapshot önce yazılsaydı o koşunun kalıcı görünümü tear sheet linksiz kalırdı. `_recent_runs()` satırlarına da `ts` eklendi (Run History linkleri). **Test kapsamı genişletildi, kod değişmedi (DeepR #59, 2026-08-08):** `_is_equity_target`/`_local_fallback_breakdown`/`_predict_plan_warnings`/`_preview_signals` — tek-çağrı-noktalı, zaten düz fonksiyonlardı; boşluk mimari değil test kapsamıydı. `_preview_signals` için `tests/test_backtest_route_pure_helpers.py`'de sentetik BTC 1m serisiyle 7 built-in giriş tipi + 2 çıkış dalı + allow_short bastırma ampirik olarak doğrulandı. `run()`'ın 3 instrument-kind dalı ve `describe()`'ın worker gövdesi bilinçli olarak çıkarılmadı — ayrı, gelecekteki bir plan. Ayrıntı: [[nau_deepr_ikinci_tur_2026_08_08]]. | [[backtesting_guide]], [[environment_contexts]], [[bar_aggregation_and_type_syntax]] |
| `web/routes/strategy_studio.py` | **GET /studio/{strategy_id} — görsel strateji kurucu (2026-07-25)**. `studio_app` paketinin ana uygulamaya birleştirilmiş hâli: FastAPI app → `APIRouter` (34 route), şablonlar `web/templates/studio/`, statikler `web/static/studio.{css,js}` host mount'larından. Bare `/studio` (Composer+Backtest) ile çakışmaz — farklı yol şekli. İki motor anahtarı: `STUDIO_BACKTEST` (Run butonu) ve `STUDIO_BACKTEST_OPT` (sweep + AI denemeleri); ayrı olmalarının nedeni fan-out maliyeti (sweep üst sınırı `min(sweep, 400) × (1 + folds)` motor koşusu) — sweep için gerçek motor seçiliyse `STUDIO_OPT_MAX_ENGINE_RUNS` üstünde baştan 422. AI guardrail baseline'ı denemenin koştuğu motorda ölçülür (`_trial_baseline`, LRU); deploy kapısı bilerek `latest_run`'ı okur. **HTTP semantiği (2026-07-25)**: kaynak-yok her uçta 404 (`RuleNotFound`, `MutationError` alt sınıfı — mevcut except'ler bozulmadan ayırt edilir; `PATCH`/`DELETE /rules/{id}` + `opt/toggle` + `opt/range`), geçersiz girdi 422; beklenmedik motor hatası da 422 + mesaj (500'de HTMX hiçbir şeyi swap etmez → hata görünmez olurdu). **Motor izolasyonu (2026-07-25)**: `NautilusBacktestAdapter._run_one` artık `run_composed_backtest`'i doğrudan çağırmaz — uygulamanın geri kalanıyla aynı yoldan, `sandbox.run_backtest_guarded(..., force_subprocess=True)` ile öldürülebilir child'da koşar (Nautilus backtest'i GIL'i tuttuğu için bir "Optimize" tıklaması tüm HTMX pollingini dakikalarca donduruyordu); child enstrümanı `_recipe(symbol, timeframe)` ile yeniden kurar. `load_bars` (`BARS_TTL_S=300`) bir sweep'in aynı barları aday×fold kadar yeniden decode etmesini bitirir — adapter modül-düzeyi singleton olduğu için TTL zorunlu. `store.py`: `studio_runs.defn_hash` (additive sütun) + `latest_run_for_hash` → **deploy geçidi konuşlandırılan tanımın koşusunu arar**, "en son koşu"yu değil; `save()` `BEGIN IMMEDIATE` ile sürüm yarışını kapatır, bağlantılar `with` çıkışında kapanır (`busy_timeout=30s`). **`promote_draft` atomik (2026-07-26)**: eskiden `load_draft` → `save` → `delete_draft` üç ayrı işlemdi ve aradaki boşluğa düşen bir `save_draft` (UI otomatik kaydı / AI döngüsünün geri yazması) sondaki delete tarafından siliniyordu — kullanıcının en yeni düzenlemesi hata üretmeden kayboluyordu. Artık tek `BEGIN IMMEDIATE` işlemi; silme okunan json'a koşullu (`AND json=?` — *okumadığın draft'ı silme*); ortak gövde `_insert_version(con, defn)`. `schema.py`: `OptimizeRange.n_values` artık `floor(span/step+ε)+1` — grid kullanıcının `max`'ını aşmıyor. Detay: [[strategy_studio]], [[nau_guvenlik_dayaniklilik_duzeltmeleri]] **Bağımlılık tazeliği (2026-07-26)**: `OPTIMIZER` adaptörünü import anında yakalıyordu — `TRIAL_ADAPTER` sonradan değişirse (test, ileride config yeniden yüklemesi) sweep'ler **bayat** motorla koşuyordu (tek anahtar, iki doğruluk kaynağı). `_optimizer()` güncel adaptörü çözer; dokunulmamış yolda aynı nesneyi döndürür, yalnız ayrışma varsa yeniden kurar. Modül-düzeyi kurulumun kalanı (store/adapter/LLM/runner import anında) bilinçli olarak korundu — bkz. [[nau_guvenlik_dayaniklilik_duzeltmeleri]] §9.5. **Ana kabuğa gömüldü (2026-07-27)**: sayfa artık `base.html`'i extend ediyor (sol nav + topbar), `studio_page` `active="studio_builder"` + `page_title` geçirir; `studio.css`'in TÜM kuralları `.studio-embed` altına kapsandı — global `:root`/`body`/`header` kuralları ve ortak sınıf adları (`--panel`, `.btn`, `.metric`, `.tab`) kabuğu yeniden boyuyordu. UI adı "Strategy Builder" (nav'daki "Strategy Studio" = `/studio`, farklı sayfa); modül/rota adları tarihsel kaldı. Bkz. [[strategy_studio]] "Kabuk entegrasyonu". **Canvas görünümü (2026-07-27)**: aynı stratejinin node graf'ı — form görünümünün YANINDA, yerine değil. Eklenen üç route'un üçü de **GET**: `/studio/{id}/canvas` (sayfa), `/studio/{id}/canvas/graph` (JSON), `/studio/{id}/canvas/inspector/{node_id}` (seçili node'un blok partial'ı). Yazan hiçbir şey eklenmedi — drop `POST …/blocks/{block}/rules`'a, ✕ `DELETE …/rules/{id}`'ye, chip düzenlemesi `PATCH …/rules/{id}`'ye, Save/Discard/Run mevcut uçlara gider; bu yüzden "entry en az bir kural tutar" gibi sunucu kuralları canvas'ta yeniden yazılmadan bağlar (aynı 422, aynı error banner). Serbest node editörü (React Flow/Drawflow) **kullanılmadı**: `StrategyDefinition` serbest graf değil kısıtlı ağaçtır, kenarlar şemadan türer → `strategy_studio/graph.py` `to_graph(defn, ghosts=None)` saf fonksiyonu `{nodes, edges, meta}` üretir (store'a dokunmaz; ghost'lar tanımda değil store'da yaşadığı için argüman olarak gelir), `web/static/canvas.js` katman indekslerini piksele çevirir (SVG `viewBox` zoom/pan, minimap, klavye — bundler yok). Görünüme dair hiçbir şey saklanmaz: pozisyon/zoom/kenar listesi şemaya girmez, layout her render'da yeniden hesaplanır. İki uygulama kısıtı: bir rule node'u tek başına `_rule.html` ile değil **kapsayan bloğuyla** açılır (yalnız kuralın kontrolleri `closest .block`'u hedefler), ve sayfa `studio.js`'i de yükler (partial'ı davranışı olmadan yeniden kullanmak = düzenlenebilir görünen ama düzenlenmeyen chip'ler). `canvas.css` `.cv-embed` altında kapsanmış (`.studio-embed` değişkenlerini miras alır). **Sembol dökümü (2026-07-28)**: `GET …/runs/latest/symbols` + `…/symbols/{idx}/trades` — motorun zaten hesaplayıp harmanda attığı sleeve metrikleri `InstrumentMetrics` olarak saklanır ve tablo+işlem listesi olarak render edilir (idx dışı → 404, sözel → 422). **Tarih aralığı (2026-08-01)**: `BacktestMetrics/InstrumentMetrics.date_from/to` — footer Range rozeti + tabloda Range kolonu; gerçek adapter'ın yalnız `lookback_days=180` yüklediği gerçeğini görünür kılar. **Seçilebilir aralık + MAX (2026-08-01)**: run barında `date_from/date_to` date inputları (`hx-include` ile `POST …/backtest`'e biner; boş = eski davranış) + `GET …/data-range` (enstrümanların önbellek kapsamının BİRLEŞİMİ — MAX butonu doldurur, kapsam yoksa 404). Route ISO+sıra validasyonunu koşu başlamadan yapar (400); `NautilusBacktestAdapter.run(date_range=)` her sleeve'i `_slice_dates` ile [from,to] gün-kapsayıcı dilimler (dilim `MIN_WINDOW_BARS` altına düşerse anlaşılır `UnsupportedStrategy`), stub aynı imzayla aralığı Range rozetine yansıtır. **Tema birleşmesi (2026-07-29)**: `.studio-embed` app kabuğunun değişkenlerine bağlandı, palet üst şerit oldu. Üçünün detayı: [[strategy_studio]] | [[strategy_and_actor]], [[backtesting_guide]], [[portfolio]], [[nau_guvenlik_dayaniklilik_duzeltmeleri]] |
| `web/routes/agent_backtest.py` | **Arama ekonomisi (2026-08-04):** `_clamp_spec_trade_size` artik hisse spec'ini 1 hisseye SABITLEMEZ — mod `fixed` ve olcek bozuksa (<1) `percent_equity`'ye gecirir (`AGENT_EQUITY_PCT`, vars. %95); sabit adet QQQ'nun 30x fiyat araliginda calismaz. Eski davranis her hisse backtest'ini $220'lik pozisyona kilitleyip sabit IBKR komisyonunu islem basina %0,91 vergiye ceviriyordu — bkz. [[auto_arama_ekonomisi]]. Ayrica `_thin_curves`: oturum loguna yazilan yapidaki 40'tan uzun SAYI dizilerini indirger (dict listeleri ve metrikler korunur, girdi degismez); `robustness_result` olay basina 3,5 MB yaziyordu. | Otonom backtest pipeline (5-faz: veri→strateji üret→backtest döngüsü→sıralama→robustness). **NAU kompozit skor** (`_score`): `0.7·calmar + 0.3·sharpe_per_trade` × `n/(n+20)` güven sönümü; junk kapısı `n<20 VEYA max_dd≥0 → -inf` (NAU ile hizalı); sharpe terimi **per-trade** ((mean/std)×√n), annualized DEĞİL. Robustness kapısı (`_robustness_passed`) IS/OOS + penalized OOS sharpe + Monte-Carlo (medyan DD>-25%) + multi-symbol katmanlıdır. Backtest'ler `sandbox.run_backtest_guarded(force_subprocess=True)` ile öldürülebilir child'da (event loop donmaz); robustness `run_robustness_guarded` child'ında, ilerleme `_IPC_Q` kuyruğuyla parent'a akar. Canlı **Gantt zaman çizelgesi**: her işlem (veri/LLM/backtest/robustness) bir span; `_tl_begin/_tl_end` ile SVG'de çizilir, `/sessions` replay eder. Bellekte olmayan run için dürüst terminal mesajı (session_end görülmüşse "tamamlandı", log yarıda kesikse "sunucu yeniden başlatıldı"). **Kullanıcı tarih penceresi (2026-08-01)**: form `range_start/range_end` (+ kaynak-farkında MAX butonu → /data/range); route ISO+sıra validasyonu (400), worker `_load_tf_uncached`'de bybit yolunda varsayılan lookback'i override eder (erken start → `load_bybit_bars` backfill), external yolunda tam katalog yükünü [start,end] dilimler; AÇIK kullanıcı penceresi 1-MINUTE 2-yıl kırpma guard'ını da devre dışı bırakır (kullanıcı seçti — olduğu gibi koşulur). **TF çoklu seçim (2026-08-01)**: `tfs: list[str]` form alanı — studio AUTO sekmesinin (studio.html cockpit) Timeframe chip'leri; bilinmeyen kodlar düşürülür, seçim `intervals` listesi olur (Multi-TF preset checkbox'ı küratörlü üçlüsünü korur, hiçbiri yoksa tekli `interval`'a düşer); external'da chip kodları `EXTERNAL_GRAN_BY_BYBIT_CODE` ile granularity'ye eşlenir. AUTO sekmesine Start/End + MAX da eklendi — /agent sayfasındaki pencereyle aynı arka uç; kokpite M22 bütçe tavanları da açıldı (Max saat / Max token alanları — route'un mevcut `max_hours`/`max_total_tokens` parametreleri, 0 = sınırsız). **Holdout ısınma payı + "0 işlem = ölçülemedi" (2026-08-02)**: 60 günlük mühürlü OOS dilimi indikatör ısınmasından (NAU_WINDOW≈260 bar) kısa kaldığı için 20/20 kazananda holdout 0 işlemle dönüyordu — katman sessizce hiçbir şey ölçmüyordu. `HOLDOUT_WARMUP_BARS=300`: holdout koşu çerçevesi = lead-in + mühürlü dilim (`holdout_cache` artık `(df, sealed_start)` tuple'ı); skorlama `_sealed_holdout_stats` ile YALNIZ mühürlü penceredeki girişleri sayar (per-trade Sharpe, 10k varsayılan sermayeye PnL oranı), 0 işlem `measured:false` + ⚠ ÖLÇÜLEMEDİ uyarısı olarak yüzeye çıkar (nötr geçer not değil). İkinci-beyin kavramı: holdout_penceresi_isinma_payi. **token_snapshot cost fix (2026-08-02)**: `_llm_cost_usd` artık `_MODEL_PRICING` yerine `token_ledger.cost_usd`'yi (tek fiyat kaynağı, 1h-TTL cache kuralı dahil) `current_model()` ile çağırır — eski kod CLI/abonelik arka ucunda None döndürüp her koşunun maliyet satırını boş bırakıyordu; artık kavramsal API karşılığı hep dolu. **Model seçici + QQQ (2026-08-01)**: `model` form alanı worker'a iner, worker başında `agent.set_thread_model` ile YALNIZ o worker thread'inin LLM çağrıları seçilen modele sabitlenir (boş/bilinmeyen = uygulama varsayılanı; eşzamanlı yüzeyler etkilenmez; ledger çağrılan modele doğru atfeder). AUTO kokpitinin Symbol select'ine QQQ.NASDAQ eklendi — route noktalı id'yi otomatik external'a terfi ettirir (`source`/`instrument_id` ayrı alan gerekmez; app-geneli "noktalı id = external" konvansiyonu) | [[backtesting_guide]], [[environment_contexts]], [[crash_only_design]] | **Mission Control sunumu (2026-08-02)**: `GET /agent/progress/{run_id}` artık `?view=mission` ile AYNI state'i kokpit fragment'i olarak render eder (parametresiz → klasik dikey `agent_progress.html`, `/agent` sayfası onu kullanmaya devam eder); `POST /agent/run` aynı ayrımı `view` form alanıyla yapar. **Yeni endpoint/poll yolu YOK** — bir koşu iki ekrandan izlenirse ikisi de aynı gerçeği gösterir, ayrışacak ikinci durum makinesi yoktur. State'e koşu başında üç anlık görüntü eklendi (worker sonradan değiştirmez): `brief` (BRIEF rayının kaynağı — kokpit request formunu yeniden türetmez), `started_at` + `max_hours`/`max_total_tokens` (BÜTÇE göstergeleri) ve backtest satırlarında `_spark_points()` ile **≤40 noktaya indirgenmiş** `equity` (ham eğri 100k+ nokta olabilir; 1 sn poll'da saniyede megabaytlarca JSON demekti). Eşleme [[auto_mission_control]] / `web/mission.py`'de. **Çalıştırılamayan model için kapıda ret (2026-08-02):** `POST /agent/run`, worker thread'i yaratmadan `agent.model_unavailable_reason()` ile seçimi yoklar; koşamayacak bir model 400 + sebep döndürür. Gerekçe: LLM'e hiç ulaşamayan bir AUTO turu rastgele kompozisyon üretip bunu normal koşu gibi gösteriyordu — yapılandırma hatasında kurtarılacak bir koşu yok, degradasyon yanlış ilaç. **`_agent_worker` bölündü (DeepR #47, 2026-08-08, tamamlandı):** ~1750 satırlık monolit, davranış korunarak modül-seviyesi düz fonksiyonlara çıkarıldı — `_WorkerState`, `_make_llm_control`, `_rank_and_filter`, `_propose_initial_strategy`, `_scan_one_candidate`, `_run_promotion_gate` (sealed-holdout kapısı), `_run_backtest_iteration`, `_load_timeframe_bars` + `_TfLoader` (Faz 0 veri/holdout yükleyici — planın en riskli adımı, en sona bırakılıp `TestContinuousCircuitBreaker`'ın değişmeden yeşil kalmasıyla doğrulandı). Faz 2'nin lookahead-generation bloğu ve alt-paket bölmesi bilinçli olarak plan dışı. Ayrıntı: [[nau_deepr_ikinci_tur_2026_08_08]]. **Sealed-holdout kapısının gerçek uçtan-uca kapsamı (2026-08-09, DeepR üçüncü tur, [YÜKSEK]):** `tests/test_agent_worker_e2e.py` — yalnız 4 dış sınır (`load_bybit_bars`, `propose_composed_strategy`, `run_backtest_guarded`, `run_robustness_guarded`) mock'lanıp Faz 0→1→2→3→4→5'in tamamı gerçek koşuyor; biri promosyonun geçtiği, biri mühürlü holdout'un yetersiz-işlem sayısı yüzünden reddettiği iki senaryo (red testi mutasyon testiyle doğrulandı — `_holdout_promotion_verdict` geçici olarak her zaman `True` dönecek şekilde bozulunca kırıldı). Aynı geçişte `composer._load_custom_blocks()` da sertleştirildi: `custom_block_store.list_custom()` import zamanında (modül-seviyesi çağrı) patlarsa artık `import composer`'ı düşürmüyor, uyarı loglayıp sıfır custom-block evaluator'ıyla devam ediyor. Ayrıntı: [[nau_deepr_ucuncu_tur_2026_08_09]]. **`newest_active_run_id()` eklendi (2026-08-09 [YÜKSEK]):** `studio.py`'nin ve bu modülün kendi `page()`'inin birbirinden bağımsız aynı `_AGENT_LOCK` taramasını tek fonksiyona indirdi — `studio.py` artık ham `_AGENT_LOCK`/`_AGENT_PROGRESS`'i hiç import etmiyor (deadlock geçmişi olan bir kilidin ikinci, izlenmeyen bir kullanıcısı kapandı).
| `web/routes/loop.py` | Start/stop background loop (dashboard `/` sayfasından) | [[crash_only_design]] |
| `web/routes/tokens.py` | **GET /tokens/badge + /tokens/table (2026-08-01)** — sidebar ENGINE kartı altındaki oturum-bazlı token rozeti: bu oturum (= `web.shared.SERVER_STARTED_AT`'ten beri; kalıcı ledger üzerinde `summary(since=)` GÖRÜNÜMÜ, ikinci sayaç değil — drift imkânsız) token + kavramsal $ karşılığı, ikinci satırda tüm-zaman `Σ`. `base.html` hx-get 60sn poll; `/tokens/table` = `format_table()` düz-metin drill-down. Rozet tooltip'i "$ = API liste fiyatıyla kavramsal karşılık, abonelik faturası değil" uyarısını taşır | [[crash_only_design]] |
| `web/routes/robustness.py` | IS/OOS + WFO + MC + multi-symbol | [[backtesting_guide]], [[portfolio]] |
| `web/routes/chart.py` | GET /chart/data — Lightweight Charts için OHLCV + strateji indikatörleri (JSON). Pencere/TF fizibilite guard'ı: aralık×TF >60k mum üretecekse veri yüklemeden reddeder (6y×1m ≈ 3M mum tarayıcıyı kilitler) | [[bar_aggregation_and_type_syntax]] |
| `web/routes/lab.py` | Strategy Lab — tek tıkla Claude fikir→custom block→backtest→KPI. **External dalı (2026-07-28)**: sembol select'inde "US Catalog" optgroup'u (`list_external_instruments`); noktalı id `load_external_bars` + external recipe ile koşar, `bars_info` Index konvansiyonunda ("symbol" anahtarı yok — Bybit chart'ı kline çekmesin). **MAX butonu (2026-08-01)**: form tarih çiftinin yanında, kaynak-farkında (noktalı id → external, değilse bybit; interval kodu → granularity eşlemesi inline) `/data/range`'i çağırıp doldurur | [[strategy_and_actor]] |
| `web/routes/reports.py` | Backtest Reports — backtest_log.jsonl + robustness_log.jsonl join'li geçmiş tablosu. İstemci-tarafı: hızlı sıralama (değer cache + tek-reflow), kolon filtreleri (sayısal ifade + substring), sayfalama (DOM'da yalnız aktif sayfa), kalıcı görünüm durumu (`reports_layout.json`). **`GET /reports/detail?ts=`**: log satırındaki tam spec + veri kimliğiyle backtest'i sandbox child'ında deterministik yeniden koşar (`run_in_executor`, event loop bloklanmaz) → fiyat grafiği (giriş/çıkış marker + PnL-renkli çizgiler) + trade başına giriş/çıkış sebebi tablosu + sadakat rozeti (log vs yeniden); 8-girişli FIFO cache | [[backtesting_guide]] |
| `web/routes/sessions.py` | **Ucuz ozet (2026-08-04):** `_session_summary` artik her satiri `json.loads` etmez — olay adi ve `ts` satirin ilk 400 baytindan regex ile okunur, tam parse yalniz govdesi gereken dort olay icin (`session_start`/`session_end`/`token_snapshot`/`winner`); beklenmeyen bicimde eski yola dusulur. /sessions soguk render 114 s -> 19 s, fonksiyon A/B ile birebir esdeger ([[nau_performans_denetimi]]). Agent Session Logs — agent_sessions/*.jsonl görüntüleyici **Tear sheet indeksi (2026-08-02):** `backtest_result` olayları dosyadaki sırasıyla `ev_i` ile damgalanır — şablon skora göre sıraladığı için indeks **sıralamadan önce** verilmelidir; bu sayede eski oturum dosyaları da bağlanabilir (ts damgası yalnız yeni koşularda var). | (app-spesifik) |
| `web/routes/wiki.py` | Karpathy wiki frontend. `wiki_path`'in path-traversal guard'ı (`.resolve()` + `relative_to()`, `..` kaçışlarını 404'e çevirir) **artık test ediliyor (2026-08-08)** — daha önce sıfır testti; `tests/test_wiki_helper_and_route.py`'de guard'ın gerçekten yakaladığı elle doğrulandı (guard geçici kaldırılıp test kırıldığı, sonra geri konulup geçtiği gösterildi — httpx `..`'yi istemci tarafında normalize ettiğinden `TestClient` üzerinden test edilemiyor, route fonksiyonu doğrudan çağrılıyor). | (app-spesifik) |
| `web/routes/dashboard.py`, `fragments.py` | UI | (app-spesifik) |
| `web/mission.py` | **AUTO Mission Control render modeli (2026-08-02)** — ajan koşu state'ini (`_AGENT_PROGRESS[run_id]`) kokpit şablonunun bastığı düz sözlüğe çevirir; şablon yalnız sunum, karar burada. Üç bilinçli eşleme: **altı ajan fazı → beş hücre** (`Ranking` ROBUSTNESS'a katlanır — kullanıcı için "seç" ve "doğrula" tek aşama), **duraklatmada halka boşalır sayaç kalır** (`stop_requested` → `—` + 0 dolum, ama `İTERASYON x/N` döngünün nereye geldiğini gösterir; aktif aday kartı silinmez `duraklatıldı` rozetiyle kalır), **konsol etiketleri gerçek `_add_step()` satırlarından türetilmiş kalıplarla** en-özel-önce sıralı (robustness satırı da "backtest" geçirir, blok üretimi de "strateji" geçirir → sıra anlamlı, testte verbatim örneklerle kilitli). Sparkline `_sparkline` ile ≤40 noktaya indirgenir (route'ta da indirgenir — burada savunma derinliği: replay/test state'i ham eğri taşıyabilir). Detay: [[auto_mission_control]] **Model + tear sheet (2026-08-02):** render modeline `model_label`/`model_id` eklendi (BRIEF rayı artık `"default"` yerine gerçek model adını basar; çözüm `agent.model_label` ile SUNUCUDA yapılır, çünkü çözülmüş değer seçimden sapabilir — degrade bayrağı, thread pin'i). Aday kartları `ts` taşır: iterasyonun backtest_log kaydının anahtarı, tear sheet linki bununla açılır; boşsa link basılmaz (kırık link yerine link yok). | [[strategy_and_actor]], [[backtesting_guide]] |
| `web/tearsheet.py` | **Tear sheet render modeli (2026-08-02)** — tek bir backtest'in performans sayfasını normalize eder; `fragments/tearsheet.html` yalnız sunum. Üç farklı depo (backtest_log.jsonl, ajan session log'u, `studio_runs`) farklı metrik şemaları tuttuğu için birleşim alınır: **olmayan metrik tile olarak basılmaz** (ince kaynak on altı em-dash yerine kısa ve dürüst bir ızgara gösterir), **eğriler KPI ızgarasından çıkarılır** (5000 noktalı dizi tile değildir) ve **tarihsiz eğride aylık ısı haritası çizilmez, sebebi `notes`'a yazılır**. Ton kuralı: drawdown ve maliyet asla "iyi" renklenmez, profit factor eşiği 1.0. `""` = uygulama varsayılanı mantığının kardeşi: model adı gibi burada da kullanıcıya çözülmüş değer gösterilir. Kararlar: [[tear_sheet_overlay]] | [[backtesting_guide]] |
| `web/routes/tearsheet.py` | **GET /tearsheet (2026-08-02)** — her backtest listesinin bağlandığı salt-okunur overlay uç noktası. `src` çözücüyü seçer: `log&ts` (Reports, Studio run history, AUTO kokpiti, Lab, Dashboard iterasyonları), `session&run_id&i` (Session Logs — olay dosyasındaki backtest_result sırası; şablon skora göre sıraladığı için indeks **sıralamadan önce** damgalanır), `strategy&run_id` / `suggestion&id` (Strategy Builder koşuları ve AI deneme backtest'leri). **Yeniden koşu yok**: her depo metrikleri ve equity eğrisini zaten sakladığı için sayfa anında açılır — bu, stratejiyi sandbox'ta bilerek replay eden `/reports/detail`'den ayrı bir uç nokta olmasının tek sebebi. Log çözücü rotasyon arşivini (`.jsonl.1`) de tarar. Kararlar: [[tear_sheet_overlay]] | [[backtesting_guide]] |
| `web/shared.py` | **Ortak web altyapısı (leaf modül)**: `ProgressStore` (progress dict + lock + kapasiteli done-first eviction) + `ChatStore` (conv_id→state, TTL+oldest-first eviction, get/commit lock-disiplini) + log/chart yardımcıları (`log_backtest`/`log_robustness`/`chart_url`/`rotate_if_large`/`sanitize_floats`) + **`render_md(txt, extensions=…)`** (strategy/backtest/wiki route'larındaki 3 kopya buraya indi; wiki `toc` varyantını param'la korur) + **`session_id`/`SESSION_COOKIE`** (nautlab_sid cookie helper'ı strategy.py'den taşındı — backtest.py aynı session boyutunu paylaşsın diye). Route'lar buradan import eder, bu hiçbir route'u import etmez (routes→shared tek yönlü) **`log_backtest()` artık yazdığı kaydın `ts`'ini DÖNDÜRÜR (2026-08-02)** — log'un kimlik anahtarı. Kendi satırını tutan çağıranlar (AUTO döngüsü, Lab, manuel koşu, Dashboard loop'u) bunu saklayınca tear sheet linki birebir olur; metrik üzerinden geriye dönük eşleştirmeye gerek kalmaz. | [[crash_only_design]], (app-spesifik) |

### Domain (Nautilus yüzeyi)

| Modül | Rol | Wiki karşılığı |
|---|---|---|
| `backtest.py` | `BacktestEngine` wrapper + `BacktestNode` yolu; `NAUTILUS_DEBUG_LOG` env var ile iç loglama açılabiliyor. Enstrüman spec'leri NAU universe.yaml'a pinli: `_BYBIT_SPECS` per-symbol precision (BTC/ETH sp3, SOL sp2, XRP sp1), kategori-başına venue (`BYBIT_SPOT`/`BYBIT_LINEAR`/`BYBIT_INVERSE` — katalog anahtarları ayrık kalır), `_make_index_instrument` pp2/tick 0.01. **Komisyon per-enstrüman** (`_fee_model_for`): kripto `CurrencyPair` → `MakerTakerFeeModel` (per-symbol `_BYBIT_FEES_BPS`, taban global BYBIT_*_FEE_BPS); US hisse/ETF `Equity` (ör. QQQ) → **`IBFixedFeeModel`** — Interactive Brokers Pro Fixed ($0.005/hisse, min $1/emir, %1 tavan; `max(per-share,min)` sonra `min(…, %1·notional)`). `_extract_trades` her trade'e **giriş/çıkış sebebi** ekler: positions raporundaki `opening/closing_order_id` → fills raporu emir tag'leri (`dr:<seq>`/`xr:<seq>`/`sl`/`tp`/`flip`/`eob`) → composer karar günlüğü join'i (fills lookup tek-geçiş dict). **`allow_short` desteği** (`run_backtest`): `params["allow_short"]=True` olduğunda venue `AccountType.MARGIN` ile açılır (short pozisyon açmak için zorunlu); aksi halde `AccountType.CASH`. **Başlangıç sermayesi + komisyon override** (`run_backtest(initial_capital, commission_bps_override)`): `initial_capital` verilirse `STARTING_CASH` yerine hesap bakiyesi olur; `commission_bps_override` verilirse (2026-07-20) Bybit dalında bu override `_make_bybit_instrument(fee_bps_override=…)` ile **doğrudan instrument'a baked** edilir (`maker_bps=taker_bps=override`); `run_composed_backtest` artık ayrı bir `MakerTakerFeeModel` KURMAZ — `_fee = _fee_model_for(instrument)` ile fee model fill anında override'lı fee'yi instrument'tan okur (v2.x; eskiden run içinde koşulsuz bir `MakerTakerFeeModel(maker=taker=bps/10_000)` kuruluyordu). `_metrics(starting_cash=…)` de aynı sermayeyi equity eğrisi + per-trade Sharpe normalizasyonunda kullanır (varsayılan `STARTING_CASH`). **Perf**: `_bars_from_df` satır-satır `append` döngüsü yerine vektörize `ts_close = ts_ns + interval_ns` + list-comprehension ile Bar üretir (davranış birebir aynı). | [[backtest_node]], [[backtesting_guide]], [[environment_contexts]], [[data_wranglers]], [[precision_modes]], [[portfolio]], [[v1_to_v2_migration_lessons]], [[index_backtest_via_equity_proxy]] |
| `strategies.py` | `MACrossoverStrategy` + `RSIMeanReversionStrategy` (fixed trade size, tek pozisyon, market emirleri; LLM ajanı yalnızca sayısal parametre önerir, `STRATEGY_REGISTRY`/`STRATEGY_PARAM_SPEC` ile kayıtlı). **`VolTargetedTrendStrategy` kaldırıldı (2026-07-19)** — EWMA volatilite hedefli pozisyon boyutlandırma artık composer motorunun `trade_size_mode="vol_target"` sizing modu ([[vol_targeted_trend]], `ComposedStrategy._compute_qty`). **Perf**: `_iid`/`_bt`/`_fixed_qty` `on_start`'ta cache'lenir; `MACrossoverStrategy` incremental `_fast_sum`/`_slow_sum` ile O(1) MA; 4096 bar'da bir `math.fsum` ile float drift resetlenir; `positions_open()` yalnız sinyal dalında | [[strategy_and_actor]] hiyerarşisi (Actor + Strategy + Config), [[v1_to_v2_migration_lessons]] StrategyConfig plain-class kalıbı, [[vol_targeted_trend]] |
| `strategy_studio/` | Kurucunun domain katmanı: `schema.py` (sürümlü pydantic şema), `store.py` (kendi SQLite'ı — uygulamada başka SQLite yok), `mutations.py` (insan ve AI düzenlemeleri AYNI yoldan), `compiler.py` (→ nötr `CompiledStrategy`), `registry.py` (15 indikatör; 8'i `indicators.py` fonksiyonlarına `impl(bars, **schema_params)` sözleşmesiyle bağlı), `backtest.py` (`to_nautilus` + `NautilusBacktestAdapter` + stub; `run(compiled, window=Window(start, end, embargo_bars))` ile örneklemin kesri koşulabilir — pencereli koşu kendi fold tablosunu üretmez), `optimizer.py` (**gerçek walk-forward, 2026-07-25**: ankrajlı IS eleme + purged OOS fold'ları, sıralama `mean − 0.5·std`, `dsr` deneme sayısıyla deflate edilir), `ai.py` (Anthropic istemcisi), `deploy.py` (**koşulabilir artifact, 2026-07-25**: `to_nautilus`'un indirdiği `ComposedStrategySpec` serileşir — eskiden yalnız koşul SAYIMLARI vardı; indirilemeyen strateji deploy edilemez), `runner.py` (**paper runner**: artifact → sandbox `TradingNode`, canlı Bybit verisi + `SandboxExecutionClient`, `STUDIO_RUNNER=paper` ile opt-in; `environment='live'` reddedilir). **Sessiz atlama yasağı**: composer spec'ine sadakatle çevrilemeyen her şey `UnsupportedStrategy` ile tüm gerekçeleriyle reddedilir — ekrandakinden farklı bir stratejinin metriğini döndürmektense koşuyu başlatmamak. Detay: [[strategy_studio]] `store.get_run(run_id)` + `store.runs(strategy_id)` (2026-08-02): tek koşuyu id ile okuma ve koşu geçmişi — tear sheet overlay'inin `src=strategy` çözücüsü. | [[strategy_and_actor]], [[backtesting_guide]], [[portfolio]] |
| `composer.py` | Block-based `ComposedStrategy` — **13 builtin blok** (`ma_cross`, `rsi_threshold`, `price_breakout`, `momentum`, `volume_spike`, `ema_cross`, `bollinger_break`, `macd_cross`, `atr_stop`, `adx_threshold`, `stoch_rsi_cross`, `wave_trend_cross`, `donchian_channel`). **`build_spec(**fields)` spec-upsert servisi (2026-07-21)**: `POST /strategy/save` (form) ve `/backtest/describe` (Claude NL) çağrılarının HER İKİSİNİN ortak evi — eskiden iki ayrı `ComposedStrategySpec` constructor'ı kendi inline clamp'leriyle spec kuruyordu ve **çelişiyorlardı**: save `vol_target` mode'unu sessizce `fixed`'e düşürüyordu (whitelist'te yoktu → vol_target spec'i round-trip edemiyordu, aktif bug), describe bracket/order/trend alanlarını atlıyordu. `build_spec` iki tarafın alanlarının UNION'ını kabul eder + tüm clamp/whitelist'i merkezîleştirir (`trade_size_mode` artık `vol_target` dahil); `_as_bool` HTML-form bool coercion'ı tek deyime indirir. Son 4 blok NAU parite indikatör kütüphanesi `indicators.py` üstünde kurulu (`ind` modülü olarak enjekte); bloklar `closes` + `indicators["volumes"]/["highs"]/["lows"]` (gerçek OHLC serileri) görür. **NAU_WINDOW=260 sabit pencere**: recursive (Wilder/EMA-tohumlu) `adx_threshold`/`stoch_rsi_cross`/`wave_trend_cross` blokları `_nau_win` ile son 260 barda hesaplar; bu bloklar spec'te varsa buffer ≥260 tutulur (kompaksiyon süreksizliği → sahte kesişim önlenir). **Flip yolu**: `allow_short=True` iken ters sinyal `_cancel_working()` + `close_all_positions(tags=['flip'])` sonrası ters taraf açar (M17 flat-kalma guard'ı); her flat girişte de dolmamış GTC limit iptal edilir (birikme yok). **Karar günlüğü** (`_decision_log`): her giriş/çıkış kararında ateşleyen blok(lar) + params + indikatör anlık değeri kaydedilir, emirlere `dr:`/`xr:` tag'i basılır (SL/TP/flip/eob tag'leri) → backtest sonrası sebep join'i. **2-TF trend filtresi**: `spec.trend_filter` iken `ComposedStrategy` `secondary_bar_type`'ı (üst TF, `trend_interval`) subscribe eder; üst-TF kapanışlarından `trend_ema_period` EMA hesaplayıp `_trend_bias` ile ana-TF girişlerini kapıya sokar (look-ahead güvenli — üst-TF barı yalnız KAPANDIĞINDA `on_bar`'a gelir). Perf: `_closes`/`_volumes` düz-list buffer (bar başına tam kopya yok), `_current_equity` hızlı-yol cache (`_equity_mode`) — hepsi sonuç-birebir. **`_current_equity` düzeltmesi (2026-07-25)**: `Portfolio.equity()` `dict[Currency, Money]` döndürür (skaler değil); `float(dict)` TypeError'ı geniş `except` tarafından yutuluyor ve kod bakiye taramasına düşüyordu — CASH hesapta o bakiye HARCANMAMIŞ NAKİT olduğundan (BTC alınca USDT düşer) bar-başına MTM equity serisi pozisyon açık kaldığı sürece çöküyordu (180g BTCUSDT 1h: min 588 → 9899, `max_dd` -%94.27 → -%2.99). Artık settlement para birimi girdisi okunuyor (`_settlement_ccy_code`: hesap base_currency → instrument settlement/quote). Seri `_metrics()`'in `max_dd`/`sharpe` alanlarını beslediğinden etki /backtest + robustness + WFO çıktılarının tamamındaydı — ayrıntı [[portfolio]] | [[strategy_and_actor]] altında custom Strategy; emir çıkışı [[order_flow_pipeline]]'a girer |
| `custom_block_store.py` | Kullanıcı bloklarının on-disk kayıt defteri (`registry.json` + blok başına `.py`). **Dayanıklılık sözleşmesi (2026-07-25)**: I/O hatası ≠ bozulma — okuma hatası kısa aralıklarla yeniden denenir, hâlâ başarısızsa `RegistryUnavailable` **fırlatılır** (asla "boş registry" diye raporlanmaz); yalnız gerçek *parse* hatası `.json.bak` karantinasına yol açar. Eskiden `except Exception` her hatada dosyayı yeniden adlandırıyordu → Windows'ta eşzamanlı `save_custom` sırasındaki anlık sharing-violation tüm kayıtları siliyor, ardından `load_catalog` özel-blok stratejilerini budayıp diske yazıyordu (kalıcı strateji kaybı). `list_custom`/`get_custom` artık yazarlarla aynı `_STORE_LOCK` altında okur; registry yazımı UTF-8 sabit. Bkz. [[nau_guvenlik_dayaniklilik_duzeltmeleri]] | [[strategy_and_actor]], [[nau_guvenlik_dayaniklilik_duzeltmeleri]] |
| `codegate.py` | **AI-üretilen blok kodunun güvenlik kapısı**: AST beyaz listesi (izinli düğüm/builtin/attribute/modül), dunder + alt-çizgi yasağı, subscript/alias-callee bypass reddi, `compile_with_loop_budget` (5M adım sonrası `RuntimeError` — sonsuz döngü daemon-thread sızdırmaz), `safe_builtins()` (yükleyici + smoke aynı kısıtlı `__builtins__`). **Yazma-kaçışı kapatıldı (2026-07-25)**: attribute kontrolü artık `ctx`'e de bakar — `Load` değilse reddedilir; ek olarak `safe_module_proxy` ile `math`/`statistics`/`ind` namespace'e **canlı modül değil salt-okunur proxy** olarak enjekte edilir (eskiden `ind.calc_rsi = ...` yazan bir blok, sunucu süreci ömrü boyunca tüm stratejilerin göstergesini değiştirebiliyordu; önizleme/smoke yolu sandbox subprocess sınırının dışındadır). Proxy'nin ilettiği ad kümesi ile AST'nin izin verdiği küme aynı `_ALLOWED_ATTRS` kaynağından beslenir — ayrışamazlar. **Maliyet ekseni kapatıldı (2026-08-02)**: beyaz liste "hangi ad" ve "okuma mı yazma mı" sorularını sorup "ne kadar pahalıya" sorusunu hiç sormuyordu. İki kural eklendi — (a) `MAX_POW_EXPONENT = 64`: `**` üssü küçük bir sayısal literal olmalı, hesaplanmış üs reddedilir (`9 ** 9 ** 9` sınıfı; tek kesintisiz bytecode GIL'i bırakmadığı için smoke'un `join(timeout=2.0)`'ı yapısal olarak kesemiyordu — ölçüldü: 2 s vaat, 8.8 s gerçek, hata yok); (b) `MAX_LITERAL_MAGNITUDE = 1_000_000` + `_fold_literal`: literal aritmetiği doğrulama anında katlanır, herhangi bir ARA değer tavanı aşarsa reddedilir. (b) olmadan (a) delinebiliyor — `((10**64)**64)**64` içindeki her `**` literal 64 üssü taşır ve (a)'dan geçer, değer 10\*\*262144'tür (dört seviye = 14,5 s ölçüldü); aynı boşluk `list(range(10**9))` / `[0] * 10**9` gibi döngü düğümü olmayan konteyner bombalarını da kapsıyordu. Sabit ifadeler ayrıca `compile()` içinde katlandığı için guard kurulmadan ÇAĞIRAN thread'de koşuyordu — bu, `composer._load_module_from_path` üzerinden sunucu AÇILIŞINI de asabilirdi. Aşağıdan-yukarı reddetme doğrulamayı ucuz tutar (en büyük çarpım 1e6\*\*64). Tavan veriyle kalibre edildi: diskteki 268 bloğun en büyük literali 9999, 268/268 hâlâ geçiyor; bombalar ~0,1 ms'de reddediliyor. Testler: `tests/test_guards_and_indicators.py::TestCostGate`. Bkz. [[nau_guvenlik_dayaniklilik_duzeltmeleri]] | [[strategy_and_actor]], [[nau_guvenlik_dayaniklilik_duzeltmeleri]] |

### Motor katmanı (izolasyon + paralel yürütme)

| Modül | Rol | Wiki karşılığı |
|---|---|---|
| `sandbox.py` | Öldürülebilir process sandbox: `run_backtest_guarded` (custom-block veya `force_subprocess=True` → tek killable child, 180s timeout (`DEFAULT_TIMEOUT_S`); Nautilus backtest'i GIL'i tuttuğu için sunucu event loop'unu dondurmasın diye), `run_robustness_guarded` (tüm robustness suite'i tek child'da, 900s; child NON-daemon — kendi worker havuzunu açabilsin), `_start_parent_watchdog` (sunucu ölürse child ≤1s'de self-exit). Child entry `recipe`'den `initial_capital` + `commission_bps_override`'ı `run_backtest`'e iletir (subprocess yolunda da özel sermaye/komisyon onurlandırılır). **Hızlı yol paritesi (2026-07-25)**: in-process fast path da artık aynı iki `recipe` anahtarını geçirir — eskiden yalnız child yolu geçiriyordu, yani "eşdeğer" belgelenen iki yol sessizce ayrışıyordu (ucuz yolu kullanan çağıran varsayılan sermaye/komisyonla koşardı). **Graceful-exit koruması**: çalışan non-daemon child'lar bir `atexit` handler'ıyla (mp'nin atexit join'inden önce, LIFO) terminate edilir — aksi halde zarif shutdown mp'nin join'inde kilitlenirdi. **pm2 spawn donması düzeltmesi (2026-08-01)**: pm2 (konsolsuz parent) altında konsol-alt-sistemi `python.exe` spawn çocukları Python daha yüklenmeden konsol/loader init'te donuyordu (~2MB WS, 0 CPU; 1KB yükle bile ürer, bash'te aynı prob <1 sn; `pm2 update` çare değil) → parent `Process.start()` → `reduction.dump`'ta süresiz bloklanır ve timeout bekçisi start() SONRASI kurulduğundan hiç devreye girmez: "backtest asılı ama backtest_log'da satır yok" imzası, py-spy ile teşhis. Çözüm: modül-seviye `mp.set_executable(pythonw.exe)` (yalnız `PM2_HOME` işaretli headless ortamda; kill-switch `NAUTILUS_SPAWN_PYW=0`) + dört child hedefinin ortak `_child_stdio_guard()`'ı — pythonw çocuğu `sys.stdout/stderr=None` ve geçersiz fd 1/2 ile açılır, guard her iki katmanı devnull'a bağlar (`open(os.devnull)` + `os.dup2`), konsollu çocukta ise eski UTF-8 reconfigure davranışı korunur. Uçtan uca doğrulama: pm2 altında gerçek AUTO backtest 5.5 sn'de hatasız; probe `_run_in_child` 5MB yükü 0.08 sn'de taşıdı. İkinci-beyin sayfası: spawn_start_el_sikismasi_bekciden_once_donar | [[crash_only_design]], [[single_threaded_core]] |
| `backtest_robustness.py` | Robustness motoru: `run_walk_forward` / `run_insample_oos_split` / `run_multi_symbol` (+ vektörize `run_monte_carlo`). Üçü de opsiyonel `run_many=` callable ile birim backtest'leri havuza fan-out eder; `_wfo_window_bounds` pencere matematiğini iki yol için paylaşır | [[backtesting_guide]], [[portfolio]] |
| `parallel_exec.py` | `ProcessPoolExecutor` (spawn) havuzu: WFO (pencere × aday) birimlerini çekirdeklere dağıtır. Veri bir kez parquet snapshot'a yazılır, worker initializer bir kez yükler (per-task pickle yok); her worker parent-liveness watchdog taşır (yetim süreç yok). Env: `NAUTILUS_PARALLEL=0` kill-switch, `NAUTILUS_PARALLEL_WORKERS` override. **Worker count (2026-07-19)**: `min(cpu - 2, avail_RAM / per_worker_GB)` — eski `cpu//2-2` yerine hem CPU hem RAM kapasitesine göre; `NAUTILUS_WORKER_MEM_GB` (varsayılan 1.5) per-worker bellek bütçesi; `psutil` varsa `available` bellekten, yoksa POSIX `sysconf` (toplam RAM'in ~%70'i) ile RAM cap uygulanır. Determinizm: aday üretimi (seed'li RNG) parent'ta — kazananlar sıralı yolla birebir aynı. **Zombie reap (2026-07-25)**: batch timeout'unda havuz yeniden kurulurken eski süreçler yalnız `terminate()` ediliyordu; artık `terminate → join(5) → kill → join(5)` — her timeout'ta worker başına bir zombie/handle sızıntısı birikmiyor | [[crash_only_design]] |
| `wfo_optimizer.py` | WFO parametre arama primitifleri: `build_param_space`, `mutate_spec`, `objective_value`, hafif GA (`ga_plan`/`ga_next_population`/turnuva/elit — deterministik seed'li, sıralı ve paralel yol birebir aynı adaylar), `optimize_window`. **NAU skorlama paritesi**: `objective_value` sharpe objektifi **per-trade sharpe** ((mean/std)×√n) okur — annualized 252-gün DEĞİL; `_calmar` DD_FLOOR=0.01 + CALMAR_CAP=±10; fold-kabul `WF_MIN_VALID_FOLDS_FRAC=0.6`, embargo `WF_EMBARGO_DAYS=2`. **Default budget (2026-07-19)**: `WFO_POP_SIZE=4` (eski 8; GEN≈n_samples/POP ile dengelenir), `WF_FOLDS=3` (2'ye düşürülmeydi ama `FOLDS=2` ile `MIN_VALID_FRAC=0.6` → her iki fold zorunlu — tolerans sıfırlanır; 3'te kalıyor). **Studio bunu yeniden KULLANMAZ** (2026-07-25): buradaki GA `BLOCK_REGISTRY` sınırlarından uzay türetir, studio'nun arama uzayı ise kullanıcının kural ağacına yazdığı min/step/max — GA sessizce ekrandaki aralıkların dışını optimize ederdi. Paylaşılan yalnız gelenekler: `penalized_score` (mean − 0.5·std), geçerli-fold oranı 0.6, işlem sayısı sönümlemesi `n/(n+20)`. Bkz. [[strategy_studio]] | [[backtesting_guide]], [[strategy_studio]] |

### Data

| Modül | Rol | Wiki karşılığı |
|---|---|---|
| `data.py` | Yahoo BTC-USD, Bybit v5 klines, US index tick CSVs. Ayrıca **harici salt-okunur Nautilus katalogları**: `EXTERNAL_CATALOGS` (`NAUTILUS_EXTERNAL_CATALOGS` env, varsayılan NAU_ev — 591 US-equity; **varsayılan kök 2026-07-28'de düzeltildi**: `E:\myAI_Projects\NAU_ev\...` → `D:\NAU_ev\...` — masa sürücü taşımıştı, bayat yol /data panelini, Lab picker'larını ve QQQ/SPX yüklerini sessizce boşaltıyordu; ayrıntı [[nau_guvenlik_dayaniklilik_duzeltmeleri]] §9.4), `/data` sayfasında `xq` filtreli panel (`_external_catalog_rows` — bar sayıları parquet footer'dan, kopyasız) ve `load_external_bars` ile /backtest'te tek-atışlık External kaynak (kapanış→açılış kaydırması; cache `~/.cache/.../external/`, harici köke asla yazılmaz). **Index satırları da Nautilus katalog durumunu bildirir** (`_index_rows` → `nautilus_catalog_bar_state` ile `in_nautilus_catalog`/`catalog_rows`; Bybit satırlarıyla parite) → `/data` birleşik "enstrüman kartı" (üç kaynak da ortak `.tf-grid` zaman-ızgarası + durum sözlüğü: önbellek/✓katalog/getir/desteklenmiyor/salt-okunur), `→ Catalog` yazımı `write_to_nautilus_catalog` (idempotent, delete→write guard). **Dinamik sembol listeleri**: `list_catalog_bybit_symbols()` — katalog `data/bar/` altındaki dizin adlarından `{symbol, category}` listesi (BYBIT_LINEAR/SPOT/INVERSE); `_catalog_index_symbols()` (backtest route'unda) — non-Bybit venue sembolleri (ör. QQQ.POLYGON); her ikisi de yeni sembol eklendiğinde otomatik güncellenir, sabit tuple gerekmez. **Perf (2026-07-19)**: `_read_parquet_stats` artık tam `pd.read_parquet` decode yerine **`pq.read_metadata` footer okuma** kullanıyor (3 skaler için timestamp-index col stats, ~4KB footer; `/data` sayfa yüklemesinde 0.95s→0.01s, 89×); timestamp kolonu magic `__index_level_0__` adı yerine **logical-type** (`pa.types.is_timestamp`) ile bulunur, stats yoksa full decode'a düşer ve `log.debug` ile gözlemlenebilir; boş tarih aralığında bu footer stats kullanılır (parquet'i iki kez decode etmez). Bybit sayfa fetch'indeki koşulsuz 150ms `sleep` kaldırıldı (rate-limit retry backoff zaten var); `_get_bybit_session()` thread-local `requests.Session` ile TCP+TLS connection reuse. **Missing-day sözleşmesi (2026-07-19)**: `_stream_ticker_rows` boş/bozuk gzip'te (awk hiç çıktı üretmezse — başlık satırı bile yoksa) `pd.read_csv`'in fırlattığı `pd.errors.EmptyDataError`'ü yakalar ve **boş tipli frame** (`columns=["ticker","value","timestamp"]`) döndürür — böylece bir bozuk gün tüm yüklemeyi çökertmek yerine sessizce eksik-gün gibi degrade olur (regresyon: `tests/test_index_stream_empty.py`). **Index kaynağı NFS-bağımlı + baypas (2026-07-20)**: `load_index_bars` cache'te olmayan günleri (`missing_days`) tek tek `INDEX_ROOT` (`NAUTILUS_INDEX_ROOT` env, varsayılan `/Users/…/Z/us_indices/values_v1` — bir **NFS mount**) altından `_stream_ticker_rows` ile awk/gunzip stream ederek doldurur. Mount **erişilemez/asılı** (stale NFS handle) olduğunda her eksik gün için `_index_file_for(day).exists()`/gzip stream süresiz bloke olur → geniş tarih aralıklı bir ticker'da (ör. QQQ/15m, 2003→2026, ~3900 eksik gün) `load_index_bars` dakikalarca askıda kalır ve UI "Reading data · <ticker>/<gran>…" adımında **sonsuza kadar takılı** görünür (`/backtest/run` Index dalı bu çağrıyı senkron yapar). **Baypas**: gerçek veriye ihtiyaç yoksa `NAUTILUS_INDEX_ROOT`'u **var olmayan yerel bir yola** işaret ettir (`~/.cache/nautilus_web_app/_no_index_source`) → `src.exists()` anında `False`, hiç stream olmaz, backtest yalnızca cache'teki bar'larla çalışır (ör. QQQ/15m 247k bar cache'ten → `load_index_bars` 180s+ → 0.59s). Ortama-özgü ayar; `.claude/settings.local.json` env + `~/.zshrc` export ile kalıcı. Mount tekrar erişilebilir olursa baypas kaldırılır. **Bybit TF seti genişletildi (2026-07-23)**: interval'lar artık `1/5/15/30/60/240/720/D` (30m + 12h yeni; **45m bilinçli yok** — Bybit v5 kline API'si 45 dakikayı sunmuyor). Senkron güncellenen kapalı-set kopyaları: `BybitInterval` Literal + `_BYBIT_MS` + `BYBIT_ALL_INTERVALS` (data.py), `_make_bybit_bar_type` + `_BYBIT_TO_DSL` (backtest.py — `30-MINUTE`/`12-HOUR` DSL), `_SEC_PER_BAR`/`ms_per_bar` (chart.py). Yeni interval eklerken BU DÖRT NOKTA birlikte değişmeli. **Kendi equity ingest kökü (2026-07-28)**: `EQUITY_CATALOG_DIR = ~/.cache/nautilus_web_app/equity_catalog` — `ingest_equities.py`'nin yazdığı kök, VAR OLDUĞUNDA `EXTERNAL_CATALOGS` listesine sona eklenir (env ayarlıyken de; kendi ingest'inin panelden kaybolması en büyük sürpriz olurdu). Sona eklenmesi bilinçli: aynı enstrüman iki kökte varsa /data dedup'ı (`setdefault`) ilk kökü, yani NAU_ev'in adjusted sürümünü kazandırır. NAUTILUS_CATALOG_DIR DEĞİL çünkü `data.py rebuild` orayı siler. Aynı gün iki ek: `EXTERNAL_GRAN_BY_BYBIT_CODE` (Bybit interval kodu → external granularity DSL; studio adaptörü + Lab paylaşır, 30m/12h bilinçli yok) ve /data external panelinde **kullanılan-önce sıralama** (`EXTERNAL_CACHE_DIR`'de pandas cache'i olan enstrümanlar başa, satırda `used` rozeti — alfabetik ilk 50 kimsenin istemediği ticker'larla doluyordu). **Panel başlığı artık gerçek kaynağı yansıtıyor (2026-08-08 DeepR)**: "External Nautilus Catalog (NAU_ev)" sabitti — NAU_ev kökü yokken `EQUITY_CATALOG_DIR` paneli sessizce dolduruyordu ama başlık hâlâ "NAU_ev" diyordu; `_external_catalog_source_label()` artık hangi kök(ler)in gerçekten aktif olduğuna göre "NAU_ev" / "local ingest" / "NAU_ev + local ingest" / "not found" döndürüyor. | [[data_engine]] Layer-1 ingest bacağı; per-source parquet cache = küçük [[parquet_data_catalog]] analogu; `_prepare_df` OHLCV normalizasyonu; [[bar_aggregation_and_type_syntax]] `BarType` string üretimi burada; [[index_backtest_via_equity_proxy]] tick→bar resample deseni |
| `ingest_equities.py` | **US-equity ingest CLI (2026-07-28)** — NAU_ev `tools/ingest_flatfiles.py` + `tools/build_tf_bars.py` portu: `E:\MarketData\massive-flatfiles\us_stocks_sip` (Massive/Polygon, ~12.400 ticker, tarih-partisyonlu) arşivinden hedef ticker'ların 1-MINUTE bar'larını yıl-parçalı akışla `EQUITY_CATALOG_DIR`'a yazar, ardından AYNI koşumda RTH (09:30–16:00 NY) resample ile 5m/15m/1h/4h/1d türetir (right-label: minute ts = window_start+60e9; TF sağ kenar). Porttaki üç bilinçli fark: (1) hedef NAU_ev kataloğu değil bu projenin kendi kökü — başka projenin veri klasörüne yazılmaz; (2) universe.yaml dışlaması yerine "başka external kökte olan bare-ticker atlanır" guard'ı (adjusted sürüm orada daha iyi, /data'da çift kayıt olmaz; `--force` geçer); (3) TF üretimi ayrı CLI değil ikinci faz — "minute yazıldı TF unutuldu" ara durumu yok, NAU_ev'in manifest-tazeliği makinesine gerek kalmadı. Manifest EN SONDA yazılır (yarım koşum manifest'e yazmaz), `adjusted: false` + `source: flatfile:minute_aggs` — data.py M21 okuyucusu bunu /data rozetine çevirir, `load_external_bars` UNADJUSTED uyarısı basar. Ölçüldü: AA 2026 tek yılı 109 s / 58.988 minute bar; sentetik-arşiv testleri right-label, RTH dışlama (premarket 1-DAY'e girmez), guard ve taze-yeniden-ingest'i kilitler | [[parquet_data_catalog]] `write_data` hedefi; [[bar_aggregation_and_type_syntax]] BarType DSL üretimi; [[instruments]] `Equity` tanımı (precision 2); [[index_backtest_via_equity_proxy]] RTH resample deseninin hisse tarafı |
| `download_flatfiles.py` | **Flat-file S3 aynası (2026-08-03)** — `ingest_equities.py`'nin OKUDUĞU yerel arşivi kuran adım: Massive'in S3 ucundan (`files.massive.com`, bucket `flatfiles`) günlük `.csv.gz` ağacını `E:\MarketData\massive-flatfiles` altına indirir, **yerel yol = S3 key yolu** (dataset eklendikçe ingest tarafındaki yol kuralı değişmez). Üç sözleşme: (1) yarım dosya asıl adıyla kalmaz — `.part`'a inilir, tamamlanınca `os.replace` ile atomik taşınır; (2) yeniden koşum boyutu S3'tekiyle aynı olan dosyayı atlar, tutmayanı (yarım kalmış) yeniden indirir — 119 GB'lık aynanın kesintiden sonra baştan inmemesi bu ikisine bağlı; (3) **403 yeniden denenmez**: `FlatFileError` ile koşum durur, çünkü bu ağ hatası değil abonelik kapsamı. **Ölçülen kapsam ayrımı (2026-08-03)**: aynı kimlik bilgileriyle LIST her dataset için 200 ama GET yalnız `us_indices/` için 200; `us_stocks_sip/` (day/minute/trades/quotes), crypto, forex, futures, options **403 `NOT_AUTHORIZED`** — listeleme aboneliğe bağlı değil, indirme bağlı. Hız: tek akış 0,9 MB/s ölçüldü ama gerçek ayna **48,5 MB/s** koştu (boto3 `download_file` dosya başına multipart eşzamanlılık kuruyor; tek-akış probu toplam kapasiteyi ölçmüyor) → `us_indices/minute_aggs_v1` 904 dosya / 119,4 GB ~41 dk. `boto3` yeni bağımlılık | [[index_backtest_via_equity_proxy]] index veri kaynağının besleme ucu; [[parquet_data_catalog]] ingest'in nihai hedefi |
| `download_massive.py` | **US-equity REST indiricisi (2026-08-03)** — `ingest_equities.py`'nin kardeşi: aynı hedef kök (`EQUITY_CATALOG_DIR`), aynı right-label sözleşmesi, aynı Faz B/C (RTH TF resample → manifest en sonda) — yalnız Faz A'nın kaynağı değişir: 78 GB'lık yerel flat-file arşivi yerine Massive (Polygon'un yeni markası; `api.massive.com` ve `api.polygon.io` aynı yanıtı verir) `/v2/aggs/ticker/{tk}/range/1/minute/...` ucu. Dört sözleşme: (1) veri **ADJUSTED** (`adjusted=true` varsayılan, `--unadjusted` kapatır) → manifest `adjusted: true` + `source: massive:rest_aggs`, /data UNADJUSTED uyarısı basmaz — bu, flat-file yolunun bilinen zayıflığının (split sıçraması) REST karşılığındaki çözümü; `write_manifest` bu yüzden `adjusted`/`source`/`note` parametreleri aldı, iki kaynak tek manifest yazıcısını paylaşır. (2) Anahtar yalnız `MASSIVE_API_KEY`/`POLYGON_API_KEY` ortam değişkeninden okunur ve `Authorization: Bearer` başlığıyla gider — repoda ve URL/loglarda görünmez. (3) **Plan kotası akışı belirler, koşumu düşürmez**: kotanın dışındaki yıl 403 `NOT_AUTHORIZED` döner, o yıl "plan dışı" diye loglanıp atlanır ve manifest yalnız gerçekten inen aralığı yazar — yarım veri tam görünmez. (4) `_Pacer` istekler arası `60/rpm` sn bekletir, 429'da `Retry-After`/üstel geri çekilme (ücretsiz anahtar: **5 istek/dk**, dakika-bar geçmişi **~2 yıl**; ücretli anahtarda `--rpm 0`). ms→ns dönüşümü: `t*1e6 + 60e9` (pencere BAŞI → KAPANIŞ). Ölçüldü: HOOD 2026-07 → 16.337 minute bar / ~2 s, türetilen 1-DAY etiketleri flat-file'dan gelen AA ile birebir aynı konvansiyonda | [[parquet_data_catalog]] `write_data` hedefi; [[bar_aggregation_and_type_syntax]] BarType DSL; [[instruments]] `Equity` tanımı; [[index_backtest_via_equity_proxy]] RTH resample deseni |

### Runtime

| Modül | Rol | Wiki karşılığı |
|---|---|---|
| `state.py` | In-memory session state (`IterationResult` listesi) `IterationResult.log_ts` (2026-08-02): iterasyonun backtest_log damgası — Dashboard satırının tear sheet anahtarı; `""` ise link gizlenir. | [[cache]] (ephemeral, single-source-of-truth, restart'ta boş) |
| `loop_runner.py` | Background loop: `agent`- veya `catalog`-mode; `symbol/category/interval` parametreli. **`bars_df` artık her iterasyonda tazeleniyor (2026-08-08)**: eskiden `/loop/start`'ın tek seferlik `server._context["bars"]` snapshot'ı `run_loop`'a sabit referans olarak geçiyordu — loop günlerce açık kalırsa hep aynı bayat pencereyi backtest ediyordu (uygulamanın diğer TÜM veri yollarından — backtest/robustness/lab/chart/reports — farklı olarak, onların hepsi her istekte taze `load_bybit_bars` çağırır). Şimdi döngünün başında `load_bybit_bars(symbol, interval, category)` tekrar çağrılıyor; fetch başarısız olursa (ağ kopması) önceki `bars_df` korunup `log.warning` yazılıyor, döngü çökmüyor — bkz. [[nau_deepr_toplu_sertlestirme_2026_08]]. | [[crash_only_design]] (her iterasyon idempotent; engine yeniden yaratılır), [[nau_deepr_toplu_sertlestirme_2026_08]] |
| `agent.py` | **EFFORT kolu (2026-08-04):** `--effort` artık CLI çağrısına geçiyor — `set_thread_effort()`/`current_effort()` model piniyle aynı thread-yerel desende, seviyeler `low|medium|high|xhigh|max`, süreç varsayılanı `NAUTILUS_LLM_EFFORT`; seçilmediğinde bayrak HİÇ geçilmez (ucun kendi varsayılanı korunur). OpenRouter yolunda karşılığı `extra_body={"reasoning": {"effort": …}}`, dar sözlük yüzünden `xhigh|max` → `high`. Modelden bağımsız ve onunla ÇARPILAN ikinci maliyet kolu — bkz. [[llm_maliyet_kaldiraclari]]. Claude tabanlı strateji önerici + custom block AST-validated code generation; `hint` parametresi `propose_composed_strategy`'e iletiliyor; `_get_client` thread-safe double-checked lock. LLM backend'i seçilebilir (`NAUTILUS_LLM_BACKEND=auto\|api\|claude-cli`): `ANTHROPIC_API_KEY` yoksa `claude -p` headless CLI ile abonelik (OAuth) üzerinden çağrı — system prompt `--system-prompt-file` ile (Windows 8K sınırı), araçlar kapalı. **`propose_refined_description(raw, backtest_metrics=None, robustness=None)`**: kullanıcının ham strateji metnini alır, Claude'a temiz+kesin+test edilebilir formda yeniden yazdırır; `{refined, notes, suggestions}` döndürür (system prompt TÜRKÇE — `_REFINE_SYSTEM_PROMPT`); `suggestions` stratejiyi güçlendiren 2-4 SOMUT madde: her biri `{kind: "oneri"|"uyari", text}` (trend filtresi/hacim teyidi/stop-TP önerileri → `💡`, aşırı-AND/Bollinger-cross/overfit tuzakları → `⚠️`); boş/geçersiz maddeler ayıklanır, `max_tokens=700`; hata/timeout'ta orijinal metni + boş `suggestions` ile döner (graceful fallback). **`robustness` parametresi (2026-07-21)**: `{overfitting_score, verdict, wfo_efficiency, oos_sharpe, stability}` alır — overfitting skoru < 0.4 veya OOS Sharpe zayıfsa parametreleri sadeleştir/aşırı-optimizasyondan kaçın, ≥ 0.7 robustsa güçlendirme/pozisyon ölçekleme öner. **Prompt: her zaman ≥2 öneri (2026-07-21)**: `_REFINE_SYSTEM_PROMPT` artık "girdi net görünse bile her zaman en az 2 somut öneri ver" der (eski davranış: "netse ve metrik yoksa tarifi aynen döndür, notes='Değişiklik gerekmedi'" idi — buton her basışta boş/aynı öneri döndürdüğü için kaldırıldı; "her basışta taze AI önerisi" akışıyla hizalı). `plan_preview` endpoint'i bu fonksiyonu `propose_condition_breakdown` ile paralel çağırır; sonuç `#plan-preview` içinde düzenlenebilir öneri kutusu + **COPY butonu** (`btCopyRefined` — refined metni panoya kopyalar) + öneri/uyarı listesi + "Tamam" butonu olarak gösterilir — kullanıcı onaylamadan blok üretimi başlamaz. **Çok-turlu sohbet (`chat_refine`, 2026-07-21)**: tek-atışlık `propose_refined_description`'ın yanına, "AI ile iyileştir"i çok-turlu sohbete çeviren `chat_refine(conversation_messages, context=None)` eklendi — tam Anthropic `messages[]` listesini `_create_message`'a geçirir (multi-turn için ek altyapı gerekmedi; merkezi sarmalayıcı zaten `messages` + Fable→Opus fallback yapıyordu), `max_tokens=1000`, system prompt `_CHAT_SYSTEM_PROMPT` (TÜRKÇE, sohbet-tonlu, "çekirdek mantığı değiştirme" kuralı korunur). **İşaretli hibrit çıktı**: JSON zorlaması yerine model serbest sohbet eder + yanıt sonuna `[NET_TARİF]: <...>` tek satırı koyar; regex ile ayıklanır (`{text, refined}` döner), sohbet metnini bozmaz. Hata/timeout'ta nazik Türkçe özür + boş `refined` (crash yok — canlı testte provider 429 `PROVIDER_RATE_LIMIT_EXCEEDED` bu yolla yakalandı). Metrik gömme mantığı `propose_refined_description` ile paylaşılan **`_format_metrics_block(raw, bt_metrics, robustness)`** yardımcısına çıkarıldı (davranış birebir). **claude-cli backend multi-turn join düzeltmesi**: `_ClaudeCLIMessages.create` eskiden yalnız `role=="user"` turn'lerini join edip assistant turn'lerini atlıyordu → model kendi yanıtını göremiyordu; artık multi-turn'de her turn `Kullanıcı:`/`Asistan:` etiketiyle join edilir, **`len(messages)==1` guard'ı** ile TÜM mevcut tek-atışlık çağrılar (propose_strategy/refined/breakdown/custom_block) birebir korundu (regresyon yok); `--no-session-persistence` kalır (state sunucuda). **Blok-düzenleme çok-turlu sohbeti (`chat_edit_block` + `chat_edit_blocks`, 2026-07-21)**: `chat_refine` deseninin bloklara taşınması. **`chat_edit_block(name, existing_meta, existing_code, messages) → {text, meta, code, changed, error, usage}`** — custom block KODUNU sohbetle düzenler: `_BLOCK_EDIT_SYSTEM_PROMPT` (TR; **`name` DEĞİŞMEZ**, mevcut kodu koru + sadece isteneni uygula, kod değişince yanıt sonuna `[NET_KOD]:` + tek JSON `{meta, code}`); regex ile `[NET_KOD]` ayıklanır → `_extract_json_object` + `json.loads`; bulununca `propose_custom_block`'un doğrulama bloğu aynen tekrar (`_validate_generated_code` + `_test_execute_generated(require_max_lookback=True)`) — codegate/smoke reddederse eski kod korunur, `changed=False` + `error=<mesaj>` (**otomatik iç-tur YOK**, kullanıcıya gösterilir); etiket yoksa `changed=False` (soru sormuş). **`chat_edit_blocks(existing_blocks, messages) → {text, blocks, changed, error, usage}`** — strateji draft SignalBlock listesini sohbetle düzenler: `_BLOCKS_EDIT_SYSTEM_PROMPT` katalog özetini (`_catalog_summary()`) gömer → model YALNIZCA `BLOCK_CATALOG` tiplerini seçebilir (halüsinasyon tip önleme); protokol `[NET_BLOKLAR]:` + `{"blocks":[...]}`; `_coerce_catalog_blocks(raw_blocks)` katalog-dışı tipleri düşürür, params'ı katalog aralığına clamp'ler, cross-family'de slow>fast + atr_stop=exit zorlar (`_validate_composed`'dan farkı: entry/exit ZORLAMAZ, fallback EKLEMEZ). Kod üretmez → `type in BLOCK_CATALOG` + coercion yeterli. Her ikisi de hata/429'da nazik TR fallback + `changed=False`. **Kalıcı token ledger hook'u (2026-07-24)**: `_create_message` artık merkezî tek-çıkış-noktası olarak her başarılı çağrıdan sonra `_ledger_record(resp, called_model, _purpose)` çağırır → `token_ledger.record` ile kaydeder; model **`resp.model`** (API'nin gerçekte cevapladığı model) alınır, böylece Fable→Opus fallback DOĞRU modele atfedilir (CLI yanıtında `.model` yok → çağrılan modele düşer). Opsiyonel `_purpose` kwarg'ı çağrı yerini etiketler (best-effort). Ledger yazımı best-effort — I/O hatası bir LLM çağrısını asla bozmaz. Önceden `client.messages.create`'i DOĞRUDAN çağıran 4 narrative/summary yardımcısı (`web/routes/backtest.py::_generate_narrative`, `lab.py::_lab_narrative`+lab-idea, `agent_backtest.py::_winner_narrative`) artık `_create_message` üzerinden geçer → hem ledger'a düşer hem fallback kazanır. **CLI zarf normalizasyonu (2026-08-01)**: yeni Claude CLI sürümleri `--output-format json`'da tek nesne yerine TÜM olay akışını tek JSON DİZİSİ basıyor (system/assistant/rate_limit_event/… + terminal `type=="result"` öğesi = eski zarf) — `_ClaudeCLIMessages.create` bunu bilmediği için `envelope.get` `'list' object has no attribute 'get'` ile patlıyordu ve TÜM LLM çağrıları (SIMPLE sihirbaz "No entry block", AUTO loop `_propose_agent_strategy_idea`) sessizce genel hata mesajına düşüyordu. Parse artık iki şekli de normalize eder: dict → aynen, list → sondan ilk `type=="result"` dict'i (yoksa son dict). Belirti-teşhis dersi: UI'daki "tarifi somutlaştır" mesajı yanıltıcıydı; gerçek neden pm2 error logundaki AttributeError'dı. **CLI maliyet doğruluğu (2026-08-01)**: `_CLIUsage` artık zarfın `usage.cache_creation` 5m/1h TTL kırılımını da taşır (CLI 1h TTL ile yazar → ×2 fiyat, bkz. token_ledger satırı); yeni `_record_cli_side_models(envelope, called_model)` zarfın `modelUsage`'ındaki CLI-İÇ yan-model çağrılarını (her `claude -p`'de ~$0.0006'lık Haiku yardımcı geçişi) `purpose="cli_internal"` ile ledger'a ayrıca yazar — ana modelin girdisi atlanır (`resp.usage` üzerinden zaten kaydediliyor, çift sayım yok); best-effort, asla raise etmez. **Katalog-özeti TTL memo'su + kompakt agnt satırları (2026-08-02)**: `_catalog_summary` composed-proposer'ın SYSTEM prompt'una gömülür ve AUTO loop her iterasyonda yeni agnt_* bloğu kaydettiğinden her çağrıda değişiyordu → ~30K'lık prompt cache'e her seferinde yeniden yazılıyordu (263 çağrı × ~30K = gecelik yazımların %80'i, cache_read=0; canlı CLI probu bayt-sabit sistemde prefix'in doğru cache'lendiğini kanıtladı). Düzeltme: 30 dk TTL memo (`force=True` taze üretir) → prefix bayt-sabit, yazımlar okumaya döner (×2→×0.1); agnt_* blokları tam param şeması yerine TEK kompakt satır (deterministik sıralı, >80'de kırpılır — hem ~100 token/blok şişkinliği hem kendi-çıktısına-yanlılık azalır; param'lar kayıtlı default+clamp ile). Ayrıca üç çağrı yeri artık ledger'a `_purpose` etiketi yazar (composed/idea/custom_block) — gelecekteki maliyet analizi defterden doğrudan kırılır. İkinci-beyin kavramı: llm_maliyetinde_cache_yazimi_baskin_olabilir. **Thread-local model pin (2026-08-01)**: `SELECTABLE_MODELS` + `set_thread_model(model)` — `current_model()` önceliği: kredi-fallback kilidi (fatura gerçeği) > thread-local pin (AUTO loop model seçici) > `MODEL` varsayılanı; pin thread'e özgü olduğundan eşzamanlı chat/PRO çağrıları etkilenmez. **Koşu-başına OpenRouter (2026-08-02)**: pin `or:<openrouter-id>` biçimini de kabul eder — `_create_message` bu pinli çağrıları varsayılan backend'e dokunmadan `_get_openrouter_client()` singleton'ına yönlendirir (kurucu `_build_openrouter_client` tüm-uygulama `NAUTILUS_LLM_BACKEND=openrouter` dalıyla paylaşılır; Fable→Opus kredi fallback'i OR yolunda uygulanmaz, hata çağıranın graceful fallback'ine düşer). `selectable_models()` seçici seçeneklerini üretir: Claude dörtlüsü + `OPENROUTER_API_KEY` ayarlıysa `NAUTILUS_OPENROUTER_MODELS` (virgüllü id listesi, varsayılan üçlü) — studio kokpiti ve /agent formu bu listeyi `llm_models` context'iyle render eder; fiyat tablosunda olmayan OR modellerinin maliyeti defterde dürüstçe "?" kalır **Model görünürlüğü + canlı OpenRouter kataloğu (2026-08-02):** `selectable_models()` OpenRouter satırlarını artık sabit üçlüden değil `https://openrouter.ai/api/v1/models`'ten canlı çeker (`openrouter_catalog()`; ölçüm 341 metin modeli). Cache iki yönlü — başarı 1 saat, **başarısızlık 60 sn**: ikincisi olmadan kapalı/engelli bir uç nokta her sayfa render'ına bir ağ zaman aşımı ekler. Çekim başarısızsa liste eski statik üçlüye düşer, ASLA uydurma id'ye; `NAUTILUS_OPENROUTER_MODELS` verilmişse ağa hiç çıkılmaz (sabitleme + testlerin ağdan bağımsızlığı). Yeni `model_label()` / `model_id()`: boş seçimi (`""` = uygulama varsayılanı) GERÇEK modele çözer, `or:` pinini `OR · <id>` etiketler, kredi fallback'i devredeyse onu yazar — arayüz "default" kelimesini değil faturalanacak hedefi göstersin diye. Kararlar: [[model_secici_ve_gorunurluk]] **Ücretsiz filtresi + elle izin (2026-08-02, aynı gün):** picker VARSAYILAN olarak yalnız ücretsiz uçları listeler (337 → 17). Ücretsizlik `:free` son ekinden değil `pricing.prompt`/`pricing.completion` alanlarından okunur (`_is_free`) — `openrouter/free` o eki taşımaz ama ücretsizdir; eksik/bozuk fiyat PARALI sayılır. Bayrak katalog cache'ine girer (`(id, ad, free)`), süzme render'da olur → anahtar çevrilince ağ turu gerekmez. `NAUTILUS_OPENROUTER_FREE_ONLY=0` tüm kataloğu açar; free-only modda yedek listesi ayrıdır (`_DEFAULT_OPENROUTER_FREE_MODELS`) çünkü eski statik üçlü tamamen paralıydı. `NAUTILUS_OPENROUTER_EXTRA_MODELS` ücretsizlere EK olarak paralı id ekler (pin'den farkı: pin listenin yerine geçer ve ağa çıkmaz); eklenen satır etiketinde `· paralı` yazar ve `openrouter_paid_extras()` ile ayrı optgroup'a çıkar — "ücretsiz (N)" başlıklı grubun içinde paralı satır dursaydı başlık yalan söylerdi. **Maliyet denetimi (2026-08-02):** 10 günlük defter — fatura output + cache yazımı; ölçülen kaldıraçlar model (sonnet-5 −61%, opus-5 uzun yanıt yüzünden fable'dan +22% PAHALI), `--effort` (sonnet'te ek −52%), prefix sabitliği. İki tuzak: `_ClaudeCLIMessages.create` `max_tokens`'ı DÜŞÜRÜR (çağrı noktalarındaki 400/900/4000 varsayılan yolda ölü; gerçekleşen çıktı medyanı 1091) ve `_purpose`suz altı çağrı noktası harcamanın %92'sini oluşturur. `set_thread_model` pini koşu-başına olduğundan kazanç kalıcı değil. Kullanılmayan CLI kolları: `--effort`, `--json-schema`, `--max-budget-usd`. Ayrıntı: [[llm_maliyet_kaldiraclari]]  **Seçilen model gerçekten koşuyor mu (2026-08-02):** picker'ın bir modeli LİSTELEMESİ onu ÇALIŞTIRABİLDİĞİ anlamına gelmiyordu — `openai` paketi (pyproject'te ZORUNLU bağımlılık olarak yazılı) kurulu olmadığı için `_build_openrouter_client` her çağrıda düşüyor, çağıranların graceful fallback'i devreye girip koşu "Random … (Claude unavailable)" kompozisyonları üretiyor ve fazlar `✓` ile kapanıyordu. İki ekleme: `model_unavailable_reason(model)` — ağa çıkmadan yalnız yapılandırmayı yoklar (yalnız `or:` için; Claude yolu uygulamanın varsayılanı), rota bunu koşu başlamadan çağırır. `_or_create_with_backoff` — 429 kalıcı arıza değil ZAMANLAMA arızasıdır; plan `(5, 15, 45)` sn, toplamı 65 sn çünkü ücretsiz uçların sınırı DAKİKA başına ve bir dakikalık pencereyi kapsamayan retry dekoratiftir (openai SDK'sının varsayılan ~1.5 sn'lik iki denemesi tam bu yüzden yetersizdi); `Retry-After` tahmini ezer, toplam bekleme `NAUTILUS_OPENROUTER_429_MAX_WAIT` (varsayılan 75 sn) ile sınırlı — pencereyi kapatacak kadar uzun, STOP yanıtını kilitlemeyecek kadar kısa; YALNIZ 429 denenir. Beklemenin yama yüzeyi modüle ait (`_sleep = time.sleep`): global `time.sleep`'i yamalamak süreçteki her thread'i etkiler ve zamanlamaya duyarlı BAŞKA testleri uzaktan kırar. Ayrıntı: [[model_secici_ve_gorunurluk]] | (app-spesifik) |

### Ops

| Modül | Rol | Wiki karşılığı |
|---|---|---|
| `capture_baseline.py` | Migration-öncesi parite snapshot'ı | [[v1_to_v2_migration_lessons]] "6 catalog spec bit-identical parite" iddiasını üretir |
| `wiki_helper.py` | `[[bare]]` → `/wiki/*` HTML rewriter. Sıfır testti (2026-08-08 DeepR bulgusu) — `tests/test_wiki_helper_and_route.py` artık `_strip_frontmatter`/`_rewrite_wikilinks` (kod bloğu içindeki `[[...]]` syntax'ının atlanması, çözülemeyen slug'ın görünür `**[slug]**`'a düşmesi, label HTML-escape) ve slug index'in `wiki/` > `sources/` önceliğini kapsıyor. | (app-spesifik) |
| `nautilus_wiki/tools/wiki_tools.py` | Wiki CLI | (app-spesifik) |
| `token_ledger.py` | **Kalıcı per-model LLM token defteri (2026-07-24)**: her başarılı LLM çağrısı `_create_message` hook'uyla `~/.cache/nautilus_web_app/token_usage.jsonl`'e TEK JSONL satırı yazar (ts · model · purpose · input/output/cache_read/cache_write). Model = API'nin gerçekte cevapladığı (`resp.model`) → Fable→Opus fallback doğru atfedilir. `record` best-effort (LLM çağrısını asla bozmaz, torn/boş satır atlanır); `summary()` per-model toplar, `format_table()` düz-metin tablo, `__main__` CLI (`python token_ledger.py`). Rapor: **`GET /agent/tokens`** (text/plain tablo; `?format=json` ham breakdown). Eski bellekte-tek-model AUTO sayacının (restart'ta uçan) kalıcı ikamesi. **Maliyet + oturum görünümü (2026-08-01)**: `_PRICES_PER_MTOK` (Anthropic liste fiyatları, prefix-eşleşme; Fable $10/$50, Opus ailesi $5/$25; cache read ×0.1) + `cost_usd(counts, model)` (bilinmeyen model → None, uydurma sayı yok) + `summary(since=ISO)` (string karşılaştırma — satırlar hep `isoformat(timespec="seconds")`); toplam ve model-başına `cost_usd` alanları. NOT: CLI/abonelik çağrıları token-başına faturalanmaz — bu KAVRAMSAL API karşılığıdır (para ölçeği, fatura değil). **Cache-write TTL fiyat düzeltmesi (2026-08-01)**: ilk sürüm tüm cache yazımlarını 5 dk TTL fiyatıyla (×1.25) çarpıyordu; canlı CLI probu Claude CLI'nin **1 saatlik TTL** (`usage.cache_creation.ephemeral_1h_input_tokens`) ile yazdığını ve bunun **×2** fiyatlandığını gösterdi — ×2 ile hesap CLI'nin kendi `total_cost_usd`'sini kuruşuna kadar yeniden üretti ($0.240859), ×1.25 %28 eksik gösteriyordu. `_extract_usage` artık `cache_creation` 5m/1h kırılımını `cache_write_5m/_1h` olarak ledger satırına yazar (sıfır anahtarlar satıra girmez — legacy şekil korunur); `cost_usd` 5m'i ×1.25, 1h'i ×2 fiyatlar; **etiketsiz** (kırılım-öncesi legacy) yazımlar 1h varsayılır çünkü bu ledger'ın tüm geçmişi CLI kaynaklı. `summary` kırılımı fiyatlama için toplar ama `total`'a İKİNCİ kez eklemez (cache_write'ın alt kümesi). Tüm-zaman düzeltme etkisi: $7.62 → $8.72 (146.608 yazım tokeninde +$1.10). **Defterden yapılan ilk tam denetim (2026-08-02)**: 3.304 çağrı / 21,5M token / ~$346 — kalem kırılımı, model kıyası ve kaldıraç sıralaması [[llm_maliyet_kaldiraclari]]'nda; `cache_read/cache_write` oranı çağrı-sınıfı başına izlenmeli (koşu toplamı ayrışmayı gizler). | [[crash_only_design]] |
| `scripts/install-wiki-sync-skill.ps1` | wiki-sync skill kurulumu (2026-07-23): SKILL.md + SCHEMA_TEMPLATE.md + bootstrap_wiki.py + wiki_tools.py dosyalarını gist'ten `~/.claude/skills/wiki-sync` altına klonlar — Windows'ta yeni makine kurulumu; wiki iş akışının kendi kurulum aracı | (app-spesifik) |

## Uçtan uca akış

Kullanıcı web UI'da bir strateji seçip **Run backtest**'e tıkladığında olan sıra:

1. **State** — `web/routes/backtest.py` spec'i `state.py`'den okur (draft/composed).
2. **Data** — `data.py:load_btc_bars()` cached parquet döndürür ([[data_engine]] ingest paritesi).
3. **Sandbox** — `sandbox.run_backtest_guarded(force_subprocess=True)` her zaman TEK yoldan çalıştırır (**BacktestEngine**, `backtest.py:run_composed_backtest`) — killable bir child process'te, event loop'un bir Nautilus koşusu boyunca donmaması için.
4. **BacktestEngine yolu** — `run_composed_backtest`:
   - `BacktestEngineConfig(...)` + `BacktestEngine(config=...)` ([[nautilus_kernel]] küçük ölçekte)
   - `add_venue` + `add_instrument` + `add_data(bars)` + `add_strategy(spec)`
5. **Nautilus** — Strategy `submit_order` → [[order_flow_pipeline]] (OrderEmulator → ExecAlgo → RiskEngine → Adapter → Venue)
6. **Metrics** — `engine.portfolio.statistics()` ([[portfolio]] surface, v2 rc1 sharpe NaN buglığıyla) → `_metrics()` → `_equity_curve()`
7. **State** — Sonuç `IterationResult` olarak state'e append ([[cache]] eşdeğeri)
8. **UI** — HTMX fragment ile ekranda güncelleme.

**Düzeltme (2026-08-09, DeepR [ORTA]):** yukarıdaki akış eskiden "katalogda veri
varsa BacktestNode, yoksa BacktestEngine" diye otomatik motor seçiminden
bahsediyordu — `backtest.py:run_backtest_node`/`run_composed_backtest_node`
(BacktestNode + ParquetDataCatalog yolu, ~450 satır). Bu sayfa hiç
güncellenmemiş: `sandbox.run_backtest_guarded` gerçekte HİÇBİR ZAMAN bu iki
fonksiyonu çağırmıyordu (repo genelinde sıfır çağıran doğrulandı) — hangi
geçişte kaldırıldığı belirsiz, muhtemelen sandbox.py'nin tek-yol subprocess
mimarisine geçişte. Kod artık silindi (bkz.
[[nau_deepr_ucuncu_tur_2026_08_09]]); Nautilus'un BacktestNode API'sinin
kendisi hâlâ [[backtest_node]] sayfasında belgeli (webapp'in bunu kullandığı
iddiası kaldırıldı, API referansı kaldı).

## Karpathy loop: neden bu köprü var

Karpathy'nin "Explorations always add up in the knowledge base" prensibi:

- **Kod → wiki**: `wiki_helper.py`, `tools/wiki_tools.py` ve `nautilus_wiki/wiki/**` wiki'yi ekose halinde tutar.
- **Wiki → kod**: bu sayfa + her modülün `Wiki References` bloğu ters yönü kurar. Böylece yeni bir developer wiki üzerinden koda, kod üzerinden wiki'ye gidebilir; LLM ajanları ise dediklerini kod tabanında doğrulayabilir.

Örnek: [[v1_to_v2_migration_lessons]] sayfası "portfolio.statistics().returns içinde Sharpe NaN" der. `backtest.py:_metrics` bu davranışı Sortino'ya düşerek yönetir; kaynak dosyanın `Wiki References` bloğu tam olarak bu sayfayı gösterir. Wiki iddiası ↔ kod uygulaması bağlantı zinciri açıktır.

## Sağlamlaştırma & regresyon (2026-07)

- **NAU-uyum denetimi** (fonksiyon-fonksiyon, `NAU_ev` referansına karşı): sayısal çekirdek — fitness formülü, calmar cap/floor, junk kapısı, WF fold/embargo/60-gün holdout, `MIN_VALID_FOLDS_FRAC`, **per-trade sharpe**, profit_factor cap (99.0), indikatör port'u — NAU ground-truth'una sadık doğrulandı.
- **NAU per-trade sharpe hizalaması**: composite skorun 0.3 terimi annualized 252-gün Sharpe yerine per-trade sharpe kullanacak şekilde değişti (`_score` + `objective_value`). Backtest metrikleri değişmez, yalnız kazanan-seçimi; bu tarihten önceki mutlak skorlar yeni skorlarla kıyaslanamaz.
- **Yürütme-yolu düzeltmeleri**: composer NAU_WINDOW buffer + flip GTC-birikme, codegate loop-budget (helper içi sonsuz döngü artık yakalanıyor), sandbox graceful-exit, katalog yazım kilidi (delete+write atomik), data force_refresh cache-merge + inverse-USD.
- **Birleşik backtest UI + çoklu-TF (2026-07-18)**: `/backtest` "01" paneli tarif-odaklı tek akışa indirildi — kayıtlı-strateji dropdown'ı ve instrument-kind seçici kaldırıldı; stratejiyi tarif et → Claude blok yazar → seçilen zaman dilimlerinin hepsinde backtest. `POST /backtest/sweep` aynı spec'i çoklu-TF'de koşup karşılaştırma tablosu üretir; `describe` 2+ TF'de sweep'e, 1 TF'de run'a zincirler. Sembol datalist typeahead. Ayrı ayrıca **2-TF trend filtresi** manuel composer'a açıldı (yukarı bkz.).
- **Web-katman refactor + güvenlik + performans (2026-07-18)**: (1) **`web/shared.py` + `ProgressStore`** — 5 route modülündeki kopya-yapıştır progress dict/lock/eviction ve çapraz-import edilen `_log_backtest`/`_log_robustness`/`_chart_url`/`_rotate_if_large` yardımcıları tek leaf modülde toplandı (routes→shared tek yönlü; davranış-koruyan, alias'larla). (2) **Güvenlik**: codegate AST kapısında subscript/dict/alias-callee bypass'ı (`[exec][0]("…")`, `sorted(key=eval)`) ve dangerous-builtin referans kaçağı kapatıldı, çalışma-anı yükleyici artık kısıtlı `__builtins__` ile exec ediyor (savunma derinliği, smoke ile parite); `data._bybit_cache_path` path-traversal'i (Windows `\`, `..`) sanitize edildi. (3) **Performans**: boş tarih aralığında backtest son **100.000 bara** sınırlanıyor (1M-bar full-cache sandbox-timeout'unu çözer; açık tarih tam onurlandırılır — `/backtest/run` + `/sweep`), sweep interval'leri **paralel** koşuyor, `load_catalog` mtime-anahtarlı ham-JSON cache (taze spec'ler → mutasyon riski yok), backtest sayfa handler'ı threadpool'a alındı. **NautilusTrader kütüphanesine dokunulmadı** — yalnız beslenen bar sayısı + orkestrasyon değişti.
- **Backtest aksiyon-akışı sadeleştirmesi (2026-07-19)**: 3-butonlu kalabalık (`✨ AI önerisi al` birincil + `Doğrudan Oluştur (öneriyi atla)` ikincil + plan içi `Tamam`) **tek net akışa** indirildi. Yeni hiyerarşi: **`▶ Backtest Çalıştır`** birincil buton (`btn-primary`, `POST /backtest/describe` → tarif yaz → doğrudan çalıştır) + **`✨ Önce AI ile iyileştir`** ikincil/ghost buton (`btn-ghost`, `POST /backtest/plan` → sağda düzenlenebilir plan + `✅ Tamam`). Eski ters hiyerarşi düzeltildi (AI-önerisi butonu `btn-primary` idi ama gerçek çalıştırma yolu ikincil `btn` "öneriyi atla" adındaydı → hangisinin ana yol olduğu belirsizdi). Endpoint'ler/worker mantığı DEĞİŞMEDİ — yalnız şablon buton düzeni + TR metin (`describe`/`plan` POST'ları, `allow_short`/`intervals` alanları, plan-preview `Tamam` chain'i aynen korundu). Boş-sonuç placeholder + plan-preview alt notu da yeni buton adlarına güncellendi. **NautilusTrader kütüphanesine dokunulmadı** — yalnız şablon/metin.
- **Backtest panel yerleşimi + plan-önerisi tetiği (2026-07-19)**: (1) **Plan önizleme artık düğmeyle** — describe textarea'sındaki otomatik `hx-trigger="keyup changed delay:800ms"` (ve `allow_short` checkbox'ının otomatik plan yenilemesi) KALDIRILDI; yerine buton (`POST /backtest/plan`, `hx-include="closest form"`). Kullanıcı denetimli — boş/yarım tarifle gereksiz LLM çağrısı ve yazarken sürekli yeniden-render önlenir. Onay akışları korundu. (2) **Plan preview sticky** — `#plan-preview:not(:empty) { position:sticky; top:70px }` (topbar altı): dolu olunca alttaki daha-uzun sonuç/robustness/geçmiş yığını kayarken AI önerisi görüş alanında sabit kalır; boşken sticky değil (yükseklik yok → sonuçların üstüne binmez). (3) **Layout bug fix** — sol `.stack` eksik `</div>` yüzünden kapanmıyor, sağ `.stack` onun İÇİNE nested oluyordu → `grid-2 equal`'ın ikinci hücresi hiç oluşmuyor, sağ yarı boş kalıp plan-preview form altına düşüyordu (HEAD'den beri gizli bug); eksik `</div>` eklendi, iki `.stack` de artık grid'in doğrudan çocuğu (div dengesi = net 0, served-HTML üzerinde depth 2'de doğrulandı). **NautilusTrader kütüphanesine dokunulmadı** — yalnız şablon/CSS.
- **AI öneri kutusu + özel sermaye/komisyon + sweep B&H sütunu (2026-07-19)**: (1) **AI strateji-önerisi onay akışı** — `/backtest/plan` artık `propose_refined_description` (agent.py) ile `propose_condition_breakdown`'ı **paralel** çağırır (`asyncio.gather`, `return_exceptions`); ham tarif temiz+kesin+test-edilebilir formda yeniden yazılıp `#plan-preview` içinde düzenlenebilir bir textarea + "✅ Tamam — Blokları Oluştur" butonu olarak gösterilir. Kullanıcı onaylamadan blok üretimi BAŞLAMAZ; buton `hx-vals` ile düzenlenen metni `description` olarak `/backtest/describe`'a taşır. Eski "Generate & Backtest" butonu "Doğrudan Oluştur (öneriyi atla)" fallback'ine dönüştü. Refine sonucu da `(desc, allow_short)` cache + in-flight future'a eklendi; LLM hatası orijinal metne graceful fallback. (2) **Özel başlangıç sermayesi + komisyon override** — `run_backtest(initial_capital, commission_bps_override)` + `_metrics(starting_cash)`; `sandbox` child entry recipe'den bu iki alanı iletir (subprocess yolunda da onurlandırılır). (3) **Sweep tablosu B&H + renk** — `buy_hold_pct` referans sütunu, PnL % yeşil/kırmızı, Max DD % eşik-renkli (<%10 yeşil · <%30 sarı · ≥%30 kırmızı; `best_dd` artık `min`). (4) **`base.html` topbar-stats kaldırıldı** (spark/instrument/bars/range/last şeridi). **NautilusTrader kütüphanesine dokunulmadı.**
- **Performans düzeltme turu (2026-07-19)**: 10-maddelik perf turunun adversarial review'ı sonrası correctness/behavior düzeltmeleri — (1) `VolTargetedTrendStrategy` throttled MTM snapshot (`_MTM_SAMPLE=5`) geri geldi (turda tümüyle silinmişti → `max_dd`/Sharpe/equity-eğrisi sessizce trade-çözünürlüğüne düşüyordu); (2) `WF_FOLDS` 3'e geri (2 → `MIN_VALID_FRAC=0.6` toleransını sıfırlıyordu); (3) incremental EWMA seed'i `calc_ewma_vol` ile birebir hizalandı (warmup ~%13 sapma giderildi, birim testle diff=0 doğrulandı); (4) vol-warmup off-by-one; (5) MA koşan-toplam float drift'i 4096 barda `math.fsum` reset; (6) `get_worker_count` RAM-aware (psutil yoksa sysconf); (7) `_PLAN_CACHE` in-flight dedup + fail-cache atlama; (8) `allow_short` açık string parse; (9) `run_vtt` footer-stats (çift decode giderildi); (10) footer guard logical-type + fallback log. Ayrıca turun kaçırdığı bir **test regresyonu** yakalandı: thread-local session değişikliği `test_bybit_timestamps`'in `requests.get` monkeypatch'ini etkisiz bırakmıştı → test session'ın `get`'ini patch'leyecek şekilde düzeltildi. **NautilusTrader kütüphanesine dokunulmadı.**
- **Backtest ekranı ferah yeniden tasarımı (2026-07-19)**: sol form bütünüyle yeniden düzenlendi — sıkışık `bt-compact` (9–11px) yerine ferah **`bt-airy`** ölçeği + **numaralı 3 adımlı akış** (`.bt-step`: ① Enstrüman & veri · ② Stratejini anlat · ③ Çalıştır). Yeni kontroller: asset-type ve pozisyon-boyutu için **segmented toggle** (`.seg-toggle`/`.seg-opt` — gizli radio, `:has(input:checked)` ile dolu pill), timeframe seçimi için **chip-toggle** (`.chip-choice` — gizli checkbox, tıklanabilir pill), `allow_short` için görsel **switch** (`.switch`/`.switch-track`), tanımsız kalan `.btn-ghost` kuralı + `.btn-hero` birincil aksiyon. Broker + pozisyon boyutu tek `<details>` "Gelişmiş ayarlar"da; sizing modu artık `<select>` yerine segmented toggle (`name="trade_size_mode"` korundu). Sağ sütun + `plan_preview`/`sweep_progress` fragmanları TR'ye çekildi (Plan önizleme · GİRİŞ/ÇIKIŞ · yerel tahmin · Zaman Dilimi Taraması · Kazanç % · ✓ bitti). **KRİTİK: backend/endpoint sözleşmesi HİÇ değişmedi** — tüm `name=` alan adları (`instrument_kind`/`symbol`/`intervals`/`allow_short`/`trade_size_mode`/…), JS'in bağlı olduğu ID/class'lar (`#gen-form`/`#bybit-fields`/`#tf-row`/`#plan-preview`/`.size-param`/`.sp-<mode>`/…), `hx-*` bağlantıları ve describe→run/sweep chain'i birebir korundu; `btToggleAsset`/`btToggleSizeMode`/`btCopyRefined` JS'i `display=''` göster/gizle idiomuyla yeni layout'ta da çalışır. CSS blokları `.bt-airy` altında scope'lu (başka sayfaya sızmaz). UI-metni kilitleyen regresyon anchor'ları güncellendi (`test_plan_preview` 3 + `test_tf_sweep` poll "✓ bitti"). Driver PASS (3.3M bar, +5,778 USDT); ilgili tüm testler yeşil. Golden-parity/orphan-reaping hataları bu turdan bağımsız (motor/subprocess, CSS/şablona dokunulmadı). **NautilusTrader kütüphanesine dokunulmadı** — yalnız şablon/CSS/test-anchor.
- **Sonuç-kaybolma yarışı düzeltmesi (2026-07-20)**: "backtest sonucu geliyor sonra bir anda boş placeholder'a dönüyor" bug'ı — kök neden `describe/progress` chain-tetiği **idempotent DEĞİLDİ**. Üretim `done` olunca `_gen_state_view` her poll'de `chain_vals` döndürüyor, fragment de `hx-trigger="load"` chain div'ini (`→ /backtest/run|/sweep`, `hx-target="#result"`) basıyordu. Normalde HTMX done sonrası poll'ü durdurur, ama gecikmeli/örtüşen bir in-flight poll (yavaş yanıt, geçmiş geri-yükleme, `--reload`, çift tetik) done fragmanını `#result`'a **ikinci kez** yazarsa backtest yeniden tetiklenip o an ekranda duran sonucu siliyordu (art arda çalıştırmada / reload sonrası `last` doluyken deterministik reprodüksiyon). **Düzeltme**: gen state'e `chained` bayrağı + `_mark_gen_chained(gen_id)` atomik test-and-set (`_GEN_LOCK` altında); `describe_progress` chain'i BİR kez servis edip sonraki chain-poll'lerinde **`HTMLResponse(status_code=204)`** döner — HTMX 204'te innerHTML swap yapmaz, `#result` olduğu gibi korunur. Salt route katmanı; endpoint sözleşmesi/worker/şablon değişmedi. `describe`/`plan`/`sweep` testleri 21/21 + driver PASS. **NautilusTrader kütüphanesine dokunulmadı.**
- **Üretim ilerlemesi akış-içine alındı (2026-07-20)**: kullanıcı "en son sonuç toast olarak çıkmasın, sabit kalsın" dedi — `fragments/describe_progress.html` kökündeki `.bt-gen-float` class'ı (üretim ilerleme panelini `position:fixed` ile ekranın sağ-üst köşesine "toast" gibi sabitliyordu) 3 fragman dalından da (`state is none` / `not done` poll dalı / `done` dalı) kaldırıldı; kullanılmayan `.bt-gen-float` CSS bloğu (`web/static/app.css`) silindi. İlerleme paneli artık sağ sütundaki `#result` içinde **akış-içinde/sabit** render edilir, üretim bitince chain aynı yere `/backtest/run|/sweep` sonucunu yazar. Salt şablon/CSS; `every 1s` self-poll, chain, endpoint sözleşmesi ve idempotent-chain (204) düzeltmesi aynen korundu. Driver PASS (3.3M bar). **NautilusTrader kütüphanesine dokunulmadı.**
- **Plan-preview açıkken backtest sonucu görünmüyor bug'ı (2026-07-20)**: "▶ Backtest Çalıştır" butonuna basılıyor ama ekranda hiçbir şey görünmüyor — kök neden `backtest.html`'deki `htmx:afterSwap` listener'ında `#plan-preview`'a swap olunca `#result`'ı `display:none` yapan koddaydı. Kullanıcı "✨ Önce AI ile iyileştir"e basınca plan gelip `#result` gizleniyordu; sonra "▶ Backtest Çalıştır"a basınca sonuç `#result`'a yazılıyor ama div gizli kaldığı için görünmüyordu. **Düzeltme** iki satır: (1) `#plan-preview` swap listener'ındaki `result.style.display = hasContent ? "none" : ""` bloğu tamamen kaldırıldı — plan-preview ve result artık bağımsız, plan açıkken result gizlenmez, ikisi sağ sütunda üst-alta yığılır; (2) `#result`'a swap yapan listener'a `evt.detail.target.style.display = ""` eklendi — herhangi bir yoldan `display:none` kalmışsa swap anında sıfırlanır. Salt `backtest.html` JS; endpoint/worker/şablon sözleşmesi değişmedi. Playwright ile doğrulandı (plan açıkken backtest başlatılınca `#result display: block`). **NautilusTrader kütüphanesine dokunulmadı.**
- **Progress-bar "Reading data · parquet cache" takılması + Bybit cache metadata-only + fee-baking (2026-07-20)**: (1) **"read" fazı takılı görünmesi** — Index/Equity dalında `_progress("Reading data · …")` çağrısı parquet full-read'den SONRA geliyordu; blank tarih aralığında cache bounds keşfi için önce tüm parquet okunuyor, bu sürede UI "start" fazında/`Reading data · parquet cache` label'ında donuk kalıyordu. Çağrı okuma öncesine taşındı; Bybit dalına da metadata-öncesi bir `_progress` eklendi → faz UI'da anında ilerliyor. (2) **Bybit cache bounds metadata-only**: FULL `pd.read_parquet` yerine pyarrow row-group istatistikleri (min/max timestamp) — 3M+ satır taranmaz; index kolonu `path_in_schema`'da `"index"` geçen kolon, stats yoksa `columns=[]` fallback. Varsayılan pencere blank tarihte 7 gün → `max(cache_start, cache_end−365d)` **365 gün cap**. (3) **Komisyon override instrument'a baked**: `commission_bps_override` artık `_make_bybit_instrument(fee_bps_override=…)` ile instrument'a yazılır, `run_composed_backtest` ayrı `MakerTakerFeeModel` kurmaz (`_fee = _fee_model_for(instrument)`); `_bars_from_df` vektörize (`ts_close`) list-comprehension'a döndü (davranış birebir). Salt route/domain katmanı + `base.html` `htmx.config.allowScriptTags=true` + `describe_progress` chain-tetiği data-attr'a (`#bt-chain-trigger` `data-bt-chain-url`/`data-bt-chain-vals`) taşındı + `plan_preview`'e `✕ Kapat` butonu (`#plan-preview` boşaltıp `#result`'ı `display=''` geri gösterir). **NautilusTrader kütüphanesine dokunulmadı.**
- **Robustness paneli OOB swap bug'ı + MakerTakerFeeModel API uyumu (2026-07-20)**: (1) **Robustness "Analizi Çalıştır" hiçbir şey yapmıyor** — kök neden: `backtest_result.html` sonunda `hx-swap-oob="outerHTML"` ile `#robustness-section`'ı güncelleyen blok, HTMX 1.9.12'de `innerHTML` swap ile geldiğinde OOB swap'ı işlemiyordu (`backtest_progress.html` `hx-swap="innerHTML"` kullanıyor). `spec_id` boş gidince `POST /robustness/run` 404 dönüyor, HTMX 404'ü sessizce yutuyor. **Düzeltme**: OOB blok kaldırıldı; `backtest_result.html` ana `<div class="panel">`'ine `data-rob-spec-id/symbol/category/interval/start/end` attribute'ları eklendi; `backtest.html`'deki `htmx:afterSettle` listener'ı `#result` swap sonrası bu attribute'ları okuyup `#robustness-section` form inputlarını dolduruyor ve paneli `display=""` ile gösteriyor. (2) **`MakerTakerFeeModel(maker_fee=…, taker_fee=…)` TypeError** — NautilusTrader 1.230.0'da `MakerTakerFeeModel.__init__` yalnız `config=None` alıyor; fee'leri instrument'tan okuyor. `_make_bybit_instrument`'a `fee_bps_override: float | None = None` parametresi eklendi; override varken instrument'ın `maker_bps`/`taker_bps`'i bu değerle değiştiriliyor. `run_composed_backtest`'teki ayrı `MakerTakerFeeModel(maker_fee=…)` bloğu kaldırıldı; `_fee = _fee_model_for(active_instrument)` her zaman kullanılıyor. Salt şablon JS + `backtest.py`; NautilusTrader kütüphanesine dokunulmadı.
- **Backtest UX yeniden tasarımı: sekmeli sağ sütun + verdict kartı + geçmiş geri-yükleme (2026-07-20)**: `/backtest` sağ sütunu **kullanım açısından** yeniden kurgulandı (görsel grafit+mavi tema geçişinin ardından). (1) **Sekmeli sağ sütun** — eskiden `#plan-preview → #result → #robustness-section → geçmiş` alt alta yığılı `.stack`'ti (uzun sonuçta bitmeyen dikey kaydırma); artık tek panelde **`Sonuç · Robustness · Geçmiş`** sekmeleri (`.bt-tabs` sticky şerit `top:58px` topbar altı, `.bt-tab`/`.bt-tabpanel`, global `btTab(name)` JS — `robTab`/`rob-tab` desenini taklit eder). `#result`, `#plan-preview`, `#robustness-section`, `#robustness-result` id'leri ve içindeki tüm form `name`/`hx-*` sözleşmesi **korundu** — panel `<div>`'leri yalnız sekme panellerinin içine taşındı, `data-bt-chain-*` zinciri + `htmx:afterSettle` doldurma bloğu (`data-rob-*`/`data-bt-*`) `#result` id-check'iyle aynen çalışır. Sonuç sekmesinde **trade-sayısı rozeti** (`#bt-tab-badge-result`): backtest çalışırken `⟳` (progress panel `#describe-progress`/`#progress-panel`/`#sweep-progress` görülünce `running`), bitince trade sayısı. (2) **Verdict/özet kartı + otomatik odak** (`backtest_result.html`) — hero'nun üstünde tek bakışta "iyi mi kötü mü" şeridi (`.verdict-card`): eşiklerle Jinja'da türetilen **Güçlü/Umut verici/Kârlı ama oynak/Zayıf/Elenmeli** rozeti + inline PnL/Sharpe/Win-Rate/Max-DD/Trades; **yeni backend alanı YOK** (mevcut `pnl_dir`/`sharpe`/`win_rate`/`max_dd`/`n_trades`'ten). Fragment sonundaki koşulsuz inline script sonuç yerleşince `btTab('result')` + `#result.scrollIntoView` yapar (bu fragment YALNIZCA final sonuç için render edilir — progress'te değil). (3) **Geçmiş: tam sonuç sakla + birebir aç** — jsonl log yalnız skalar metrik tutuyor (equity/trade listesi yok → geçmiş satırı tam ekranı log'dan kuramaz). Çözüm: run-worker `_LAST_RESULT` set noktasında **tam view-model'i** `bt_results/<run_id>.json`'a yazar (`web.shared.save_result_snapshot`, son 20 tutulur, mtime-prune); yeni route **`GET /backtest/result/{run_id}`** snapshot'ı okuyup **aynı `backtest_result.html`'i** `last=<dict>` ile render eder → `#result`'a swap edilince chart/robustness/hidden-doldurma yeniden-init otomatik. Geçmiş satırları artık **tıklanabilir** (`.bt-hist-row` + `hx-get="/backtest/result/{run_id}"` + `onclick="btTab('result')"`); snapshot yoksa 200 + `HX-Toast: err|…` (header ASCII — latin-1 encode zorunlu, Türkçe karakter header'da hata verir). `page()`'in view-model kurma bloğu `_result_viewmodel(r, spec_name, narrative, bars_info)` yardımcısına çıkarıldı (page + worker-snapshot + reload aynı ekranı üretir). `log_backtest`'e opsiyonel `run_id` param'ı (geriye uyumlu), log kaydına `run_id` yazılır; `_recent_runs` satırlara `run_id` ekler. **Yan düzeltme**: `_recent_runs` sabit 32KB tail penceresi büyük kayıtlarda (bir kayıt equity/MTM gömülü ~280KB) tek satırın ortasına düşüp 0 kayıt döndürüyordu → pencere `max(2MB, limit·400KB)`'a çıkarıldı (kısmi ilk satır json.loads'ta atlanır). Salt sunum + `web.shared` snapshot store + tek yeni GET route; endpoint/worker/spec sözleşmesi değişmedi. Ruff temiz; `log_backtest`'i kapsayan `test_ops_fixes.py` 8/8; headless-Chrome ekran görüntüleriyle (form / otomatik-odaklı sonuç+verdict / geçmiş sekmesi) doğrulandı. **NautilusTrader kütüphanesine dokunulmadı.**

- **AI Plan ayrı sekme + robustness ilk-yükleme fix (2026-07-20)**: (1) **AI iyileştirme kendi sekmesi** — `#plan-preview` Sonuç sekmesinin içinden çıkarılıp **`✨ AI Plan`** adlı 4. sekmeye taşındı (sıra: Sonuç · AI Plan · Robustness · Geçmiş). "✨ Önce AI ile iyileştir" butonuna `onclick="if(window.btTab)btTab('plan')"` eklendi; boş `#plan-empty` durum kartı, plan dolunca `htmx:afterSwap` (`target.id==='plan-preview'`) ile gizlenir. `plan_preview.html` "✕ Kapat" artık `#result.display=''` yerine planı boşaltıp `btTab('result')`'a döner; "✅ Tamam" `#result` hedefli olduğundan basınca otomatik Sonuç sekmesi açılır. (2) **Robustness "Analizi Çalıştır" çalışmıyordu** — kök neden: robustness formunu (+plan hidden-input'larını) `data-rob-*`/`data-bt-*`'den dolduran kod YALNIZ `htmx:afterSettle`'daydı; sunucu-render `last` sonucu (sayfa açılışı / yenileme / geçmiş dolu) htmx swap'ı tetiklemediğinden `#rob-spec-id` **boş** kalıyor, form submit `spec_id`'siz gidip `POST /robustness/run` sessizce 404 dönüyordu. **Düzeltme**: doldurma mantığı `btFillPanels(root)` fonksiyonuna çıkarıldı; hem `htmx:afterSettle` (`#result` swap) HEM `DOMContentLoaded`'da çağrılır. CDP (headless Chrome) ile doğrulandı: sayfa yüklenince `#rob-spec-id="vizdemo1"`, form submit → `#robustness-result` progress paneliyle doldu. Salt `backtest.html` JS + `plan_preview.html`; endpoint/worker/spec sözleşmesi + `data-bt-chain-*` zinciri değişmedi. **NautilusTrader kütüphanesine dokunulmadı.**

- **Robustness sonuç ekranı görsel yenileme (2026-07-21)**: `fragments/robustness_result.html` tek dosya değişti. (1) **Hero karar kartı** — panel-body'nin başına, tab navigation'dan önce `#rob-hero` banner'ı eklendi; `data-rob-*` attribute'larından `buildHero()` JS fonksiyonu ile doldurulur; `overfitting_score >= 0.5` → yeşil + "ROBUST", `0.25–0.5` → sarı + "DİKKAT", `< 0.25` → kırmızı + "OVERFITTING"; banner'ın `border-left` ve `background` rengi dinamik; sağ köşede renkli badge + açıklama metni. (2) **KPI renk kodlama** — WFO: `oos_sharpe_optimized`/`oos_sharpe_naive` > 0 yeşil / < 0 kırmızı; `wfo_efficiency` ≥ 0.5 yeşil / 0.2–0.5 sarı / < 0.2 kırmızı. IS/OOS: `in_sample_sharpe`/`oos_sharpe` > 0 yeşil / < 0 kırmızı. MC: `p5_final`/`median_final` starting_cash üstü yeşil; `max_dd_p50` abs < %15 yeşil / < %25 sarı / ≥ %25 kırmızı; `max_dd_p95` abs < %25 yeşil / < %40 sarı / ≥ %40 kırmızı. (3) **IS/OOS eşik düzeltmesi** — banner renk sınırları 0.7/0.4'ten **0.5/0.25**'e düzeltildi (gerçek `backtest_robustness.py` eşikleriyle hizalandı); açıklama metni `≥0.5 robust · 0.25–0.5 caution · <0.25 overfitting suspected` olarak güncellendi; banner arka plan rengi de verdict'e göre dinamik. (4) **Sekme açıklama kutuları** — her tab'ın başına sabit `.info-box` eklendi: WFO için "optimize vs. naive karşılaştırması + WFO efficiency", IS/OOS için "Overfitting Score formülü + eşik yorumu", MC için "bootstrap yöntemi + P5/P95 yorumu + Max DD p95 uyarısı". Python, endpoint, veri yapısı değişmedi. **NautilusTrader kütüphanesine dokunulmadı.**

- **"AI ile iyileştir" → çok-turlu sohbet (2026-07-21)**: Backtest ✨ AI Plan sekmesindeki tek-atışlık "AI ile iyileştir" çok-turlu sohbete dönüştürüldü — mevcut single-shot akış (`/backtest/plan` + `propose_refined_description`) **hiç bozulmadan korundu**, sohbet onun YANINA ikinci mod olarak eklendi (iki buton: "✨ AI ile iyileştir" + "💬 Sohbet ederek iyileştir"). **`agent.py`**: yeni `chat_refine(conversation_messages, context=None)` — tam `messages[]` listesini `_create_message`'a geçirir (merkezi sarmalayıcı zaten multi-turn + Fable→Opus fallback yapıyordu, ek altyapı gerekmedi), `_CHAT_SYSTEM_PROMPT` (TR, sohbet-tonlu), `[NET_TARİF]:` işaretli hibrit çıktı (JSON zorlaması yerine serbest sohbet + sondaki tek-satır regex ayıklaması → `{text, refined}`), hata/429'da nazik fallback (crash yok). Metrik gömme `_format_metrics_block` yardımcısına çıkarıldı (paylaşılan). **claude-cli backend multi-turn join düzeltmesi**: `_ClaudeCLIMessages.create` artık assistant turn'lerini de `Kullanıcı:`/`Asistan:` etiketiyle join eder; **`len(messages)==1` guard'ı** tüm mevcut tek-atışlık çağrıları birebir korur (regresyonsuz). **`web/routes/backtest.py`**: `_CHAT_STORE` (conv_id→state) + `_CHAT_LOCK` + TTL(30dk)/kapasite(100) eviction (`ProgressStore`'un hafif hâli); lock disiplini = LLM çağrısı lock DIŞINDA (oku-kopyala→çağır→tekrar-al-yaz); `POST /backtest/chat/new` (plan_preview form imzası + `_collect_chat_context`) + `POST /backtest/chat` (conv_id+message; süresi dolan → nazik yeniden-başlat). **`fragments/chat_thread.html`** (yeni): mesaj balonları (`.chat-bubble .chat-user/.chat-assistant`, `app.css`) + güncel net tarif `#refined-description` textarea'sı + "✅ Tamam — Blokları Oluştur" (`/backtest/describe`'a AYNEN bağlı — sohbet sonucu mevcut üretim zincirini besler). Sekme boş-durum gizleme `htmx:afterSwap` `plan-preview`+`chat-thread` ikisini de kapsar. Canlı LLM ile uçtan-uca doğrulandı (gerçek Claude çok-turlu yanıt + `[NET_TARİF]` textarea'ya yerleşti + provider 429 fallback); 315 test yeşil (kalan 12 kırık golden-parity/describe baseline'da da mevcut, `git stash` ile izole edildi). Desen ikinci beyin vault'unda `single_shot_to_chat_donusum` sayfasında. **NautilusTrader kütüphanesine dokunulmadı** — salt route/agent/şablon/CSS.
- **Blokları AI ile düzenleme → çok-turlu sohbet (2026-07-21)**: `chat_refine`/`chat_edit`-deseninin **bloklara** taşınması. Bugüne dek bloklar için yalnız sıfırdan-üret+kaydet+sil vardı; **mevcut bir bloğu/listeyi düzenleme yolu yoktu**. İki yeni akış (kullanıcı seçimi: her ikisi de, çok-turlu sohbet): **(A) custom block KODU** — `agent.chat_edit_block` (`[NET_KOD]:` + `{meta,code}`, `_validate_generated_code`+`_test_execute_generated(require_max_lookback)` defense-in-depth, codegate reddi sohbette gösterilir **otomatik iç-tur YOK**), `web/routes/strategy.py` 3 endpoint (`/blocks/{name}/chat/new|chat|chat/save`), `custom_blocks_list.html`'e "✏️ AI ile düzenle" butonu, yeni `fragments/block_chat_thread.html` (META/PARAMS/KOD **+ BTC-1m sinyal grafiği** önizleme + dependent uyarısı + "💾 Kaydet"). Save yolu save-custom'ın **409'unu ATLAR** (üzerine-yazma edit'in amacı) ama doğrulamayı aynen tekrarlar; `unregister_custom_block`→`register_custom_from_disk` ile davranış tazelenir. **(B) strateji draft LİSTESİ** — `agent.chat_edit_blocks` (katalog özeti gömülü → halüsinasyon tip önleme, `[NET_BLOKLAR]:` + `{"blocks":[...]}`, `_coerce_catalog_blocks` katalog-dışı düşürür + param clamp), 3 endpoint (`/drafts/chat/new|chat|chat/apply`), yeni `fragments/drafts_chat_thread.html` (önerilen liste tablosu + "✅ Onayla ve Uygula"). Ortak: `web.shared.ChatStore` iki ayrı örnek (`_BLOCK_CHAT`/`_DRAFTS_CHAT`), LLM lock-dışı; `_preview_signals` `generate_custom_block`'tan çıkarıldı (kod tekrarı önlendi). `web/shared.py` — bu oturumda `ChatStore` genelleştirildi (backtest'in el-yapımı `_CHAT_STORE`'undan). **Doğrulama (HTTP surface)**: tüm sunucu-tarafı yol PASS — buton/hedef-div render, B add→chat/new→apply (working_blocks→`_DRAFTS` yazımı), A chat/new (dependent uyarısı "1 strateji kullanıyor" + META/PARAMS/CODE), A chat/save (codegate+smoke+save+unregister/register+**HX-Redirect**, 409-bypass, orphan yok), guard'lar (404 bilinmeyen/builtin, 400 boş-draft, 409/expired-conv). **Canlı LLM happy-path gözlenemedi**: sağlayıcı hem `claude-fable-5` hem fallback `claude-4.8-opus` için ısrarlı **429 PROVIDER_RATE_LIMIT_EXCEEDED** döndürdü (ortam kaynaklı, kod değil); feature 429'u nazik fallback + çökme-yok ile karşıladı (tasarlanmış hata yolu). **NautilusTrader kütüphanesine dokunulmadı** — salt route/agent/shared/şablon.
- **Robustness giriş formu yeniden tasarımı: gruplu + inline açıklama + presetler (2026-07-21)**: `backtest.html` Robustness sekmesindeki form (`auto-fit` tek-satır ızgara) **üç mantıksal gruba** (`<fieldset class="rob-group">` + `<legend>`) ayrıldı — **Walk-Forward Optimizasyon** (`train_months` 1–24 öneri 3–6, `test_months` 1–12 öneri 1, `n_optimize` 1–200 öneri 20–50, `objective` sharpe/sortino/calmar), **In / Out-of-Sample Bölme** (`split_pct` 0.5–0.9 öneri 0.70), **Monte Carlo Simülasyonu** (`n_sims` 50–1000 öneri 300–500). Her alanın altında `.field-hint` ile ne olduğu + önerilen aralık gösterilir (aralıklar `robustness.py run()` clamp'leriyle birebir). Formun üstünde: üç testi özetleyen `.rob-form-intro` kutusu + `robPreset(kind)` JS ile tek-tık dolduran **3 preset** (⚡ Hızlı=düşük deneme/sim, ⚖ Dengeli=varsayılan, 🔬 Titiz=yüksek); aktif preset butonu `.active` ile vurgulanır. Yeni CSS `app.css` sonunda (`.rob-form*`, `.rob-group*`, `.field-hint`, `.btn-sm`, `.rob-presets*`). Backend/endpoint/veri sözleşmesi + hidden input isimleri (`spec_id/symbol/…/train_months/…/objective`) DEĞİŞMEDİ — salt sunum + istemci JS. Uygulamada doğrulandı (HTTP render: 3 legend + 6 field-hint + 3 preset + intro; CSS/JS yüklü). **NautilusTrader kütüphanesine dokunulmadı.**

- **"AI ile iyileştir" butonu AI Plan sekmesine taşındı + yeniden adlandırıldı (2026-07-21)**: `backtest.html` — "✨ Önce AI ile iyileştir" butonu sol formdaki **③ Çalıştır** adımından (`▶ Backtest Çalıştır` yanından) kaldırılıp **AI Plan sekmesinin boş-durum kartının (`#plan-empty`) içine** taşındı ve adı **"✨ AI ile iyileştir"** oldu. Eylem artık sonucu gösterdiği yerle (sağ sütun `#plan-preview`) aynı sütunda. **Kritik**: buton artık `#gen-form`'un DIŞINDA olduğundan `hx-include="closest form"` → **`hx-include="#gen-form"`** değişti (form id `gen-form`, backtest.html:27; aksi halde `closest form` çözülmez, alanlar POST'a girmez). `#plan-spinner` da sol formdan AI Plan kartına taşındı (yoksa "Claude planı hazırlıyor…" göstergesi görünmez). `onclick="btTab('plan')"` kaldırıldı — buton zaten sekmenin içinde. Buton `btn-ghost`→`btn-primary` (kartın tek birincil eylemi). `#plan-empty` gizle/göster mantığı (`htmx:afterSwap` target `plan-preview` → innerHTML doluysa gizle) aynen çalışır: plan yüklenince buton dahil kart gizlenir, `plan_preview.html` "✕ Kapat" ile boşalınca yeniden görünür. Backend (`plan_preview` endpoint + form alanları) DEĞİŞMEDİ. Uygulamada doğrulandı: dolu tarif POST → gerçek plan (refined-description + AI önerisi + Kapat/Tamam), boş tarif → 185B prompt-stage; `/backtest/plan` hx-post tek kaynak, "Önce" kalıntısı 0. **NautilusTrader kütüphanesine dokunulmadı.**

- **Studio hata düzeltme turu: 6 bug (duplicate-submit yarışı, result-JS, wizard gating, tarih validasyonu, uzun-koşu UI, rozet) (2026-07-23)**: kullanıcının önceliklendirilmiş 6-maddelik hata raporu tek turda kapatıldı; salt route + şablon/JS/CSS. **(1) Duplicate submission / race — High**: Run Saved/Run Backtest koşu sırasında tekrar tıklanabiliyor, ikinci submit progress'i resetliyordu. İki katman: sunucuda `_ACTIVE_RUNS` sid-registry (`/run`+`/describe`+`/sweep` → canlı işte **409 + HX-Toast**, HTMX 2xx-dışını swap etmediğinden panel korunur; self-clearing + 500-eşik prune — prune'da store lookuplar registry lock DIŞINDA, lock-nesting yok); istemcide merkezi `btRunActive()`/`btSyncRunState()` tracker'ı (`#result` içinde `[hx-get]`'li progress paneli veya `#bt-chain-trigger` varken tüm `.js-run-btn` disabled) + `htmx:beforeRequest` guard'ı (istek tarayıcıdan çıkmadan iptal + toast; describe→run chain'i `_btChainFiring` bayrağıyla tam BİR kez geçer). Cookie `nautlab_sid` `/studio`'da zaten set — guard üretimde çalışır. **(2) Result-panel JS SyntaxError — High**: `backtest_result.html` fragment script'i her `#result` swap'ında yeniden çalışır; top-level `const _resultEl` global lexical binding olduğundan İKİNCİ swap'ta "Identifier already declared" **parse-time** hatası atıyor ve script'in TAMAMI ölüyordu (grafikler yeniden kurulmuyor; hata htmx'in `insertBefore` script-enjeksiyonunda görünür). Düzeltme: blok IIFE'ye alındı (`var` + `_chartsBound` idempotency); fragment'lerde top-level `const/let` kalmadığı brace-depth regresyon testiyle kilitlendi. **(3) SIMPLE wizard gating — High**: backtest/reliability hiç koşmadan Finish'e ulaşılabiliyordu → `swState.btDone/robDone` completion bayrakları + `swGoto` gating (bkz. studio.py satırı); backtest zorunlu, reliability açıkça opsiyonel etiketli. **(4) Tarih validasyonu — Medium**: Start>End submit edilebiliyordu, pipeline başlayıp geç ölüyordu → sunucu `_invalid_date_range` (worker başlamadan 400) + istemci `btDateRangeError` (ISO string karşılaştırma) + `#bt-date-error` inline `role=alert` mesajı (tarih değişince temizlenir) + sihirbazda `swRun` kendi inline hint'i. **(5) SIMPLE uzun-koşu UI koruması — Medium**: "Claude is writing…" fazında ileri akış açıktı → `bt:run-state` CustomEvent köprüsü: koşarken run butonu + Continue kilitli, `#sw-run-hint` canlı durum metni (⏳ Running → ✓ finished), robustness koşarken `#sw-rob-btn` disabled. **(6) Result sekme rozeti — Low**: "Result ⟳"/"Result 29" kararsızdı → rozet tek sahipli: koşarken yalnız spinner + `title="Backtest in progress…"`, bitince trade sayısı + `title="N trades in the last completed backtest"`, sonuç yokken "–" (loading ve count durumları asla karışmaz; eskiden hata durumunda spinner asılı kalabiliyordu). **Testler**: yeni `tests/test_studio_ui_fixes.py` 11 test (helper birimleri, 409/400 endpoint yolları, registry self-clear + prune + evict-ignore, per-session izolasyon, fragment top-level-declaration regresyonu); `test_regression_anchors` 68/68 + `tf_sweep`/`plan_preview` yeşil; `test_describe_backtest`'teki 7 kırık **baseline'da da aynı** (`git stash` ile izole — bu turla ilgisiz). Code-review turunda tek 🟡 bulgu (`_ACTIVE_RUNS` sınırsız büyüme) prune ile kapatıldı. Doğrulanmış baseline akışı (EMA Cross + ATR Stop · BTCUSDT/linear/1m) değişmedi — `/run` worker'ına/sandbox'a dokunulmadı. **NautilusTrader kütüphanesine dokunulmadı** — salt route + şablon/JS/CSS.
- **Kalıcı per-model token ledger (2026-07-24)**: "hangi modelde ne kadar token tüketildi" sorusu eskiden cevaplanamıyordu — sayaç yalnız AUTO loop başına, BELLEKTE, tek pricing modele atfedilerek tutuluyordu (restart'ta uçar; PRO describe/plan/chat hiç sayılmaz; session jsonl'lerinde usage kaydı yok). Yeni `token_ledger.py`: `_create_message` merkezî çıkış-noktasına eklenen `_ledger_record` hook'u her başarılı çağrıyı `~/.cache/nautilus_web_app/token_usage.jsonl`'e yazar — model = **`resp.model`** (API'nin gerçekte cevapladığı → Fable→Opus fallback DOĞRU atfedilir; CLI'da `.model` yok → çağrılan modele düşer), + opsiyonel `_purpose` etiketi. Best-effort (ledger I/O bir LLM çağrısını asla bozmaz; torn/boş satır atlanır). Doğrudan `client.messages.create` çağıran **4 narrative/summary** yardımcısı (`backtest._generate_narrative`, `lab._lab_narrative`+lab-idea, `agent_backtest._winner_narrative`) `_create_message`'a yönlendirildi → ledger + fallback kazandılar. Rapor: `token_ledger.summary()` per-model toplar, `format_table()` düz-metin tablo, `python token_ledger.py` CLI, **`GET /agent/tokens`** (text/plain tablo · `?format=json` ham breakdown). Testler: `tests/test_token_ledger.py` 7 (obj+dict usage extraction, per-model aggregation + fallback atfı, torn-line skip, record-never-raises, absent-ledger, format smoke); regression_anchors 68/68 yeşil, ruff temiz, `_create_message`→ledger uçtan-uca doğrulandı. Karar/sınır bağlamı ikinci-beyin `nau_token_tuketim_izleme` sayfasında. **NautilusTrader kütüphanesine dokunulmadı** — salt agent/route/yeni-modül.
- **Robustness polling duruyordu + AI Plan sekme sırası + plan cache refactor (2026-07-20)**: (1) **Robustness progress polling duruyordu** — `robustness_progress.html` içindeki `hx-target="this" hx-swap="outerHTML"` her poll'de `#robustness-result` container'ını DOM'dan kaldırıyordu; sonraki polling isteği hedefi bulamayınca sessizce duruyordu. **Düzeltme**: `hx-target="#robustness-result" hx-swap="innerHTML"` — parent container hedeflendi, kendini silmez. (2) **AI Plan sekme sırası** — kullanıcı isteğiyle `✨ AI Plan` sekme sırası `Sonuç`'un önüne alındı: `✨ AI Plan · Sonuç · Robustness · Geçmiş`. Sayfa ilk açılışında aktif sekme AI Plan; backtest tamamlanınca `backtest_result.html` inline script `btTab('result')` çağırarak otomatik Sonuç'a geçer. (3) **Plan cache refactor** — `propose_refined_description` sonucu artık **hiç cache'lenmez**; her "✨ Önce AI ile iyileştir" basışında taze AI çağrısı yapılır (kullanıcı ikinci kez basınca öneri alamaması sorunu çözüldü). Cache yalnızca `bd` (blok planı) için `(desc, allow_short)` key'iyle tutulur — `bd` cache hit olsa bile `refined` her zaman `asyncio.to_thread(propose_refined_description, desc, _bt_metrics)` ile yenilenir. `agent.py`'de `propose_refined_description` exception'ları artık `agent.refine` logger'a yazılır (sessiz yutulan hatalar görünür). Salt `backtest.html` + `robustness_progress.html` + `web/routes/backtest.py` + `agent.py`; endpoint/worker/spec sözleşmesi değişmedi. **NautilusTrader kütüphanesine dokunulmadı.**

- **Regresyon süiti**: **321 test** (pytest); NAU sabitleri, sizing modları, fold-kabul, codegate red-matrisi (subscript/dict/alias-callee bypass + dangerous-builtin referansı dahil), path-traversal guard, force_refresh-merge, delete-skip, flip yolu, per-trade-sharpe hizalaması, describe→sweep çoklu-TF zinciri (sıra-bağımsız), MTF trend filtresi ve LLM kredi-fallback dahil çapalar. Ruff lint/format temiz (PostToolUse hook).
- **Backtest UX + IB komisyonu + canlı plan önizleme (2026-07-18)**: (1) **Per-enstrüman komisyon** — kripto MakerTaker + per-symbol `_BYBIT_FEES_BPS`, US hisse/ETF (QQQ) için **`IBFixedFeeModel`** (IB Pro Fixed: $0.005/hisse · min $1 · %1 tavan) `_fee_model_for` ile seçilir; equity artık net-komisyon backtest edilir. (2) **`/data` yeniden tasarım** — üç kaynak (Bybit/Index/External) ortak `.tf-grid` "enstrüman kartı" + tutarlı durum sözlüğü; index satırları da `in_nautilus_catalog` bildirir (`→ Catalog` yazılınca "✓ catalog" rozeti, buton kaybolur); discover JSON→HTML fragment; ölü `row_fragment` endpoint kaldırıldı; dil İngilizce. (3) **Canlı plan önizleme** (`POST /backtest/plan`) — yazarken debounce ile tek breakdown çağrısı; koşullar built-in'lere yerel eşlenir, tuzaklar tahmin edilir, eşleşen built-in'lerin son ~400 barlık BTC-1m al/sat/çıkış noktaları + indikatör overlay'leri (MA/EMA/Bollinger + RSI pane) `_preview_signals` ile tahmini çizilir (salt-okunur; üretmez/kaydetmez). (4) **`allow_short` toggle** backtest formunda (varsayılan AÇIK) → üretilen spec'e taşınır (MARGIN); plan uyarısı yalnız kapalıyken çıkar. (5) **Canlı ilerleme flow'u** — `_derive_bt_phases` worker mesajlarını kanonik Nautilus fazlarına eşler (tamam=yeşil, aktif=yanıp söner) + ham log; sweep başlığı şu an koşan TF + ilerlemeyi gösterir; backtest butonunda dönen spinner; Order Flow + Nautilus Wiki panelleri kaldırıldı. `NAUTILUS_LLM_MODEL=claude-opus-4-8` (proxy `claude-fable-5`'i reddediyor). **NautilusTrader kütüphanesine dokunulmadı.**

- **AUTO ekranı Mission Control kokpiti (2026-08-02)**: Strategy Studio'nun AUTO sekmesi uzun dikey akıştan (büyük form → durum çubuğu → timeline → sürekli büyüyen adım kartları) **tek ekranlık, kaydırmayan** kokpite geçti; tasarım kaynağı repo içinde `design_handoff_auto_mission_control/` (yön `1c`). Çözdüğü iki somut kusur: koşu durumu kaydırınca kayboluyordu (20+ dakikalık bir döngüde ekranda tutulması gereken tek şeydi) ve üretilen adaylar birinci sınıf değildi ("hangisi önde" sorusunun ekranda yeri yoktu). Yeni yapı: sol ray salt-okunur BRIEF + BÜTÇE, orta sütun halka + faz şeridi + alta-yapışık konsol, sağ ray equity sparkline'lı aday kartları. **Backend'de yeni endpoint yok** — `?view=mission` aynı state'in ikinci sunumu (`/agent` klasik görünümü korur); ayrışacak ikinci durum makinesi yaratılmadı. Eşleme şablondan ayrılıp `web/mission.py`'ye alındı (şablon yalnız sunum) ve **32 birim testle** kilitlendi (faz katlaması, lider seçimi, duraklatma, bütçe oranları, konsol sınıflandırması, şablon sözleşmesi); yerleşimin sert kuralları ayrı bir tarayıcı kontrolüyle ölçülür (`scripts/check_auto_cockpit.py`: uygulamayı süreç içinde ayağa kaldırır, `fake_auto_run.py` ile LLM tüketmeden RUNNING durumu enjekte eder, 1440×900 ve tasarımın "en kötü hâl" dediği 924×540'ta kokpit taşmıyor / faz şeridi tek satır / konsol ≥130px / slide-over açılıp kapanıyor / JS hatası yok). Birim testler CSS'i, tarayıcı kontrolü iş mantığını yakalayamaz — ikisi bilerek ayrı. Turda iki gerçek bug bu doğrulamalarla yakalandı: (a) palet `.mc` üzerinde tanımlıydı ama slide-over ve üst bar `.mc`'nin DIŞINDA render edilir → hiçbir token miras alınmıyordu (`:root`'a taşındı), (b) `state.get("started_at") or now` — `started_at` bir unix timestamp'tir ve meşru olarak `0.0` olabilir, falsy tuzağı geçen süreyi sıfırlıyordu (`is None` ile düzeltildi). Süit **700 geçti / 1 atlandı**, regresyon yok. Detay: [[auto_mission_control]]. **NautilusTrader kütüphanesine dokunulmadı** — salt web katmanı + yeni render modülü.

## Bilinen boşluklar

- BacktestNode yolunda per-trade detayı (giriş/çıkış zamanları, individual fill price, karar sebebi) şu an yok — sadece `total_positions` ve özet metrikler mevcut. **BacktestEngine yolu** artık per-trade giriş/çıkış sebebini (ateşleyen blok + değerleri, SL/TP/flip/eob atfı) `composer._decision_log` + emir tag'leri üzerinden yakalar; sebepler `/reports/detail` panelinde ve /backtest sonucunda görünür. Node yolunda strateji instance tutulmadığı için bu mekanizma kullanılamaz.
- Live [[environment_contexts]] Sandbox/Live bacağı hiç kurulmadı; `TradingNode` webapp'in scope'unda değil ama [[live_node]] stub'ı Nautilus tarafında bunu bekliyor.
- [[option_greeks_pipeline]] ve [[venue_reconciliation]] webapp'te ilgisiz — bunlar sadece Nautilus wiki tarafında yaşıyor.

- **Robustness→AI Plan köprüsü: overfitting-farkındalıklı öneri (2026-07-21)**: (1) **`propose_refined_description` yeni `robustness` parametresi** — `agent.py`'de imza `(raw, backtest_metrics=None, robustness=None)` oldu; `robustness={overfitting_score, verdict, wfo_efficiency, oos_sharpe, stability}` sağlandığında prompt'a overfitting-farkındalık bağlamı eklenir (düşük skor → sadeleştir, yüksek → güçlendir). (2) **`POST /backtest/plan` robustness form alanları** — `rob_overfitting/rob_verdict/rob_wfo_eff/rob_oos_sharpe/rob_stability` alır; `robustness_result.html` içindeki `(function(){set(...)})()` bloğu fragment swap'ında doğrudan hidden input'lara yazar. (3) **Robustness sonuç ekranı görsel yenileme** — `fragments/robustness_result.html`: hero karar kartı (ROBUST/DİKKAT/OVERFITTING), tüm KPI renk-kodlu (WFO/IS-OOS/MC), IS/OOS overfitting eşikleri 0.5/0.25 olarak düzeltildi, sekme açıklama kutuları eklendi. Salt `agent.py` + `web/routes/backtest.py` + `web/templates/fragments/robustness_result.html`; NautilusTrader kütüphanesine dokunulmadı.

- **Runtime-performans denetimi (2026-07-21)**: çok-ajanlı (64 ajan, çekişmeli-doğrulama) runtime-performans denetimi — **50 ham bulgu → 31 doğrulandı**; ayrı sentez sayfası [[nau_performans_denetimi]]. **4 HIGH**: (H1) LLM prompt-caching yok — `agent.py` `_create_message` sabit ~16.6K token sistem prefix'ini `cache_control` olmadan her çağrıda yeniden gönderir (en yüksek ROI, S efor: `system`'i content-block listesine çevir + son sabit bloğu `ephemeral` işaretle); (H2) `state.py` `iterations` listesi sınırsız büyür → RAM + her 2 sn poll'da O(n) render (deque `maxlen` fix'i `loop_runner.py` `len()`-tabanlı id'yi bozar → ayrı monoton sayaç şart); (H3) `sandbox.py` her guarded çağrıda `spawn` subprocess + ağır import (~1 sn cold-boot) — önceden-ısıtılmış killable worker havuzu (yalnız `daemon=True` guarded çağrılara; robustness child'ı kendi ProcessPool'unu açar); (H4) `composer.py` NAU indikatörleri her bar 260-pencereyi saf-Python sıfırdan hesaplar (~83–160 µs/çağrı). **En sert kısıt**: H4'ün incremental optimizasyonu **NAU_WINDOW=260 sabit-pencere paritesini** korumalı + `indicators.calc_*` ile bit-parite (<1e-9) testinden geçmeli — aksi halde uygulanmaz (NAU-uyum denetiminin performans-tarafı devamı). Doğrulama cazip ama yanlış fix'leri eledi: elle min/max CPython'da ~%15 daha yavaş, equity örnekleme `_mtm_equity` metriğini bozar, thread-executor killability'i kaybettirir. Güvenli hızlı kazanımlar: `sma` running-sum, deque min/max, `on_bar` FFI sinyal-guard'ı, `_extract_trades` itertuples, WFO fold ön-dilimleme, Monte-Carlo vektörizasyon. **Bu tur kod DEĞİŞTİRMEDİ** — denetim anlık görüntüsü; bulgular uygulandıkça kapanır. **NautilusTrader kütüphanesine dokunulmadı.**

- **Mimari denetim (2026-07-21)**: çok-ajanlı (58 ajan, çekişmeli-doğrulama) yapısal/mimari denetim — **45 ham bulgu → 41 doğrulandı**; ayrı sentez sayfası [[nau_mimari_denetimi]]. Baskın sorun: **niyet düzeyinde kalan katmanlama** (her sınır sızıyor) + **4 tanrı-modül** (`agent_backtest.py` 2923, `composer.py` 2246, `backtest.py` 2286/1978, `data.py` 1996) + **fonksiyon-içi import döngüleri** (`server↔routes` 43 late-import, `backtest↔data`, `sandbox→web.routes`). **Kritik yok.** En yüksek 3+1: **(H1)** engine `sandbox.py` web'e *yukarı* uzanır — `web.routes.agent_backtest._IPC_Q` global'ini mutasyona uğratıp private `_run_full_robustness`'ı çağırır (~250 satır engine orkestrasyonu route dosyasında); **(H2)** WFO batch timeout'u (600s) 900s dış kill altında kümülatifi sınırlamaz (`n_gen=5` → ~3600s), yavaş suite hard-kill'de tüm tamamlanmış iş çöpe; **(H7)** codegen doğrulaması `agent.py` private'larından import ediliyor — `from agent import _has_builtin` (`strategy.py:440`) **zaten canlı `ImportError`** (codegate'te isim `has_builtin`), güven sınırı 3 modüle yayılmış; **(H9)** global tek-instance `AppState`, session izolasyonu yok. Diğer HIGH: H3 servis-dikişi yok (dispatch 5+ yerde, `/run`↔`/sweep` recipe sapması), H8 sıralı/paralel WFO GA döngüleri sadece "parity contract" yorumuna bağlı (saparsa `NAUTILUS_PARALLEL`'e göre farklı kazanan), H10 built-in block ekleme 4-site shotgun. Doğrulama `data.py load_index_bars` hacim-sıfırlamayı **kasıtlı** olarak REFUTED etti. **Bu tur kod DEĞİŞTİRMEDİ** — denetim anlık görüntüsü. **NautilusTrader kütüphanesine dokunulmadı.**

- **Composer + Backtest birleşik `/studio` sayfası (A-UI + C-backend, 2026-07-21)**: iki ayrı sayfa (Composer `/strategy` + Backtest `/backtest`) tek sekmeli `/studio` sayfasında birleştirildi; aynı kapsamda backend duplikasyonu temizlendi. Karar dokümanı ikinci beyin vault'unda `nau_composer_backtest_birlestirme_2026_07`. **Motivasyon**: iki halin tek kalıcı bağı `strategy_catalog.json` (anahtar `spec_id`) idi ama Backtest'te kayıtlı strateji seçme UI'ı **hiç yoktu** — `backtest.py` `preferred_spec_id`'yi hesaplıyor fakat template render etmiyordu (ölü devir); ayrıca iki `ComposedStrategySpec` constructor'ı çelişiyordu (sizing schema drift bug'ı), `render_md` 3 kopyaydı, backtest'in el-yapımı `_CHAT_STORE`'u ChatStore'un çıkarıldığı orijinaldi. **5 faz, hepsi ayrı doğrulandı**: **(Faz 0)** `render_md` → `web/shared.py` (3 kopya silindi: strategy/backtest/wiki; `extensions` param'la wiki `toc` varyantı korundu). **(Faz 1)** `composer.build_spec()` spec-upsert servisi — iki constructor'ın UNION alanı + merkezî clamp; **sizing drift bug'ı düzeldi** (`trade_size_mode` whitelist artık `vol_target` dahil → save eskiden sessizce `fixed`'e düşürüyordu, artık round-trip eder). **(Faz 2)** backtest `_CHAT_STORE`/`_CHAT_LOCK`/`_chat_evict_locked` silindi → ortak `web.shared.ChatStore` get/commit desenine port (`chat_new`/`chat_turn`). **(Faz 3)** `_sid`/`COOKIE` → `web.shared.session_id`/`SESSION_COOKIE`; `_LAST_RESULT` process-global tek-slottan session-scoped (`sid→slot`, LRU 500) — çok-kullanıcı last-writer-wins gitti (run-worker sid'i request-anında yakalar). **(Faz 4)** yeni `web/routes/studio.py` + `studio.html` sekmeli `[Compose][Backtest][Analyze]`; iki gövde partial'e çıkarıldı (`compose_body.html`, `backtest_body.html`, `backtest_scripts.html` — eski route'lar + studio paylaşır). Üst sekmeler **AYRI namespace** `.lab-tab`/`labTab()`/`.lab-tabpanel` (backtest iç `.bt-tab`/`btTab()`'e dokunmaz — yoksa `querySelectorAll('.bt-tab')` iki seviyeyi birlikte toggle'lardı). Backtest'in `htmx:afterSwap`/`afterSettle`/`DOMContentLoaded` listener'ları tek kopya, `id==="result"/"plan-preview"/"chat-thread"` guard'lı (strategy id'lerini güvenle atlar). `base.html` nav tek "Strategy Studio" (eski Composer+Backtest girişleri kalktı); `server.py` studio.router; `GET /strategy`+`GET /backtest` root → 307 `/studio` redirect (deep-link `?spec_id=` korunur, HTMX endpoint'ler aynı prefix'te). **Ölü devir canlandı**: `catalog_list.html`'e "▶ Bununla backtest" satır aksiyonu + backtest gövdesine `<select name="spec_id">` picker (`preferred_spec_id` preselect) + `studioBacktestSpec()` JS (spec seç + sekme değiştir). Not: `/lab` rotası zaten otonom "Strategy Lab" pipeline'ınındı → yeni ad `/studio` seçildi (çakışma önlendi). **`_preview_signals` bilinçli birleştirilmedi** — aynı ad, farklı işlev (strategy: custom-block `evaluate()` çalıştırır; backtest: LLM'siz built-in indikatör yaklaşımı). **Doğrulama**: 105 ilgili test (composer flow/spec round-trip/robustness/mtf/trade-reasons) yeşil; 5 kırık test baseline'da da kırık (subprocess-pool/golden-parity, ilgisiz — `git stash` ile izole); driver ile gerçek app + backtest PASS; TestClient turu (render/nav/redirect/HTMX endpoint canlılığı) ✓; ruff temiz. **NautilusTrader kütüphanesine dokunulmadı** — salt route/composer/shared/şablon/CSS.

- **Güvenlik + dayanıklılık düzeltme turu (2026-07-25)**: 5-eksenli incelemenin (kod/güvenlik + mimari + performans + entegrasyon testleri + E2E; mimari/performans eksenleri [[nau_mimari_denetimi]] ve [[nau_performans_denetimi]] ile örtüşür) **kritik/yüksek bulgularının tamamı kapatıldı**; ayrı sentez sayfası [[nau_guvenlik_dayaniklilik_duzeltmeleri]]. **(1) Sandbox yazma-kaçışı** — `codegate` AST'si attribute `ctx`'ini kontrol etmiyordu ve namespace'e CANLI `math`/`statistics`/`ind` enjekte ediliyordu: `ind.calc_rsi = ...` yazan bir blok sunucu süreci ömrü boyunca tüm stratejileri zehirliyordu (önizleme/smoke yolu sandbox sınırının dışında koşar). İki katman: attribute'a yazma/silme reddi + `safe_module_proxy` (salt-okunur sığ proxy, aynı `_ALLOWED_ATTRS` kaynağından). Diskteki **233 blok yeniden doğrulandı, 0 regresyon**. **(2) Registry/katalog veri kaybı** — `_read_registry`'nin "her hatada karantina"sı + `load_catalog`'un `custom_names = set()` fallback'i zinciri, anlık bir dosya kilidini kalıcı strateji kaybına çeviriyordu; artık I/O hatası fırlatılır, "bilinmiyor" `None` ile taşınır ve **budama/yeniden yazma yapılmaz**. **(3) Index path traversal** — `_ticker_to_filename` yalnız `:`/`/` değiştiriyordu (`\` ve `..` geçiyordu, ticker doğrudan `/backtest/run` gövdesinden gelir); artık `_bybit_cache_path` ile aynı charset sözleşmesi. **(4) Studio motoru** artık `run_backtest_guarded(force_subprocess=True)` üzerinden (bkz. `strategy_studio/` satırı) + sweep başına tek bar yüklemesi. **(5) Deploy geçidi** taslak koşusuyla atlatılabiliyordu → `studio_runs.defn_hash` içerik hash'i (sürüm defteri alanları dışlanır, böylece *düzenle→backtest→kaydet→deploy* tek tık kalır). **(6) Orta**: robustness eviction `KeyError`'ı (daemon thread ölümü + öksüz child), optimize grid'inin `max` aşımı, `kill_switch` 500→422, `store.save()` sürüm yarışı, kapanmayan SQLite bağlantıları, hızlı yol sermaye/komisyon paritesi, `BacktestPool` zombie reap, AI döngüsünün kullanıcı düzenlemesini ezmesi. **(7) Windows**: index veri hattı `bash -c "gunzip|awk"` yerine saf Python (gzip+csv) — bu hat Windows'ta hiç çalışmıyordu (`bash` bozuk WSL launcher'ına çözülüyordu). **Testler: 518 geçen/12 hatalı → 539 geçen / 0 hatalı** (9 yeni regresyon testi: deploy-geçidi istismarı, registry dayanıklılığı). 7 eskimiş `describe` testi yeni JS-chain sözleşmesine (`data-bt-chain-url`) taşındı; 3 golden hash yalnız **ekonomik alanlara** daraltıldı (`pnl`/`n_trades` birebir korunduğu doğrulandıktan sonra yeniden üretildi). E2E: driver ile 525.600 barlık gerçek backtest PASS. **NautilusTrader kütüphanesine dokunulmadı.**

- **İkinci tur inceleme (2026-07-26)**: düzeltme turunun ardından yapılan bağımsız incelemenin 4 bulgusu; detay [[nau_guvenlik_dayaniklilik_duzeltmeleri]] §9. **(1) `promote_draft` atomik değildi (ORTA)** — üç ayrı DB işlemi, araya düşen `save_draft` sondaki delete tarafından siliniyordu (bkz. `strategy_studio/` satırı). **(2) Sweep'ler bayat adaptörle koşabiliyordu (ORTA/DÜŞÜK)** — `OPTIMIZER` import anında yakalıyordu (bkz. `web/routes/strategy_studio.py` satırı). **(3) `ruff check .` tüm repoda düşüyordu (DÜŞÜK)** — ürün kodu temizdi, vendor edilmiş `.claude/skills/**` helper'ları 22 hata veriyordu; CI aynı komutu koşuyorsa kimsenin yazmadığı kod build'i kırardı → `.claude` ruff `extend-exclude`'una eklendi (skill güncellemesi o dosyaları zaten üzerine yazar, düzeltmek kalıcı olmaz). **(4) `EXTERNAL_CATALOGS` uyarısı — ORTAM, bug değil**: varsayılan yol başka makinenin diskini gösteriyor; uyarı L31'de bilerek eklenmişti, mantık değişmedi, yalnız mesaj eyleme dönüştürüldü (hangi env değişkeni, ne zaman yok sayılabilir). **Kapatılmadı (bilinçli)**: Studio route bağımlılıklarının import-anı kurulumu için DI/factory refactor'ü — somut kusuru (2) düzeltildi, kalanı davranış-koruyan refactor değil tasarım değişikliğidir (~40 `store.` çağrı yeri) ve sahibinin kararıdır. **Test yöntemi notu**: `promote_draft` için ilk yazılan thread-yarışı testi **eski kodda da geçti** (boşluk mikrosaniye mertebesinde — zamanlamaya dayalı test iki uygulamayı ayıramıyor); atıldı, yerine düzeltmenin özü ölçülüyor: `promote_draft` **tek bağlantı** açmalı (eski kodda `3 != 1` ile düşer, doğrulandı). Testler: 539 → **545 geçen / 0 hatalı**; `ruff check .` (tüm repo) temiz.

- **Nav'a "Strategy Builder" linki + bayat doküman keşfi (2026-07-26)**: kullanıcı `studio_app`'i ana app'e APIRouter olarak taşıma/mount etme işini istedi ama iş **zaten tamamlanmıştı** (bkz. `strategy_studio.py` satırı, 2026-07-25 merge) — talebin kendisi bayat bir öncüle dayanıyordu. Tek gerçek eksik: `base.html`'de zaten `/studio`'ya (Composer+Backtest) giden bir "Strategy Studio" linki vardı ama kurucuya (`/studio/{strategy_id}`) giden hiçbir link yoktu. Aynı etiketi iki farklı sayfaya vermemek için yeni link ayrı isimle eklendi: **"Strategy Builder" → `/studio/wt-funding-v3`** (kullanıcı onayladı). Bu arada `studio_app/wiki/strategy-studio.md`'nin merge-öncesi durumda dondurulmuş kaldığı (hâlâ "pending merge" + "82 test" diyordu) fark edildi ve düzeltildi — bkz. [[strategy_studio]]. **Ders**: aynı gerçeği (test sayısı, merge durumu) birden çok dosyada tutmak, biri güncellenip diğeri unutulduğunda kullanıcıyı yanlış bir "hâlâ yapılmamış" öncülüyle işe başlatabiliyor. `tests/studio` bu turda **196 geçti / 1 atlandı** olarak yeniden ölçüldü (README'deki "140" da bayatmış). **NautilusTrader kütüphanesine dokunulmadı** — salt `base.html` nav + iki wiki sayfası.

- **Strategy Builder ana kabuğa alındı + sayfa markası ayrıştırıldı (2026-07-27)**: kullanıcı ekran görüntüsüyle iki şey bildirdi — (a) kurucunun logosu ("StrategyStudio") sol nav'daki farklı bir sayfaya giden "Strategy Studio" linkiyle karışıyordu, (b) sayfa diğerleri gibi kabuğun içinde açılmıyordu. (a) için sayfa markası nav etiketiyle hizalandı (**Strategy Builder**); kod/rota adları (`web/routes/strategy_studio.py`, `strategy_studio/`, `/studio/{id}`) bilerek tarihsel kaldı. (b) için `studio/page.html` `base.html`'i extend eder oldu (`{% block head %}` + `{% block title %}` eklendi, body sınıfı `page-builder`, route `active="studio_builder"` + `page_title` geçirir). **Asıl iş şablon değil CSS kapsamaydı**: `studio.css` global yazılmıştı — `:root` içinde app.css ile aynı adlı `--panel`/`--panel-2`, `body{height:100vh;overflow:hidden}`, `header{…}`/`footer{…}` eleman seçicileri ve `.btn`/`.metric`/`.tab`/`.modal`/`.seg` gibi ortak sınıflar; gömülünce kabuğu **hata vermeden** yeniden boyayacaktı. Tüm kurallar `.studio-embed` altına kapsandı (`:root`/`body` → sarmalayıcı, gerisine önek — mekanik dönüşüm); kapsama özgüllüğü de doğru tarafa çevirir (`.studio-embed .btn` 0,2,0 > `.btn` 0,1,0), yükleme sırasına bağımlılık kalmaz. Kabukta tek ek: `body.page-builder .content` padding'i ve büyümesi kaldırıldı (kaydırma kurucunun kendi panellerinde kalsın). İki incelik: sayfanın kendi cdnjs htmx kopyası silindi (kabuğunki kalır — iki kopya çift dinleyici demekti) ve `rem` **kökten** çözüldüğü için kapsanamaz → 15px kök ölçüsü sayfa-yerel `<style>:root{font-size:15px}</style>` ile korundu (app.css'te hiç `rem` yok, ölçüldü). Sayfa içi logo kaldırıldı — topbar zaten adı yazıyor. Kalıcı ders ikinci beyin vault'unda: `global_css_kapsama_tuzagi`. **Testler: 544 geçti / 3 atlandı**, `ruff check .` temiz (`test_promote_draft_atomicity` bir kez düştü, yeniden koşumda 5/5 — eşzamanlılık flake'i, ilgisiz). **NautilusTrader kütüphanesine dokunulmadı** — salt şablon/CSS/route.

- **Strategy Builder canvas görünümü (2026-07-27)**: kurucunun formu, aynı stratejinin **node graf'ı** olarak ikinci bir yüzey kazandı (`/studio/{id}/canvas`); form görünümü aynen durur, ikisi header'dan birbirine link verir ve ikisi de aynı working copy'yi okur. Tasarım repo kökünde `CANVAS_DESIGN.md`, 4 faz ayrı commit. **Merkezî karar — serbest node editörü kullanılmadı**: React Flow/Drawflow gibi kütüphaneler kullanıcının kenar çizmesi üstüne kuruludur, oysa `StrategyDefinition` serbest graf değil **kısıtlı bir ağaçtır** (`regime? → entry/exit RuleGroup → Rule → risk → allocation`); keyfi kablo geçersiz strateji üretirdi ve bedeli, çizilebilen-ama-geçersiz her kombinasyon için validasyon yazmak olurdu. Yerine kenarlar şemadan **türetilir**: yeni `strategy_studio/graph.py` `to_graph(defn, ghosts=None)` saf fonksiyonu `{nodes, edges, meta}` üretir (store'a dokunmaz, request görmez, testte pinlenir), `web/static/canvas.js` katman indekslerini piksele çevirir — düz SVG `viewBox` zoom/pan + minimap + klavye, bundler yok. **Görünüme dair hiçbir şey saklanmaz**: pozisyon/zoom/kenar listesi şemaya girmedi, layout her render'da yeniden hesaplanır; katman ataması bilerek sunucuda (tarayıcıda değil) ki kolon sırası test edilebilsin. **Eklenen üç route'un üçü de GET** (`/canvas`, `/canvas/graph`, `/canvas/inspector/{node_id}`); yazan her şey — drop, chip düzenlemesi, ✕, Save/Discard/Run — mevcut uçlara iner, bu yüzden "entry en az bir kural tutar" gibi sunucu kuralları yeniden yazılmadan bağlar (aynı 422, aynı error banner; e2e'de doğrulandı). Ghost'lar (bekleyen AI önerileri) tanımda değil store'da yaşadığı için `to_graph`'a **argüman** olarak geçer — modül kendi girdisini aramaya gitmez. **Uygulamada çıkan üç kısıt**: (1) inspector partial'ları render ediyordu ama sayfa `studio.js`'i yüklemiyordu → chip'ler düzenlenebilir *görünüyor*, tıklayınca hiçbir şey olmuyordu (konsolda hata bile yok — "markup'ı aldım" ile "davranışı aldım" farkı sessizce kaybolur); (2) bir rule node'u tek başına `_rule.html` ile açılamaz, çünkü kontrolleri `closest .block`'u hedefler → **kapsayan blok** render edilir; (3) tasarım mutasyonların `HX-Trigger: studio:changed` yaymasını varsayıyordu, mevcut route gövdelerine dokunmamak için canvas başarılı her non-GET sonrası graf'ı kendisi yeniden çekiyor (sunucuda sıfır değişiklik). Ayrıca OOB side-panel swap'i için gizli `#side-panel` çukuru (yoksa her düzenlemede htmx "no target" hatası) ve açılış zoom'una %62 tabanı (geniş-alçak graf kare panelde okunamayacak kadar küçük fit'liyor). **İsim tuzağı**: tasarım dokümanı `web/routes/studio.py` diyordu, Strategy Builder aslında `web/routes/strategy_studio.py`; `studio.py` bambaşka bir sayfadır. **Testler: 545 → 584 geçti / 3 atlandı** (39 yeni: `test_graph` 12, `test_canvas` 5, `test_canvas_inspector` 9, `test_canvas_mutations` 6, `test_canvas_ghosts` 7), `ruff check .` temiz, her faz headless Chromium'da sürüldü. **NautilusTrader kütüphanesine dokunulmadı** — salt yeni modül/route/şablon/statik.

- **Strategy Builder enstrüman seçici (2026-07-27)**: kullanıcı "backtest edeceğim sembolü seçebilmeliyim, sadece 3 sembol görünüyor" dedi. Kök neden iki ayrı yerdeydi ve yalnız biri kurucudaydı: `studio/_instruments.html`'in "+ Add" çipi **tıklanabilir görünen bir stub**'tı (`title="Phase 2+: add instrument via config"`, arkasında endpoint yok) — sembol listesi stratejinin kendi `instruments` alanıydı ve yalnız aktif/pasif toggle'lanabiliyordu, seed fixture'ın üç enstrümanı (`XAUUSD`/`EURUSD`/`NAS100`) sabit kalıyordu. (Aynı anda AI cockpit/Lab/Agent sayfalarındaki `<select>`'ler HTML'de sabit BTC/ETH/SOL tutuyor; bu tur kapsam dışı bırakıldı — Backtest sekmesi zaten `_bybit_symbols()` ile katalogdan besleniyor.) Çip artık gerçek bir seçici: `POST /studio/{id}/instruments` (symbol + timeframe) ve `DELETE /studio/{id}/instruments/{symbol}` eklendi, mutasyonlar `add_instrument`/`remove_instrument` olarak `strategy_studio/mutations.py`'ye indi. **Üç tasarım kararı**: (1) sembol alanı `<select>` değil **`<datalist>`** — katalog sembolleri yalnızca öneridir, `load_bybit_bars` bilinmeyen sembolü ilk koşuda API'den çeker, kapalı liste kullanıcıyı kataloğa hapsederdi; öneriler **sadece linear** kategoriden, çünkü `BybitBarsAdapter._recipe` her tarifi `category="linear"` kurar ve inverse/spot bir sembol yanlış piyasadan çekilirdi. (2) timeframe listesi `BYBIT_ALL_INTERVALS` etiketlerinden türer — adaptörün `_interval_code`'u ile aynı tablo, yani sunulan her etiket tanımı gereği yüklenebilir. (3) çipte toggle ve ✕ **kardeş** öğeler: `hx-delete`'i `hx-patch`'li çipin İÇİNE koymak tek tıkta iki istek attırıyordu. Silme, toggle'ın "en az bir enstrüman aktif kalmalı" kuralını miras alır; sembol tekil (rota `{symbol}` ile eşler, ikinci kayıt ayırt edilemezdi). Katalog taraması `_ctx`'te her blok render'ında koştuğundan 60 sn TTL ile cache'lendi (`/data`'dan yeni indirilen sembol yeniden başlatmadan görünsün). **Testler: 588 geçti / 3 atlandı** (4 yeni), `ruff check .` temiz; canlı sunucuda ekle/çift-ekle/geçersiz-sembol/geçersiz-TF/sil/son-aktif-koruması uçtan uca sürüldü. **NautilusTrader kütüphanesine dokunulmadı.**

<!-- BACKLINKS:BEGIN -->
## Referenced by

- [[getting_started_roadmap]]
- [[llm_maliyet_kaldiraclari]]
- [[nau_deepr_toplu_sertlestirme_2026_08]]
- [[nau_guvenlik_dayaniklilik_duzeltmeleri]]
- [[nau_mimari_denetimi]]
- [[nau_performans_denetimi]]
- [[parquet_data_catalog]]
- [[portfolio]]
- [[strategy_studio]]
- [[v1_to_v2_migration_lessons]]
<!-- BACKLINKS:END -->
